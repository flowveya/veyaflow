# 2026-09-03 — coding lane

## Skeppat

- **#95** — Brand Pack compliance rows compute their value column from the measured
  label width instead of a hardcoded `margin+38`. `eada442`. The 32-character
  `EU-established economic operator` label had been overprinting its value on a pack
  shipped 2 Sep. `Math.max(38, labelW + 2)` keeps the floor, so every label that
  already fitted lands on exactly the pixel it landed on before — the cosmetic path is
  unchanged **by construction**, not by inspection. Smoked: label prints in full with a
  gap before the value, short labels unmoved.
- **#96A / #96B / #97** — section titles now pass through `stripMd`; the preamble before
  the first `## ` is dropped rather than rendered as a phantom section; the unset target
  market is omitted rather than printed as the literal "Market". `39b7725`. Packs went
  from 10 pages to 8. Smoked.
- **Target market field** (number owed) — placeholder option, no retailers offered until
  a market is chosen, a market guard before the retailer guard, and the `||'Denmark'`
  prompt fallback removed. `3676623`. Smoked: field opens on `Select market…`, Generate
  refuses without a market, and Denmark now appears on every surface **including the AI
  pages** — the DENMARK/Market contradiction in one document is closed.

`index.html` moved `a7f44f07 → 844d3887 → 3ff93df4 → be86c431`. Each value named by the
lane after reading the edit at the bytes and confirming a uniform shift below the edit
point with nothing above it moved.

**Infrastructure shipped:** the standing verification battery (`verify.sh` + `verify.js`,
`7b42528`) — one command, 40 gates, NOT FOUND semantics throughout, and a NOT CHECKED
block so a green run cannot be mistaken for a shipping verdict. `ast_verify.js`
superseded: its extraction parsed block 1 only, so every "acorn clean" it ever produced
covered 63% of the file. Spec archive (`3fb36eb`), protocol v2 (`ae391ee`) and
`CODING_STATUS.md` (`09d934f`) are now under git.

## Fynd

- **#99 — the repo root is the public web root.** `netlify.toml` sets `publish = "."`, so
  every tracked file was fetchable by name. Confirmed in a browser:
  `veyaflow.netlify.app/CODING_STATUS.md` served the entire backlog and
  `/netlify/functions/share-dpp.js` served the server-side validation source. No key
  values — the no-secrets rule held — but the backlog states that ~13 RLS-enabled tables
  carry zero policies and that `session_id` is an unexpiring bearer value. Mitigated with
  24 forced-404 rules scoped from `git ls-files`. **Not closed:** blocking by enumeration
  depends on remembering. Structural fix is moving the app into its own directory.
- **AI prose fabricated regulatory credentials.** The Kicks pack asserted a Swedish
  operator registration, GPSR compliance and the existence of a clinical substantiation
  file — four claims, all favourable to the brand, all contradicted by the pack's own
  verified page two. Escalated and ruled the same evening.
- **`verifyBrandPackProof` has two distinct defects.** Its vocabulary is a blocklist of
  certainty phrasings (`verified`, `fully compliant`, `substantiated?`) and misses plain
  declaratives — it missed "substantiation" by one word-form. And line 16869 calls
  `.match()` on non-global patterns, so **at most one hit per pattern is ever flagged**,
  however many times it occurs. The number patterns two lines above do it correctly.
- **#98 shrank under scrutiny, and one of my claims was false.** It began as "two
  constants disagree", was specced as five paths across three constants, and resolved to
  two live render sites. My claim that the DPP Generator shows a cosmetics brand the
  Wellness assertion is **wrong** — the filter at 34732 tests keys that do not exist, so
  nothing renders. Retracted here and on the pilot gate page. CC caught that
  `espr_disclosure` is scoped `cat:["Fashion & Apparel"]`, a scope contradiction the lane
  read past on 2 Sep.
- **Truncated generations, three instances.** Every pack's final section ends mid-token —
  `positions at **Step`, `| Step`, and worst, `veyaflow.com/dpp/vf-1`: a **cut passport
  URL inside a compliance section**. Consistent with token exhaustion; nothing detects it.
- **Three functions carry the same copied defects.** `generateBizCasePDF` has
  `||'Denmark'` feeding its prompt (15814) and `||'Market'` printing in its PDF (30531),
  with no placeholder and no guard; L25938 holds a fourth copy of the preamble defect.
  Today fixed one of the three.

## Metodfynd

- **Two smoke rounds lost to unpushed commits.** PDFs were generated against code that
  existed only on the laptop while the battery reported GREEN. The deploy check is now
  mandatory before any smoke, and an `origin/` comparison goes to the front of the
  harness queue: *run smoke against code that has actually shipped* — the sibling of
  *run smoke where the data is*.
- **A guard in my own spec counted a comment as a call site** — precisely the epitaph
  error this lane wrote into `verify.js` two days earlier. True count was 2; the fix
  added a third; the guard would have read as satisfied at 3 for the wrong reason.
- **Witness lines pinned to fixed line numbers degrade after every edit** and should be
  anchored by content instead.
- **A smoke step named a menu path that does not exist.** Brand Pack is under MY BRAND,
  not Buyer Documents. Fifth instruction in two days that did not survive contact with
  the UI.
- **The `---` absent from one pack was generation variance, not a fix.** #96C was held;
  reading its absence as success would have closed a finding that is still open.

## Carried as debt

- **#95 step 1 unrun** — proving the cosmetic path unmoved needs the serum as hero, there
  is no picker, and delete-and-re-add would orphan a published passport (#86).
- **#96C held** — extending `stripMd` moves Business Case output; the catman prompt
  instructs pipe tables.

## Öppet / behöver Strategy

- **#56 gate-versus-warn** — still blocking, since 2 Sep. The `[FLAG_COPY]` line has been
  printing on every buyer-facing cover for days and packs kept generating, which is
  itself evidence that warning does not work.
- **Five findings await numbers:** the dead render block at 34732; the ESPR Article 24
  declaration `.txt`; `smartEmpty` (a ~90-line empty-state system never called anywhere);
  the parked domain printed as a live passport reference in the Brand Pack; the target
  market field.
- **Language of these entries.** Body written in English to match `CODING_STATUS.md` and
  the spec archive, with the Swedish headings from the protocol template. Say if you want
  the whole entry in Swedish — translating technical detail carries its own risk, which
  is why it defaulted this way.
- **Backfill for 2026-09-02?** That day's record lives in the handoff and
  `CODING_STATUS.md` rather than in this structure. Not written unless you want it.
