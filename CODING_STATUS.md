# VeyaFlow — CODING LANE STATUS

The coding lane's own memory. Plain text, in the repo, under git — so the lane can read it without a shell and it cannot silently diverge.

Seeded 2 September 2026 from the CODING block of `VeyaFlow_Daily_Log.docx`. That block in the `.docx` is now a pointer to this file, not a copy.

Owned by the coding lane. Strategy and Design do not edit this file; they read it (paste it to them when asked). Day-by-day narrative history stays in the `.docx`.

> **CC — READ THIS BEFORE ANYTHING ELSE IN THIS FILE.**
> This is the coding lane's memory, not a work instruction. It is a backlog: eighteen-plus open findings, rulings, priorities and things deliberately held. **Nothing in this file authorises an edit.**
>
> Your scope is the spec you were handed and nothing else. If you are working in `index.html` and notice something described here — an open finding, a queued batch, an obvious adjacent fix — **do not fix it.** Report it in your report-back and stop. A fix made outside the spec cannot be verified against it, which is hard stop 3 in the protocol, and it costs more to unpick than it saves.
>
> This warning exists because the file lives in the repo you work in, so you can open it whether or not anyone pasted it to you. Being able to read something is not being told to act on it.

Coding lane refreshed 2 September 2026 (evening). (Strategy / Design / Charlotte lanes below remain as-of their own dates — refresh owed by their owners.)

---

SHIPPED TO DATE — EIGHT batches on coding-aug2026: Truth Batch fbc351e · routing f972ad2 · save integrity 8fd92bc · readiness 63cf567 · buyer-surface truth pass 9e944fb · DPP truth pass 7f82323 · DPP export truth pass a29c8cc · REGIME TRUTH PASS 649f0ef (2 Sep, a29c8cc..649f0ef, 1 file, 340 insertions / 49 deletions). All eight byte-verified before commit.

BATCH #6 SMOKE — PARTIALLY DONE, still owed in part. Batch #7's smoke covered step (1), the unpublished DPP refusing its exports. STILL UNRUN: (2) estimate-only SKU → the public passport shows the figure WITH the Estimated badge (previously the whole row vanished); (3) measured value with a leftover estimate → NO badge; (4) the LED Face Mask generator row no longer reads 'CPNP' and the ESPR line reads as a brand-category statement; (5) the 'data advantage' panel is gone. Carried forward deliberately rather than quietly dropped.

#53 REGIME LABELS — SHIPPED 2 Sep as 649f0ef (Shipment 1 + 1b). The label now routes off the SKU framework through ONE table, FRAMEWORK_VOCAB, with three accessors (getOperatorRegime / getSafetyDocRegime / getNotificationRegime) and identical null semantics: cosmetic → 'EU Responsible Person' / 'Safety Assessment' / 'CPNP'; device → 'EU-established economic operator' / 'Technical file' / no notification; beauty_accessory → operator / 'Safety documentation' / no notification; anything else (unknown, textile, food, supplement) asserts NOTHING. NO ARTICLE NUMBERS — Strategy ruled 1 Sep that citations wait for RP confirmation and then enter as registry data with a source. A separate brand.euOperator field was added (never sourced from euResponsible: the same company may hold both roles but holding one does not confer the other). THE REGULATORY HALF STILL OPEN: which instrument grounds the obligation for a device vs a non-harmonised accessory — RP question four, alongside MDR Annex XVI, REACH Annex XVII and the substance discriminator.

BATCH #7 SMOKE — DONE, 5 of 5 PASSED (2 Sep). QR displays veyaflow.com · QR download refuses on the unresolved-domain gate and explains why · JSON and PDF download normally and are NOT gated on that flag · unpublished passport refuses all three on publication grounds · @context unchanged. Step 4 needed an unpublished passport simulated by deleting publishedVersion locally; the restore snippet failed because the DPP save path PRUNES UNKNOWN KEYS (do not stash a backup inside a store that prunes — the _pvBackup lesson). Restored via Re-publish, which takes the value from the server.

BATCH #8 SMOKE — 6 OF 6 PASSED (2 Sep, evening, desktop). (1) Face Serum spec sheet byte-unchanged: 'CPNP notification', 'EU Responsible Person — Cosmeservice GmbH · rp@cosmeservice.com' with the name+email format preserved, 'RP agreement renewal'. (2) LED MASK spec sheet: 'EU-established economic operator — not confirmed', NO CPNP row, Cosmeservice GmbH NOWHERE, header 'Cloud & Glow · Device'. (3) Serum DPP PDF has NO CE marking row; mask DPP JSON compliance block = {ceMarking:'Yes', euEconomicOperator:null, safetyReference:null} — no euResponsiblePerson key, no cpnp key, operator key NAMED BY THE REGIME. (4) Brand Pack with the mask as hero: no CPNP row, 'EU-established economic operator — Not set', 'Technical file — Not set'. (5) Apotek article template with the mask: the EU Responsible Person column shows a neutral grey em-dash, NO badge and NO 'fix in SKU' prompt — CC's 'a false claim can be phrased as an instruction' fix verified in the wild. (6) Brand Profile carries the EU economic operator block, empty, with its explanatory copy and an ENTER YOUR ECONOMIC OPERATOR button. THE FINDING THE BATCH EXISTS FOR IS FIXED. SURFACE NAME FOR THE RECORD: it is the SPEC SHEET (Menu → Buyer Documents → 'Spec sheet · data artifact' → 'Generate, VeyaFlow-standard'); 'Compliance Declaration' is MY name for it and appears nowhere in the UI. PLUS THE #93 SIZING TEST, RUN AND CONCLUSIVE: mask spec sheet with ceMarking='Yes' = Document ID 916a40d70242; changed to 'In progress' = 64898e75186e; set back to 'Yes' = 916a40d70242 AGAIN, across separate generations minutes apart. The ID MOVED on a payload field and RETURNED EXACTLY. That rules out all three failure modes at once — the hash is content-derived not time-derived, it is stable and reproducible, and the mechanism reads the payload correctly. #93 IS THEREFORE A FIELD-LIST FIX, NOT A BROKEN MECHANISM: add cpnp, safetyRef, brand.euResponsible.renewalDate, the SKU-level euResponsible and certifications to _ssCanon. Small, rides in batch #9 rather than needing its own. THE URGENCY IS UNCHANGED — cheap today, permanent once documents have been issued under the old scheme, which makes it the easiest kind of thing to fix and the easiest kind to postpone into a corner.

NEXT — BATCH #8 SHIPMENT 2 (#73), SPECCED AND WAITING. A real beauty_accessory branch in scoreReadiness, inserted BEFORE isUnknown, total 24 points so the CEILING DOES NOT MOVE and only the unearned part disappears: EU economic operator (via _operatorStatus) · material composition · REACH Annex XVII screening · GPSR technical documentation — all AMBER, matching euRpMissing, because inconsistent severity for the same class of obligation across frameworks is what teaches a user to stop believing the colours. Plus the two form fields those checks need (reachAnnexXVII with 'Not applicable' modelled DISTINCTLY from unanswered; gpsrTechFile, rendered label 'Safety documentation' to match FRAMEWORK_VOCAB), four BLOCKER_RESOLUTION entries with any reused figures explicitly marked, READINESS_FIELDS additions, and THE DEVICE BRANCH GAINING THE OPERATOR CHECK (fixing the accessory and leaving the device wrong is the narrow-anchor pattern with extra steps). RULED 2 Sep: THE UNKNOWN BRANCH LOSES ITS +6 AND ITS GREEN — batch #5 let it run the RP check as 'framework-agnostic', and batch #8 falsified that premise (two roles, two instruments, two fields), so it is #53 inside the scorer. This overturns part of my own #50 ruling; recorded as an overturn rather than quietly amended.

WHY #73 MATTERS MORE THAN IT LOOKS — beauty_accessory is ONE OF THE THREE PRODUCT TYPES THE UI OFFERS (_CURRENT_PRODUCT_TYPES_LIST) and it falls through to else { comp += 24; }: 24 compliance points, no checks, no blockers. An UNKNOWN SKU raises an amber and scores nothing; a STATED accessory raises nothing and scores 24. SETTING THE PRODUCT TYPE CORRECTLY MAKES A PRODUCT LOOK MORE COMPLIANT THAN LEAVING IT BLANK — the exact inversion batch #2 was built to prevent. It fires the moment the face tape is entered as an accessory.

OPEN FINDINGS QUEUE — #88 THE PITCH PROMPT INSTRUCTS FABRICATION (L25129): 'if EU RP is "Not set", say the appointment is in progress, never claim it is confirmed' — inside the same rule string that says 'State each item's factual status only' and 'NEVER assert that compliance is current/complete/done'. The instruction CONTRADICTS ITSELF: forbids upgrading absent→present, mandates upgrading absent→in-progress, and nothing in the data establishes an appointment is in progress. Plus the regime half: for a device the block now reads 'EU-established economic operator: Not set' and the rule still talks about an RP appointment. NOT a labelling defect — a fabrication instruction in a pitch sent to a buyer. · #89 a sixth CPNP assertion on Brand Pack surface A (L16708 hero banner), ungated, directly above the row 1E gated — held out of 1b on purpose and checked at the bytes: neither C&G SKU carries a CPNP, so it renders nothing today. · #90 daysUntil clamps with Math.max(0,…) so EXPIRED deadlines render '0d' and the calendar renders 'Today'; both live banner items are 33 and 34 days OVERDUE, and renderRegStrip sorts ascending so expired items sort to the FRONT permanently. One implementation, two surfaces. · #91 the in-app DPP Data Summary falls through to ['Regulatory reference', cpnp||ceMarking||novelFoodStatus] for accessory and unknown, scavenging whichever field is populated — and now DISAGREES with its own JSON export, which after batch #8 emits no key at all. · #92 THE SPEC SHEET PRINTS AN EXPIRED RP MANDATE FLAT. The serum's sheet renders 'RP agreement renewal 2025-12-31' as an ordinary data row with no expiry test, while scoreReadiness raises an amber euRpExpired on the SAME value and _rpDateExpired sits one call away with nine existing callers. That is finding #50 — an RP that EXISTS is not an agreement that is CURRENT — surviving on a surface batch #5 never touched: batch #5 fixed the SCORE, the DOCUMENT still prints it flat, and the document is the thing that leaves the building. Third instance this week of in-app state and exported artifact disagreeing (with #91 and the _dppFwFields divergence). Ships with #91. · #93 THE INTEGRITY STAMP DOES NOT COVER THE REGULATORY SECTION — the most serious finding of the day, and demonstrated live during smoke. Charlotte added a CPNP number to the serum, regenerated the spec sheet, and the Document ID did NOT change: 56220dbef259 on both the version with no CPNP and the version reading CPNP-2025-0231233. Checked at the bytes: _ssCanon (L29769) hashes name, EAN, article no, net content, country, shelf life, device flag, model, voltage, ceMarking, docOnFile, materialComposition, INCI, RSPs, MOQ, lead time, claims and brand.euResponsible.NAME — and OMITS sku.cpnp, sku.safetyRef, brand.euResponsible.renewalDate, sku.euResponsible and sku.certifications. So almost the entire Regulatory status section sits outside the stamp, while the DEVICE's regulatory fields (ce, doc) are inside it — inconsistent as well as wrong. The footer claims 'content fingerprint identifying this version' and the code comment claims 'any content change changes it'; both are false for the compliance section. THIS IS WORSE THAN NO STAMP: a document with no fingerprint promises nothing, while this one promises version identity, in a footer, on the page a retailer would check, and fails on the fields they would check it for — it invites exactly the reliance it cannot support. Truth-Batch class: a claim the app makes about ITSELF that is not true. TIMING IS STRUCTURAL, same shape as the stable brand id: fixing it changes the payload and therefore EVERY Document ID the app would produce, so any spec sheet already issued becomes unreproducible. Free today because nothing has gone to a real retailer; permanent afterwards — broken fingerprint history, or two hashing schemes forever. Charlotte is keeping both PDFs as evidence. **ADDED 2 SEP EVENING — A SIXTH OMISSION, FOUND WHEN VERIFYING THE GATE PAGE AT THE BYTES: sku.productType IS NOT IN THE PAYLOAD EITHER.** _ssCanon carries only `dev:isDevice`, a boolean. Changing a SKU from cosmetic to beauty_accessory leaves isDevice false, so THE DOCUMENT ID DOES NOT MOVE — while the operator row, the vocabulary and the regime printed on the page all change, because those route through resolveProductFramework. The fingerprint is blind to the exact axis batch #8 made load-bearing. The field-list fix must add the RESOLVED FRAMEWORK, not only the five regulatory fields. · #94 THE HERO SKU CANNOT BE CHOSEN. const heroSku = skus.length>0?skus[0]:null, identically in FIVE places; there is no picker, no heroSkuId, no setter anywhere in the file. A brand cannot choose which product fronts the pack it sends to a buyer. AND THE BANNER SAYS 'Hero SKU detected' — 'detected' claims a determination that was never made; it is array position. A small truth defect wearing a product gap. · #95 TEXT OVERLAP ON THE BRAND PACK PDF — MY REGRESSION FROM BATCH #8. The compliance rows render as doc.text(label, margin, y) and doc.text(value, margin+38, y): A FIXED 38mm COLUMN. 'EU Responsible Person' fits it; 'EU-established economic operator' does not, so the label OVERPRINTS the value and the buyer-facing pack reads 'EU-established economic opNot set'. I made the label 60% longer and never checked it fits. THIS IS THE SHARPEST METHOD FINDING OF THE WEEK: byte verification checks WHAT A STRING SAYS and cannot see WHERE IT LANDS. Every guard passed, acorn passed, thirteen call sites byte-identical — and the document is visibly broken. Only looking at the PDF catches it. Ships FIRST in batch #9. · #96 MARKDOWN LEAKING INTO THE BRAND PACK PDF — page 3 is a single line '# CLOUD & GLOW — BRAND PACK FOR MAGASIN DU NORD' with the literal hash, rendered as a page title and OVERLAPPING the running header, rest of page empty; page 4 contains only '---'; every content page ends with a stray '---'. The LLM returns markdown and the PDF builder places it as text without stripping. Two wasted pages and a collision in a nine-page buyer document. · #97 'TARGET MARKET: MARKET' — the field renders the literal string 'Market' on page 2 and in every page subheader ('Magasin du Nord · Market'). Same family: 'Annual revenue 2500000 SEK' unformatted.

#98 — ESPR_TIMELINE ASSERTS A PROPOSED OBLIGATION AS ACTIVE (found 2 Sep evening, by the verify.sh article-number diagnostic on its FIRST run). ESPR_TIMELINE (L1467–1473) tells a Skincare & Beauty brand 'Article 24 disclosure already active' and a Wellness & Supplements brand 'ESPR Article 24 disclosure required'. REG_DEADLINES row espr_disclosure (L1221) marks the SAME instrument confidence:"proposed", deadline 2027-03-01. Two constants in one file, 250 lines apart, OPPOSITE TRUTH VALUES on the same obligation. ESPR_TIMELINE carries NO source and NO confidence field — the only registry in that region that does not, so it also fails provenance-or-nothing. RENDERS AT TWO LIVE SITES: L26812 (Circular GPS category tooltip) and L34712, which does Object.entries(ESPR_TIMELINE) and PRINTS EVERY ROW — so a cosmetics brand is shown the Wellness assertion too. THAT SECOND RENDER IS A DISTINCT DEFECT, not a symptom of the rows being wrong. GROUNDED ON ITS OWN THREE LEGS, NOT ON §6: Strategy's citation ruling covered the OPERATOR OBLIGATION; ESPR Article 24 is a different instrument and stretching that ruling would be the lane inventing scope. NOTE THE NARROW-ANCHOR SHAPE, EIGHTH OCCURRENCE: batch #2 already touched this constant and fixed its GATE (the L1453 comment: 'Callers render NOTHING when null: no dot, no blank label, never a green tick') — and never read the STRINGS INSIDE. ALSO SCOUT urgencyColor at L34713, declared immediately after that Object.entries, against batch #2's 'carries no urgency levels and none may be invented' — possible re-invention of what batch #2 removed. APPROVED AS REPORT-FIRST: CC classifies every string against REG_DEADLINES.confidence and returns WITHOUT editing. **SURFACE CORRECTED 3 SEP — "TWO LIVE SITES" ABOVE IS WRONG; IT IS FIVE PATHS ACROSS THREE CONSTANTS.** Full scout: resolveEsprTimeline (L1458, reads ESPR_TIMELINE at 1463) has THREE callers at 34753, 34812, 35043 — the lane had traced none of them; the direct read at 26812 becomes `esprtip` and renders at 26835 and 26840; Object.entries at 34712 is the fifth. PLUS TWO CONSTANTS THE FINDING DID NOT ORIGINALLY NAME: ESPR_BAN_CATS (L1441) and ESPR_DISC_CATS (L1442), read at 26809/26810 and again at 26934/26935 (second site unexamined), which drive the banner at 26836–26838 — and L26838 PRINTS THE WORDS 'ESPR disclosure required' for any category in ESPR_DISC_CATS. That is a THIRD constant asserting the proposed obligation as required, from a different code path, and it was found only because the spec-writing forced a scout of every caller. ALSO: urgencyColor has THREE DECLARATIONS (31120, 34713, 35044), two of them immediately after an ESPR_TIMELINE read. SPEC WRITTEN: veyaflow-specs/VeyaFlow_Batch9_Finding98_ReportFirst_Spec.md, six numbered questions, baseline a7f44f07 named, verification INVERTED (a digest that has MOVED is the failure).

#98 — CC'S REPORT RETURNED 3 SEP AND IT REFUTED ONE OF MY OWN CLAIMS. **RETRACTION: "the 34712 render prints every row so a cosmetics brand is shown the Wellness assertion" IS FALSE.** Verified at the bytes: L34732 is `timeline.filter(([k])=>['garment','device','cosmetic'].includes(k))` and ESPR_TIMELINE's keys are "Fashion & Apparel" / "Skincare & Beauty" / "Electronics & Tech" / "Wellness & Supplements" / "Food & Beverage". ZERO OVERLAP — the filter yields nothing and the block renders nothing. L34734/34735 read `v.urgency` and `v.label` on a value that is a STRING, residue of the pre-batch-#2 shape when the constant was keyed by productType and held {urgency,label} objects. Batch #2 changed the keys AND the value type and left this site reading the old shape. THIS WRONG CLAIM WAS PUBLISHED to the pilot gate artifact and recorded above before it was checked — correct both. CC'S WARNING WORTH KEEPING: the obvious repair, aligning the filter strings to the real keys, CREATES the cross-sector defect I imagined. · WHAT CC CAUGHT THAT THE LANE MISSED: (1) espr_disclosure at L1221 carries cat:["Fashion & Apparel"], so the registry scopes Article 24 to APPAREL while ESPR_TIMELINE asserts it for Skincare and Wellness — a SCOPE contradiction on top of the timing one, and the harder of the two, since a date can be corrected and a category claim is a different assertion. The lane read that line on 2 Sep and did not register it. (2) Three of the five strings (Electronics, Food) have NO REG_DEADLINES row at all, so they contradict nothing and are unfalsifiable from inside the app rather than reconcilable — reported by CC, NOT independently verified by the lane. (3) A SIXTH READER the lane's "complete" scout missed entirely — see #101.

REACHABILITY RULING, 3 SEP — THE CIRCULAR GPS PAGE IS UNREACHABLE IN THE SHIPPED APP, so everything inside renderCircular is LATENT, not live. Three independent gates: 'circular' is in PARKED_IDS (L4154); it is EXCLUDED from validPages in handleDeepLink (L4076–4085, with L4086 stating parked deep-links fall through to home); and the only `showPage('circular')` call site (L34019) sits inside `smartEmpty`, a function DEFINED AT 33951 AND NEVER CALLED — one occurrence in the whole file, its own definition. THIS FOLLOWS THE #89 PRECEDENT: record it, do not ship it, and gate unparking on fixing it first. Unparking is a one-array config change, which is exactly why the debt must be written down rather than assumed safe. SEPARATE FINDING IN ITS OWN RIGHT: `smartEmpty` is a complete personalised empty-state system, ~90 lines, entirely dead.

WHAT IS ACTUALLY LIVE IN #98, AFTER THE ABOVE — TWO RENDER SITES, NOT FIVE. resolveEsprTimeline's callers at 34753 and 35043 (rendering at 35064) print `Brand category (<cat>): <text>` on the DPP Generator and DPP view, both of which ARE in nav and ARE in validPages. So a Skincare & Beauty brand IS told "ESPR cosmetics packaging requirements: 2027. Article 24 disclosure already active." The third caller, 34812, assigns `espr` and never renders it — the dead assignment reported in batch #6, still dead. The 26812 read is inside renderCircular and therefore latent. BATCH #9 SCOPE FOR #98 IS THEREFORE THE FIVE STRINGS AND THEIR PROVENANCE, not the render sites.

#99 — THE REPO ROOT IS THE PUBLIC WEB ROOT (found and mitigated 2 Sep evening; NOT fully closed). netlify.toml line 3 is `publish = "."`, so every TRACKED file is fetchable by name at veyaflow.netlify.app. The `/*` catch-all is status 200 WITHOUT force, so it does not shadow files that exist. CONFIRMED IN A BROWSER, not reasoned: veyaflow.netlify.app/CODING_STATUS.md rendered this file in full, and /netlify/functions/share-dpp.js rendered the function's SOURCE — DPP_PUBLIC_FIELDS, DPP_CONDITIONAL_GATING, DPP_ID_REGEX, MAX_PAYLOAD_BYTES, and confirmation it runs on SUPABASE_SERVICE_KEY. NO KEY VALUES were exposed — the no-secrets rule held — but the worst of it is the COMBINATION: this file's multi-user block states plainly that ~13 RLS-enabled tables carry ZERO policies, that the service role bypasses RLS, and that session_id is a bearer value with no expiry and no revocation. That paragraph plus the server-side validation source, both under the vendor's own domain, is an attack roadmap rather than an embarrassment. TIMING: the exposure existed for every doc already tracked, but the SPEC ARCHIVE, THIS FILE AND THE HARNESS went live within minutes of the 2 Sep push — work this lane recommended without first checking what `publish` pointed at. The scout rule applies to infrastructure and was not applied. MITIGATION SHIPPED: 24 forced-404 redirect rules in netlify.toml, scoped from `git ls-files`, covering every tracked file that is not one of the four app surfaces (index.html, portal.html, dpp/index.html, brand/index.html). `force = true` is what makes them beat the static file — WITHOUT IT NETLIFY SERVES THE FILE AND THE RULE DOES NOTHING. They must stay above the `/*` catch-all; rules are evaluated in order. VERIFIED IN A BROWSER: CODING_STATUS.md blocked, veyaflow-specs/ splat blocked, netlify.toml blocked, function source blocked. `/netlify/*` alone did NOT take on the first deploy; the specific `/netlify/functions/*` did. Function INVOCATION is unaffected — /.netlify/functions/* has a leading dot and is a different path — proven by publishing a DPP to v3 with the rules live. STILL OWED: re-publish to v4 to confirm publishing survives the second deploy. WHY THIS IS NOT CLOSED: blocking by ENUMERATION means every future tracked file at the repo root is public until someone remembers to add a rule — a control that depends on remembering, which is the class this project has a name for. THE REAL FIX is to move the four surfaces plus dpp/, brand/ and assets into their own directory and point `publish` at it, making public the EXCEPTION rather than the default. That also fixes the root cause of the function-source leak: the functions directory is published as static files only because it sits inside the publish directory. Real batch, real breakage risk, needs a spec.

#94 RULED BY STRATEGY, 3 SEP — PICKER, and the truth half goes now. The word "detected" asserts a determination never made and leaves in the copy sweep regardless of the feature. THE TWO REJECTED OPTIONS, with reasons worth keeping: NO HERO AT ALL removes a decision the brand may not want, but an evenly weighted product list is a CATALOGUE, not a pitch document, and a Brand Pack's whole job is selling one thing to one buyer. DERIVED HERO (e.g. highest readiness for the target market) is the one Strategy disliked most and the reason generalises: it would make "detected" TRUE by inventing a ranking the brand never asked for, then needing to explain it on the page — a system-generated judgement presented as fact, the exact class removed all week. FIXING A FALSE WORD BY BUILDING A CALCULATION THAT JUSTIFIES IT IS THE WRONG DIRECTION. Picker makes the existing word honest instead of replacing the concept, puts the choice with the brand, and is small: default to the first SKU, an obvious way to change it, and copy that says what is actually true. #94 blocks nothing; copy fix now, picker when it fits the queue. · CORRECTION TO THE #94 RECORD: it says heroSku appears "identically in FIVE places". It is ELEVEN — 15748, 15816, 16675, 16914, 17104, 23536, 23726, 25093, 25918, 30305, 30499 — and NOT identically: 16914 and 25093 wrap it as sanitiseSkuForRetailer(skus[0]). · CONSEQUENCE FOUND 3 SEP: THE MISSING PICKER BLOCKS A SMOKE STEP FOR A DIFFERENT FINDING. #95's step 1 needs the serum as hero to prove the cosmetic column did not move; the hero is skus[0], new SKUs are prepended (L19919), editing replaces in place (L19921-22), and NO reorder mechanism exists anywhere in the file. The only way to change it is delete-and-re-add, which per #86 orphans a published passport. Step 1 is therefore UNRUN and carried as debt, not quietly dropped.

#95 SHIPPED AND SMOKED 3 SEP — eada442, batch #9 item 1, the first app-code change in two days. FIX: the Brand Pack compliance rows now compute the value column as `margin + Math.max(38, doc.getTextWidth(l) + 2)`, measured while the LABEL font is still active. VERIFIED IN A GENERATED PDF, not inferred: the Matas pack prints 'EU-established economic operator' in full, a gap, then 'Not set' — while 'Technical file' and 'Certifications' sit at EXACTLY the x-position they held in the pre-fix Normal and Lyko packs. The floor does both jobs in one document. STEP 1 (cosmetic path unchanged) COULD NOT BE RUN — it needs the serum as hero and there is no picker (see #94); the residual gap is that no COSMETIC label has been observed post-fix, and the case rests on CPNP notification (23.83mm) and EU Responsible Person (31.84mm) both sitting under the 36mm threshold, now demonstrated at 17.88mm and 18.03mm. Carried as debt, not closed silently. TWO ROUNDS WERE LOST TO A CAUSE WORTH RECORDING: the first two smoke PDFs were generated against code that was COMMITTED BUT NOT PUSHED, so Netlify was serving the old renderer and the overlap was still there. verify.sh reported GREEN on a tree whose verified commit existed only on the laptop. NEW RULE: run smoke against code that has actually shipped — the sibling of 'run smoke where the data is'. HARNESS FIX QUEUED AT THE FRONT: compare local HEAD against origin/ and print 'the deployed site does NOT contain these commits'. ROOT CAUSE HELD OUT OF SCOPE ON PURPOSE: pdfKeyValue (L30146) is the shared two-column primitive and has the same flaw; the broken site was a hand-inlined copy of it; the spec sheet row() (29691, 52mm) and DPP section() (35478, 65mm) are two more. FOUR IMPLEMENTATIONS, NONE MEASURING — the other three are safe only because their labels are short today, the spec sheet by about 3mm. Fixing the primitive moves output on documents that currently render correctly, which the cosmetic-path guarantee forbids doing casually. Needs its own spec, baseline and before/after PDFs.

NEW 3 SEP, NUMBER PENDING — AI-GENERATED COMPLIANCE PROSE GAVE OPPOSITE REGULATORY CONCLUSIONS FOR THE SAME PRODUCT ON THE SAME DAY. The Lyko pack (p9) states "GPSR & Material Safety: NOT APPLICABLE to this product type (beauty device, not accessory)" — a regulatory conclusion the app has no basis to draw, pointing the wrong way (telling a retailer to relax rather than verify), on the page whose reader is the compliance team. The Matas pack (p9), same SKU, same day, states the opposite: "Buyer must confirm whether LED mask falls under MDR, GPSR, or cosmetic classification." NOT A CONSISTENT BUG — MODEL VARIANCE ON A REGULATORY QUESTION. Every neighbouring line on both pages correctly says outstanding / to be confirmed / brand to provide; only this one closes a question. ALSO IN BOTH PACKS: the deterministic half of page 2 prints 'EU-established economic operator' and 'Technical file' — labels that ONLY appear when resolveProductFramework returns 'device' — while the AI prose on p6/p9 says "Product type not set; applicable requirement cannot be determined." THE TWO HALVES OF ONE BUYER DOCUMENT DISAGREE ABOUT WHETHER THE PRODUCT TYPE IS KNOWN. Scout what the Brand Pack prompt is actually handed for product type; the renderer and the prompt are evidently reading different fields.

NEW 3 SEP, NUMBER PENDING — THE BRAND PACK PRINTS THE PARKED DOMAIN AS A LIVE PASSPORT REFERENCE. Page 8 of the buyer-facing pack, in the Compliance & Sustainability table: `Digital Product Passport | Published | veyaflow.com/dpp/vf-1786819586659-mtbhin0o`. veyaflow.com is a Loopia parking page (Gate 1). Batch #7 made the print gate a CODE rule and gated the QR DOWNLOAD on DPP_ORIGIN_LIVE = false — it did not gate this PROSE reference, which reaches a retailer's compliance team in a document telling them the passport is Published. A buyer following it lands on a domain-sale advert. GATE 1 HAS REACHED A BUYER DOCUMENT; it is no longer only a blocker on printing. Same class as the narrow-anchor pattern: batch #7 gated the path it was looking at and not the sibling.

NUMBERS ASSIGNED BY STRATEGY, 3 SEP — #100 dead render block L34732 (filter tests 'garment'/'device'/'cosmetic' against keys that do not exist; renders nothing; the obvious repair CREATES the cross-sector defect) · #101 the ESPR Article 24 declaration `.txt` (downloadESPRDeclaration L26800, a signed document asserting compliance with an instrument the app's own registry marks proposed) · #102 `smartEmpty` (~90 lines, defined L33951, NEVER CALLED — one occurrence in the file, its own definition) · #103 the parked domain printed as a live passport reference in the Brand Pack · #104 the target market field (retroactive; FIXED 3 Sep as 3676623) · #105 truncated generations · #106 verifyBrandPackProof's blocklist misses plain declaratives · #107 verifyBrandPackProof's non-global `.match()` at L16869.

STRATEGY RULINGS, 3 SEP (evening) — (1) **#56 IS A GATE, NOT A WARNING.** Blocking since 2 Sep; the evidence arrived while it was blocked. `[FLAG_COPY]` printed on buyer-facing covers for days and packs kept generating. That is not an argument for better warning copy, it is proof that warning is not a mechanism: a flag the BUYER sees but which does not stop the document is worse than no flag — it shows the customer our internal uncertainty and ships the document anyway. (2) **#105 IS THE SAME GATE, and it is the day's most serious finding.** A cut passport URL inside a compliance section is the #68 class — an assertion leading nowhere, automatically, with nobody having clicked. RULING: a completeness check on the generation output BEFORE rendering; if the final section ends mid-token the pack does not render at all. Better no document than a severed one. (3) **#99 GOES INTO THE PILOT GATE as a sixth point.** The mitigation is 24 forced-404 rules and depends on someone remembering; the structural fix — the app in its own directory — happens BEFORE pilot, not after. The reason is not hygiene: the backlog that sat publicly names ~13 RLS tables without policies and session_id as a non-expiring bearer. We published our own vulnerability list. (4) **LANGUAGE — English body, Swedish headings** in docs/daily-log/*.md. Coding's sources of truth are English (CODING_STATUS.md, the spec archive, the code); translating technical detail inserts a step where meaning can slip, which is the exact error class this lane exists to catch.

#99 RECURRED WITHIN FOUR HOURS, 3 SEP — and the recurrence is the strongest evidence for ruling (3). The 24 forced-404 rules were scoped from `git ls-files` in the afternoon. `docs/` was created that evening for the new log protocol; `docs/daily-log/2026-09-03.md` was committed and pushed and was therefore PUBLICLY SERVED, including the paragraph naming the RLS gap and the session_id bearer value. No rule covered it, because no rule could — the control is enumeration and the file did not exist when the enumeration was written. THE FAILURE HAPPENED IN THE MOST FAVOURABLE POSSIBLE CONDITIONS: same day, same repo, the failure mode written in a comment DIRECTLY ABOVE the rules, by the lane that wrote the comment. Nothing about the next occurrence will be that favourable. Blocked with a 25th rule (`/docs/*`, commit b20c24c, verified 404 in a browser). THE CANONICAL LOG IS SAFE BY A STRONGER MECHANISM: docs/VeyaFlow_Daily_Log.docx and its .txt export are GITIGNORED, so they are never committed and therefore never deployed — a guarantee that cannot be defeated by forgetting, unlike a rule.

TWO NOTES ON THE RULINGS, FOR WHOEVER SPECS THEM — (a) #105's completeness check must NOT live in one generator. Brand Pack, generateBizCasePDF and the magic-link path all generate, and only Brand Pack was touched on 3 Sep. A check in one of them repeats the copied-pattern problem that produced #96, #97 and #104. Shared path, or all three at once. (b) #106 AND #107 ARE ONE REPAIR AND ONE RULING, NOT TWO REPAIRS. #107 (the broken counter) is mechanical and certain. #106 CANNOT be fixed by extending the blocklist — that is the fail-open trap Strategy names in §1 of the AI direction: a blocklist is a prompt instruction wearing different clothes and fails open on whatever nobody enumerated. #106 closes only via the structural gate. Shipping #107 alone would make the next pack LOOK better inspected without being safer, which is the worse of the two states.

BATCH #9 SHIPMENT 4 — THE TRUTH GATES. SHIPPED 4 SEP as 62aaa80 (code) + 145d386 (baseline), branch f2b-async, index.html be86c431 → ef786f2a, 192 insertions / 38 deletions = +154 lines matching the delta exactly. Five items: #106 the regulatory-prose gate · #107 the counter · #105 the shared completeness check · #93 the _ssCanon field list · #56 as a GATE.

#93 CLOSED 4 SEP — PROVED, NOT ASSERTED. Three separate generations of the serum spec sheet on the live app: CPNP-2025-0231233 → `e266ee1dae17`; changed to CPNP-2020-0231233 → `13cde2340f38` (MOVED); set back to CPNP-2025-0231233 → `e266ee1dae17` (RETURNED EXACTLY). Every other field on the sheet identical across all three. That rules out all three failure modes at once — the fingerprint now covers the regulatory section, it is content-derived rather than time-contaminated, and it is reproducible across generations. On 3 Sep the same edit left the ID at 56220dbef259 both times. THE ONCE-AND-ONLY-ONCE MOVE IS SPENT: every Document ID the app produces changed, deliberately, while nothing had been issued to a retailer. It cannot be spent again. NOTE ON THE FOUR IDs IN THE RECORD: CC reported 5836261e14ab → 3af3865024a8 on a constructed FIXTURE; the live serum went 56220dbef259 → e266ee1dae17. Both moved in the same direction; only the live pair is evidence about real data.

#56 + #106 GATE — LIVE AND WORKING ON FIRST CONTACT, 4 Sep. The first Brand Pack generated after the shipment was REFUSED. The withhold panel named nine regulatory terms written as prose — Compliance, notified body, substantiation, GPSR, MDR, LVD, EMC, RoHS, WEEE — plus the unsourced-claims half (500 DKK, Verified). Both gates fired on one panel. THE FIRST ACT OF THE CONTROL WAS TO REFUSE A DOCUMENT, which is the correct first act for a control whose predecessor warned and shipped anyway.

CONSEQUENCE, AND IT REORDERS THE QUEUE: nine regulatory terms in a single draft is not marginal drift caught at the edges — it is the model writing the Compliance & Sustainability section as prose BECAUSE THE PROMPT ASKS IT TO. CC predicted this before smoke and it confirmed on the first attempt. **THE PROMPT SWEEP (#88/#76) IS NOW A DEPENDENCY OF THE BRAND PACK WORKING AT ALL**, not an improvement queued behind it. Until it lands, most or all packs will be withheld.

ITEM 6 — SECTION 5 REMOVED FROM THE BRAND PACK PROMPT, 4 Sep. `## SECTION 5 — Compliance & Sustainability` deleted from the prompt (its two instruction lines, the #68-guarded Digital Product Passport line, and the `Language register: <retailer> compliance team` line); sections 6 and 7 renumbered to 5 and 6 so the model is not presented with a gap. The section number is cosmetic in rendered output — `title:lines[0].replace(/SECTION \d+ — /,'')` strips it — but the prompt now reads 1..6 continuously.

**THE REMOVAL IS NOT LOSSLESS AND MUST NOT BE RECORDED AS IF IT WERE. AN OWED CAPABILITY, NOT A DELETED SECTION.** Section 5 carried two things. (1) A prose duplicate of page two's verified rows — this goes without cost, page two already carries it, built from saved data. (2) THE TRANSLATION: what a status means operationally, what the retailer should confirm, what remains outstanding. That does not exist anywhere in the verified rows, and per §8 of the AI direction it is not decoration — it is the product. The brand thinks in products, the retailer thinks in articles, and the translation between them is what VeyaFlow is for. Calling the whole section a duplicate would silently close a capability by renaming it. IT IS NOT BEING DELETED, IT IS BEING MOVED OFF A SURFACE THAT CANNOT CARRY IT. OWED: one STRUCTURED open item per compliance row, carrying `not_recorded` via `checkState` — which is where a question about regulatory status belonged from the start. NEVER as generated text again. Until that ships, the translation is absent from the product and no document says so.

ITEM 6 DID NOT UNBLOCK THE PACK BY ITSELF — SCOUTED AT THE BYTES, 4 Sep, AND THIS IS THE PART TO ACT ON. Removing section 5 removes the section that ASKED for regulatory prose; it does NOT remove the regulatory data from the prompt, nor the instruction to render it. Three sites survive, all outside section 5 and all outside item 6's named scope: **L17102 `COMPLIANCE DATA (render EXACTLY — do not upgrade a missing item to present):`** — an imperative to render, now with no section to render into, so the model will place it wherever it likes; **L17103 `${complianceData}`** — the block itself, still built at L17067 and still injected, and after batch #8 it carries `EU Responsible Person` / `EU-established economic operator`, `CPNP`, `Technical file` / `Safety Assessment` verbatim, every one of them blocked vocabulary; **L17124 `- COMPLIANCE HONESTY (absolute): state each compliance item's EXACT status only…`** — a second imperative to state compliance items. So the gate can still refuse a pack, and if it does, the vocabulary is entering from the DATA BLOCK rather than from a section nobody scouted. Deciding whether `complianceData` should stop being injected at all is a prompt ruling, not a coding one, and it was NOT in item 6's scope — left untouched and reported. THE PROMPT-SWEEP DEPENDENCY RECORDED TWO LINES ABOVE IS THEREFORE NOT DISCHARGED BY ITEM 6.

ITEM 6 FOLLOW-THROUGH, 4 Sep — THREE MORE PROMPT SITES, THEN A FULL SCAN RATHER THAN A LIST OF THE ONES SOMEBODY NOTICED. Removing section 5 was not sufficient and the lane said so on sight: the prompt still SUPPLIED the regulatory data and still INSTRUCTED its rendering, from three sites outside the deleted section. Now removed: (a) the `COMPLIANCE DATA (render EXACTLY — do not upgrade a missing item to present):` label and the `${complianceData}` injection — an imperative to render, left with no section to render into, so the model would have placed it wherever it chose; (b) `- Key claims with compliance status (Green/Amber/Red) for ${market}` and `- Certifications`, TWO INSTRUCTION LINES IN **SECTION 2 — PRODUCT HIGHLIGHTS**, a section nobody had scouted because the finding was framed as a section-5 problem; (c) the ` | Certs: ` fragment in the hero-SKU data line, which supplied the token without instructing its use. Also corrected: `Generate the Brand Pack in 7 sections` → 6, missed by the renumbering — a model told to write seven from six headers invents one. THE VARIABLE `complianceData` IS KEPT: it is the allowlist argument to verifyBrandPackProof, and deleting it would blind the gate rather than feed it. The prohibition at L17122 (COMPLIANCE HONESTY) is kept byte-identical — the gate never reads the prompt, and with the data gone that line is what stops invention from nothing.

METHOD NOTE, AND IT IS THE POINT OF THIS ENTRY: the change was declared complete by SCANNING THE WHOLE PROMPT against `_REGULATORY_STATUS_VOCAB` READ OUT OF THE SOURCE — 19 patterns, parsed from the constant itself rather than retyped — not by fixing the last site anyone happened to notice. RESULT: instruction lines asking for regulatory content = **ZERO**; one hit remains, L17122, and it is the prohibition. Three successive passes each found "the last one" and each was wrong; the scan is what ended it. Any future prompt edit on a gated surface should be closed the same way. HONEST CAVEAT ON L17122: it opens `state each compliance item's EXACT status only`, which reads as an instruction to state compliance items even though its whole body is a prohibition — with the data block gone there are no items to state, so it is inert, but it is the one line that would need rewording if a pack is ever refused with no other candidate.

OWED — CLAIM VERDICTS AS A VERIFIED ROW, NOT GENERATED TEXT. `- Key claims with compliance status (Green/Amber/Red) for <market>` asked the MODEL to write something THE APP ALREADY HOLDS DETERMINISTICALLY: per-claim Green/Amber/Red verdicts, computed from saved data. That is the same error as section 5's prose duplicate, one section earlier and unnoticed for longer — the app asking a language model to restate its own facts, on a surface where the model may not judge. Page two of the Brand Pack does NOT currently carry claim verdicts, so removing the instruction removes the content from the document entirely. OWED: claim verdicts as VERIFIED ROWS on page two, built from the same source the Claim Localizer reads. NEVER to return as generated text. Recorded alongside section 5's owed translation — TWO capabilities now owed off the same surface, and neither is a deletion.

#103 IS NOT CLOSED BY ITEM 6. Deleting the Digital Product Passport line removes ONE occurrence, not the class: `DPP_CANONICAL_ORIGIN` is read at nine remaining sites, including the DPP PDF's own `section('Digital Product Passport')`. The parked domain is still printed as a live passport reference elsewhere. #103 stays open until the surface is swept, and the line gets NO NEW HOME while the domain is parked — it was not relocated into another section, into the verified rows, or anywhere else.

SMOKE DEBT FROM THIS SHIPMENT, carried explicitly: steps 3, 4 and 6 are UNRUN because there is no clean pack to inspect. We have shown that an unclean pack is refused; we have NOT shown that a clean pack still renders correctly after five items landed in those functions. That gap stays open until the prompt sweep lets a pack through. Also untested: the second refusal CC put at the top of generateBrandPackPDF as belt-and-braces against a caller bypassing the render gate.

#105 — MY CEILING HYPOTHESIS WAS WRONG, AND THE MEASUREMENT SAYS SO. I scouted `callAPI(prompt, 1400)` and called the ceiling the probable cause. CC measured: the prompt instructs "under 600 words total" ≈ 798 output tokens against a ceiling of ~1,052 words — 75% HEADROOM. Truncation therefore means the model overran its OWN instruction by ~75%, and raising the ceiling would conceal that rather than fix it. Strategy's conditional ruling (raise vs section-wise, decided on the measurement) resolves to NEITHER: it is a prompt problem, and prompts are out of scope. The completeness gate ships regardless and catches the symptom; 8/8 on three observed truncations and five complete-document shapes.

HARNESS DEBT NAMED 4 SEP — verify.baseline.json was recaptured from the edited tree (authorised, after the single 19→20 drift was verified at the bytes). CONSEQUENCE: the BASELINE DRIFT section of that GREEN compared the tree against a snapshot of ITSELF and could not fail. Digest, block-count, parse and cross-file checks are unaffected and carry the weight. QUEUED AT THE FRONT: verify.js should record which index.html digest a baseline was captured from and mark that section VACUOUS when it matches — same class as the missing origin/ comparison, a section that reads as verification while testing nothing.

§3 CONTRACT CHANGED 4 SEP — `_dppIsPublished` IS NOW 1 DEFINITION / **3** CALL SITES, NOT 4. The handoff §3 table records 4 ("the publication gate") and is now stale. The fourth call gated the passport line inside the Brand Pack prompt's SECTION 5; that section was removed in shipment 4 item 6 because it was regulatory prose by construction, and the spec ordered the line deleted with it. The three survivors are batch #7's export gates — QR, JSON, PDF — which is the gate's actual job. Definition intact. `verify.js` FIXED_CALLSITES updated from 4 to 3 with the reasoning inline; this was a HARD-CODED CONTRACT, not baseline drift, so no recapture would have cleared it. ANYONE READING THE HANDOFF §3 TABLE WILL SEE 4 AND STOP ON A PHANTOM — correct it there too.

ALSO OPEN — #86 FIVE ns_dpp RECORDS AGAINST TWO SKUs. vf-1775931914850-moh0uqut is PUBLISHED AT v2 SINCE 15 MAY 2026 for a SKU no longer in the catalogue; deleting the SKU did not withdraw the passport, because the public page reads the SERVER copy. With #85 (no unpublish path) it cannot be taken down through the app at all — only a manual Supabase row change, so Charlotte in the SQL Editor + db/CHANGELOG.sql. Ships with #85, and #85 must be RESPECCED: not 'no unpublish button' but 'no withdrawal path for any published passport, including ones whose SKU is gone'.

ALSO OPEN — #87 LABEL SCAN HARDCODES THE COSMETIC REGIME. runLabelScan opens 'You are a cosmetics regulatory compliance expert', seeds its output schema with Art. 19(1)(g) EU 1223/2009, and sets brandCat = brand?.category || 'Skincare & Beauty'. Scanning a device carton returns a cosmetics analysis. It also carries const isCosmetic = brandCat.includes('Skincare')||brandCat.includes('Beauty') — ASSIGNED AND NEVER READ, a surviving instance of the brand-category inference batch #2 deleted from the resolver: the same wrong idea, still in the file, unused but readable. Its own batch — the honest fix needs the scanner to know WHICH SKU it is looking at, which is a UX change, not a truth fix.

PROMPT STRINGS ARE AN UNSWEPT SURFACE — named 2 Sep, and it reorganises the queue. The Truth Batch swept RENDERED surfaces; it never swept prompt string literals. #88 and #76 (eleven prompts hardcoding 'April 2026' as now) are the same category, and it is the worst one: a false claim inside a prompt does not merely render, it PROPAGATES into generated prose that reads as the brand's own voice. Batch #9 should be scoped as THE PROMPT SWEEP, not as scattered fixes.

EXPORT CONTENT BATCH (#78–#85) — #78 "manufacturer": sku.euResponsible (the RP is not the manufacturer; the serum's export says Cosmeservice GmbH while countryOfManufacture is Sweden) · #79 the version field · #80–#84 · #85 the missing withdrawal path, now widened by #86. DELIBERATELY NOT TOUCHED during batch #8 even though 1C stood on those exact lines: a fix made outside the spec is unverifiable against it.

THEN GTIN → HARD CHANGE (Strategy ruled 1 Sep, and ruled FIRST among new work). One wire. The registry fact sits verbatim on the retailer record at L8615 — "New GTIN = hard change = new article. Packaging/size changes that alter the GTIN must be notified as a NEW article" — and NOTHING READS IT. MEASURED: sku.ean has ~50 references and every one is presence (if(sku.ean) → +3 points, a green line, a checklist tick), format validation (13 digits, duplicate detection) or field mapping into templates and exports. Fifty touches of PRESENCE AND SHAPE, none of CONSEQUENCE.

THEN INCI → CLAIMS, finish the wire (Strategy ruled 1 Sep). Currently HALF-WIRED: sku.inci reaches the compliance and rejection-risk layer (inciMissing maps to "claims not substantiated in PIF"; "Matas buyers check methodology") but does NOT reach the Claim Localizer's per-market verdicts. So "INCI in → which markets become a problem" is the half the depth test was designed to find.

"VET INTE" — SURFACE IT, DO NOT BUILD IT (Strategy ruled 1 Sep). It already exists in TWO independent layers: checkState returns ok · blocking · attention · not_recorded (batch #5, legend live on the buyer surface), and the asset-check layer has carried {pass:null, label:"N claims — PIF status unclear"} for longer. The first-minute task is to bring it to the BRAND surface. Materially smaller than the brief assumed.

STABLE BRAND ID — on the PILOT GATE (Strategy, 1 Sep). Coding-lane task, small now, physically permanent later: brandId is slugified display name + session_id and goes into DPP URLs that get PRINTED ON PACKAGING under a dialog promising the URL is stable. Standing alongside it: the 13 RLS policies are written WHEN ACCOUNTS EXIST, not before (service role bypasses RLS; session_id lives in the request body, not a verified identity). DEMONSTRATED IN THE FIELD 2 Sep: Charlotte opened the app on her phone and got the NORDLYS seed, not Cloud & Glow — device-local storage, no brand login, ns_skus has no server mirror. Twenty seconds to hit in practice what the multi-user analysis described in three pages.

**CORRECTION ADDED 2 SEP EVENING — THE MECHANISM ABOVE IS WRONG AND THE FIX SPLITS IN TWO.** Checked at the bytes: brandId (L34246) IS slugified name + session_id as described, but it goes into the POST BODY at L34264 — **NOT into the printed URL**. The public URL is L34251 `DPP_CANONICAL_ORIGIN+'/dpp/'+dppId`, and getDPPId (L34359) builds `'vf-'+skuId+'-'+Date.now().toString(36)`. No brand name, no session id — the shape of the existing orphan vf-1775931914850-moh0uqut confirms it. SO: **RENAMING THE BRAND DOES NOT BREAK A PRINTED QR.** What does break it is that dppId is cached ONLY in ns_dpp (localStorage) — clear site data and getDPPId mints a NEW id for the same SKU, so a re-publish creates a SECOND passport at a SECOND URL while the printed one is orphaned and (per #85/#86) unwithdrawable. THE TWO HALVES HAVE DIFFERENT OWNERS: (a) brandId instability is real — server rows keyed by an owner id that changes on rename or storage loss, a DATA-OWNERSHIP defect, and the 'generated id, stored once' batch closes it; (b) the PRINTED-URL risk belongs to the storage-mirror gate, not to this one, and **the brandId batch does not touch it**. Shipping (a) alone and believing the carton is now safe is the trap. The dialog string at L34254 — 'The URL is stable across re-publishes.' — is quoted correctly and IS true for re-publishes; it is silent about storage loss, which is the case that actually bites.

THEN LISTING-CHECKLIST BATCH (Test 3 output) — printRetailChecklist ignores product type (device PDF omits CE MARKING and lists three cosmetics-only rows) · #65 print renders auto-verified and manual ticks identically · #3.6 getIncompleteChecklistSubmissions reads skuIds[0] only AND ignores product type (count moved 6→2 on an array reverse; total 10 vs 15 on screen) · the RP autoCheck never reads renewalDate · #64 the "100%" aggregate erases per-row provenance · #63 only 2 of ~28 registry retailers have a requirements list (2 of 6 in real use; Lyko has none).

THEN BRAND PACK PDF BATCH (#56–#62) — #56 [FLAG_COPY] internal QA annotation prints on the buyer-facing cover (STRATEGY RULES gate-vs-warn FIRST) · #57 raw markdown printed literally · #58 blank page 3 + "APRIL 2026" · #59 ✓ renders as an apostrophe · #60 silent mid-word truncation · #61 "Confidential — not for distribution" · #62 raw unformatted values. Order within the batch: #57/#58 first (the document is visibly broken), then #56 once ruled, then the rest.

BRAND-FACING / TIER BATCH (numbered #6 in earlier entries — the DPP pass took that number; renumber when specced). NOW ALSO CARRIES THE COMPLETENESS RULING, because it is the same code region: #48 renderVerificationCard L7343 ticks "Readiness score >70 for one market" from bestReadiness on skus[0] while calcVerificationTier requires EVERY SKU red-clear (two implementations of one rule; L7369 also emails "Best readiness score") · #49 daysUntil clamps at Math.max(0,…) so every past deadline ties at 0 and urg()'s Overdue branch is UNREACHABLE · #54 the CPNP modal status and the readiness blocker never consult each other. Hero / CRM cards / expansion overview remain gated on Design.

THE DATE SWEEP — renewalDate was ONE field with FOUR readers, none comparing to today. Batch #5 fixed three. Before calling this closed, sweep EVERY field that is stored, displayed, and never compared to today; buildComplianceEvents (silently drops past dates) belongs here alongside daysUntil. #90 NOW BELONGS TO THIS SWEEP: daysUntil is a FOURTH date function and it cannot express the past — Math.max(0,…) clamps an expired deadline to '0d' and the calendar renders it as 'Today'.

#55 MAGIC-LINK BACKFILL — DB change, so Charlotte in the SQL Editor + append db/CHANGELOG.sql. Packs shared before 31 Aug carry expires_at NULL and are read (correctly) as never expiring; the 5 May pack is still live. DECISION NEEDED: 90 days from CREATION kills live pitch links immediately; 90 days from TODAY gives a grace period. Recommend the latter — commercial call about live pitch links, not a code one.

OLDER OPENS STILL LIVE — #21 catMap2 claims routing still infers cosmetic from brand category · #22 INCI credit awarded to devices · the ||'SE' audit (13 sites) · #16/#17/#18/#19 smaller opens · multi-user layer (~13 RLS-enabled tables, ZERO policies) reclassified by Strategy from launch-blocker to SCALING problem · Portal Submission v2 sits behind that.

---

STANDING DISCIPLINE — This chat NEVER writes code; a separate Claude Code instance applies, then STOPS. Verify BYTES, never reports (5 stale/unapplied catches to date). Every spec carries baseline sha256s. NEW 31 AUG: (1) a baseline mismatch is proceedable ONLY on a sha the coding chat named — CC's own reported sha is NOT sufficient; CC flagged-and-proceeded once and the cost was that batch #5 v1.0 never got an independent pass. (2) Prove a confined edit by reconstructing the pre-file and hashing it back to baseline. (3) Spec derivation order matters: an independent "satisfied" test must NEVER outrank an emitted blocker (#52 — my error, caught only by the byte pass).

STANDING DISCIPLINE ADDED 2 SEP — (a) A FALSE CLAIM CAN BE PHRASED AS AN INSTRUCTION. Every truth fix so far hunted assertions in DECLARATIVE form; a fix prompt, empty-state instruction, blocker fix-text or action-button label is an assertion too, and an IMPERATIVE is worse — a filled cell is a claim the brand can inspect, 'add in My Products →' is a claim that recruits them into acting on it. A truth fix must scout REMEDIATION copy as well as display copy. (Found by CC, in the 1F design.) (b) A CHECK THAT CANNOT FAIL IS NOT A CHECK, and reporting its result as a pass is WORSE than reporting nothing — it spends the credibility of every other line in the report. Any guard whose subject is not found must report NOT FOUND. (c) RUN SMOKE WHERE THE DATA IS — file:// and every device have their own localStorage; a smoke run from the wrong origin passes against an empty catalogue, which is the worst kind of pass because it looks like verification and tests nothing. All three bind the SPEC as much as the report.

STANDING DISCIPLINE ADDED 2 SEP (EVENING) — (d) COUNT AST REFERENCES, NOT STRINGS. Demonstrated in live code in both directions at 649f0ef: _operatorStatus reads as 3 to grep and 2 to the AST (L6638 is a removal-comment epitaph), while _rpDateExpired at L16225–16227 LOOKS like a comment cluster and is 7 real rendered calls inside a template literal. Grep cannot tell a call from an epitaph. Every guard must state whether it counts RENDERED occurrences or RAW matches. (e) TWO CHANNELS ONTO ONE FILE NEED A CROSS-CHECK. The lane reads via file tools; Charlotte runs the harness and pastes the output. A digest alone cannot detect a divergence BETWEEN those channels — the lane would reason about bytes nobody hashed while trusting a hash of bytes nobody read. verify.js prints witness lines (true line count, block spans, verbatim content of named lines, literal counts) that the lane confirms against its own Read before trusting any digest. Byte OFFSETS are deliberately NOT used as witnesses: the lane cannot derive them by reading, so confirming one is only agreeing with a number it has no independent access to. (f) DIAGNOSTICS FIND WHAT GATES CANNOT. #98 came out of a section explicitly marked REPORT ONLY, NOT A GATE, on its first execution, after eight batches of increasingly careful gates walked past it. Gates encode failures someone already knew to name; diagnostics surface the ones nobody did. Practical rule: print counts by category, clusters and outliers — cheap, no false-failure risk, and now demonstrated to work. It was not the 63 article references that were interesting, it was the two rows with no `source` field sitting beside sixty that had one. (g) A DOCUMENTED CONTROL THAT IS NOT IMPLEMENTED IS WORSE THAN NO CONTROL, because people rely on it. Same failure as a check that cannot fail, one level up: the credibility leak moves from the report to the process document. The protocol's four hard stops now say which two are mechanical and which two are the lane reading a diff.

---

THE VERIFICATION HARNESS — BUILT AND GREEN 2 SEP (EVENING), commit 7b42528. One command, `./verify.sh`, run by Charlotte; the lane rules on the output. Contents: git state · sha256 of all four tracked HTML surfaces against verify.expected.txt (values NAMED BY THE LANE — a sha CC reported itself must never be pasted into that file) · one-edit-at-a-time · then verify.js, which does a GLOBAL block scan, acorn over EVERY block, 19 single-definition invariants, the two fixed call-site contracts, the @context-stays-a-literal check, normalizeBrandRP-must-not-exist, persistence keys, the article-number diagnostic, and baseline drift from verify.baseline.json. Any guard whose subject is absent reports NOT FOUND and exits non-zero. Report ends with a NOT CHECKED block — the seven semantic contracts, plus LAYOUT and SMOKE — so a green run cannot be read as a shipping verdict. FIRST RUN AT 649f0ef: GREEN, 40 gates, 0 failed, 0 not found; all four digests matched the §3 named baselines, which independently confirms that table was accurate. FULL DIGESTS recorded in verify.expected.txt, captured at a moment when the cross-channel witnesses had been confirmed line-for-line.

ast_verify.js — SUPERSEDED, commit 7b42528. Its L18 extraction was non-global and parsed BLOCK 1 ONLY (63% of the file), never touching block 2 which contains checkState — a clean exit from it was a green over code it never read. It now carries a header, prints a runtime banner, refuses to emit a verdict without --i-know-this-is-partial, and its clean path says PARTIAL rather than CLEAN. STILL TO DO: port its USER_STATE / TDZ lint (the Apr 2026 brandCtx orphan incident) into verify.js WITH ITS OWN TEST CASE, then delete ast_verify.js in that same commit.

HARNESS QUEUE, IN ORDER — (1) TDZ port + delete ast_verify.js. (2) STOP 3, mechanised: `git diff -U0` hunk ranges, so 'anything outside the specced regions changed' stops being a judgement call. (3) STOP 4, mechanised: a fingerprint over the cosmetic output path, so 'a cosmetic-path string moved' is a diff rather than a human reading carefully. Stop 4 is the more valuable of the two — it is the guarantee that turned 'did you gate it correctly' into a diff on batch #8. (4) Upgrade the digest compare from the 8-char prefix (32 bits) to the full 64; the full digests are already recorded in verify.expected.txt so no new capture is needed. RULE OBSERVED THROUGHOUT: no unrun code on an unrun harness — each of these waits for the previous one to run green.

---

INFRASTRUCTURE — GITHUB PAT ROTATED (was expiring 11 Sep; the project's only hard-dated item, now CLEARED). VEYAFLOW.COM STILL PARKED AT LOOPIA and still blocks ALL QR printing: the print gate is a CODE rule (DPP_ORIGIN_LIVE = false, L1488, with a nine-line comment at L1482 recording that the domain was verified parked on 2 Sep), not a human one, so flipping it is a DNS task plus a one-character commit — and someone must load a real passport at the canonical address BEFORE flipping. SPEC FILES NOW COMMITTED — 26 documents as veyaflow-specs/ in commit 3fb36eb (4,343 insertions, matching the lane's independent line count exactly). CAVEAT ON THAT COMMIT: its message claims 'handoff, protocol' but VeyaFlow_Coding_Lane_Protocol.md was never copied into the directory and is NOT in it — the protocol is still the only lane document outside git. Six spec files also still exist as untracked duplicates at repo root (Batch2 Routing, Batch3 SaveIntegrity, Batch4 MagicLink, Batch4 ReadinessBuyerFacing, PortalTruth Fixup, PortalTruth v1.1); line counts match their veyaflow-specs/ twins exactly but a `diff` has not been run, so identity is UNPROVEN — do not delete on the line-count match alone. SIX UNTRACKED SERVERLESS FUNCTIONS need a deploy-truth ruling: anthropic-proxy.js, get-brand-pack.js, get-dpp.js, share-brand-pack.js, share-dpp.js, netlify/functions/f2b-spike-background.js. Netlify deploys from the pushed branch, so untracked means UNDEPLOYED — and share-dpp.js is the endpoint L34261 posts to when publishing a passport. If it is not live, publishing does not work on the deployed site, which would sit underneath Gate 1 rather than beside it.

---

HOW TO MAINTAIN THIS FILE

Update it at the end of a working day, not during one. Edit lines in place rather than rewriting the file — on 2 September a wholesale rewrite of this block nearly sanded the detail off seven items in a single pass, and recovered batch #6's four unrun smoke steps only because the old text was read first.

Condensing is how memory decays. If a line is getting long, split it; do not summarise it.
