# VeyaFlow — named digest baselines
#
# Values NAMED BY THE CODING LANE. A sha CC reported itself is not sufficient
# and must never be pasted into this file. When a batch lands, the lane names
# the new value and it is updated here in the same commit as the change.
#
# Format:  <path>  <first 8 hex chars of sha256>
#
# Source: CODING LANE HANDOFF §3, HEAD 649f0ef8e48a66c6ef16b3babc9ecc1c5dfdb190
# ---------------------------------------------------------------------------

index.html        4b268e17
dpp/index.html    e4809761
portal.html       beaa72b8
brand/index.html  fc6c0586
netlify/functions/supabase-proxy.js  78c4f6a2
netlify/functions/share-dpp.js       500806f0

# ── Added #174, 11 Sep 2026. Five live endpoints that were never watched. ──
netlify/functions/anthropic-proxy.js             7cf20b3f
netlify/functions/anthropic-proxy-background.js  1f6f47ab
netlify/functions/get-brand-pack.js              fe285fe9
netlify/functions/get-dpp.js                     3c050904
netlify/functions/share-brand-pack.js            3791bbec

# NO TRACKED DIGEST MOVED — #174, 11 Sep 2026, HARNESS ONLY. All six existing surfaces
# identical before the edit, after it, and after the scratch tests. `verify.sh` the only file
# changed. The battery grew 6 → 11. Recorded here because the reasoning belongs with the
# shipments even when no baseline moves (#131's precedent).
#
# THE GAP: netlify/functions/ held SEVEN files and the battery named TWO. Five live public HTTP
# endpoints had no digest, no baseline and no place in the one-edit count — a change to any
# passed GREEN at "0 of 6 modified". anthropic-proxy-background.js is the sharpest: a BACKGROUND
# function (202 immediately, up to 15 minutes) holding ANTHROPIC_API_KEY and
# SUPABASE_SERVICE_KEY, which bypasses RLS. NO HARDCODED SECRETS ANYWHERE IN THE DIRECTORY —
# every reference is process.env. A PROVENANCE GAP, NOT A DISCLOSURE GAP.
#
# WHY NOW: a throwaway background function was found untracked in this directory the same
# morning — uploaded via github.com's WEB INTERFACE on 5 Jun, live on two branches for 31
# minutes, deleted, and still sitting in the working tree three months later, one `git add .`
# from being an unauthenticated write endpoint into production. Zero rows written; never on the
# deploy branch; closed as a near-miss. AND THE CONTROL THAT EXISTS WOULD NOT HAVE STOPPED IT —
# netlify.toml blocks /netlify/functions/* from being SERVED, and its own comment says why that
# is irrelevant: functions are INVOKED at /.netlify/functions/* (leading dot). THE EXISTING
# CONTROL COVERS DISCLOSURE; THE HAZARD IS DEPLOYMENT.
#
# STRATEGY'S ORDERING RULE, ADOPTED: HARNESS WORK IS ORDERED BEFORE WHAT IT CHECKS, NOT AFTER.
# Its value is the NUMBER OF SHIPMENTS THAT FOLLOW IT; deferring it loses coverage on everything
# shipped in the gap, and that gap is exactly where a newly-discovered defect class recurs. The
# lane had argued "it is cheap" — the real argument is that it is MULTIPLICATIVE. Same form as
# capture-before-render and make-it-possible-before-legible.
#
# STRATEGY'S CONDITION: EVERY NEW CHECK IS DEMONSTRATED AGAINST A KNOWN FAILING CASE. Three
# times this week an artefact vouched for a check that never happened — #160's legend, #118's
# comment, #134's note about the renderer that #168 then walked through. THE FOURTH INSTANCE
# WOULD HAVE BEEN THE BATTERY ITSELF, and it is the instrument every other check is judged by.
# A STRICT BATTERY WITH A BUG FAILS CLOSED AND IS NOTICED IN MINUTES; A BATTERY WITH A FALSE
# CLEAN BILL FAILS OPEN AND IS WORSE THAN NO BATTERY, BECAUSE GREEN CARRIES AUTHORITY.
#
# CC REFUSED THE SPEC'S GLOB AND WAS RIGHT TWICE, ON EVIDENCE RATHER THAN PREFERENCE.
#   (1) A `*.js` GLOB WOULD NOT WATCH THE DIRECTORY. On 10 Jun a web upload put
#       netlify/functions/index.html there — still on origin/legacy-web-uploads. Netlify also
#       deploys .mjs, .ts and SUBDIRECTORY functions. The spec's own gate would have missed the
#       exact historical shape it was written to catch.
#   (2) A RAW DIRECTORY LISTING IS NOT STABLE AT REST. The first .DS_Store Finder writes would
#       fail it, and A GATE THAT FAILS ON ORDINARY USE GETS MUTED.
#   So the gate reads GIT'S VIEW — tracked files plus untracked files that are not ignored —
#   WHICH IS EXACTLY WHAT `git add .` COULD STAGE, which is exactly what the spike was.
#   FUNCTIONS_CONTRACT is the single source and FILES is built from it, so the digest list and
#   the directory contract CANNOT DRIFT APART. An eighth name enters both at once, and the
#   battery then refuses until the lane names its baseline.
#
# ═══ THE CONDITION CAUGHT THE BATTERY ITSELF, AND THE DEFECT WAS OLDER THAN THIS SHIPMENT ═══
#
# PART A COULD NOT FAIL. CC altered anthropic-proxy-background.js by ONE BYTE (134, 'N'→'n',
# same length) and the battery read:
#   INFO      | …anthropic-proxy-background.js  d9870d74…  DIFFERS from named baseline (1f6f47ab…)
#   PASS      | 1 of the 11 tracked surfaces modified
#   OVERALL: GREEN — structure and provenance clean.        exit=0
# ON THE FILE THAT HOLDS ANTHROPIC_API_KEY AND SUPABASE_SERVICE_KEY.
#
# CAUSE: verify.sh's DIFFERS branch prints INFO and NEVER SETS RC. It was built for mid-batch
# runs, where the file a batch edits legitimately differs until the lane names its new value,
# and it relies on a human reading "stop, report, wait". THIS IS OLDER THAN #174 AND COVERS ALL
# ELEVEN SURFACES, index.html INCLUDED.
#
# SO THE BATTERY'S REAL GUARANTEE HAS ALWAYS BEEN "NO MORE THAN ONE TRACKED FILE CHANGED" —
# never "no unauthorised change". ANY SINGLE UNAUTHORISED EDIT TO ANY TRACKED SURFACE, WITH
# NOTHING ELSE TOUCHED, PASSED GREEN WITH EXIT 0. Every GREEN in this log predating this entry
# was a verdict from a gate that could not refuse.
#
# WHAT ACTUALLY HELD, AND IT IS WORTH STATING PLAINLY: the control was AUTHOR ≠ VERIFIER
# operating through a human — Charlotte pasting digests, the lane comparing them against named
# baselines by eye, before every commit. That is why nothing slipped. THE GATE DID NOT HOLD; THE
# PRACTICE DID. A battery that claims more than it delivers is the fourth artefact, and it was
# already in place before #174 arrived to become it.
#
# RULED (a): DIFFERS BECOMES FAIL FOR EVERY SURFACE, and it is forced rather than preferred.
# This file's own header says the expected value is updated IN THE SAME COMMIT AS THE CHANGE, so
# a named change is PASS by commit time. DIFFERS can therefore only appear BEFORE NAMING —
# exactly when "do not commit" is true. The new mid-batch message, "NOT GREEN: index.html
# DIFFERS (awaiting name)", is MORE ACCURATE THAN THE OLD ONE. IT FAILS CLOSED.
#   (b) FAIL for the functions directory only was declined: a carve-out needing a per-batch
#       allowance is a control that depends on someone remembering — the class retired three
#       times today (the spike, #99, #176).
#   (c) reword OVERALL and keep INFO was declined: anything reading the exit code still gets 0.
#       That is the defect with better wording.
# FOLDED INTO #174 RATHER THAN QUEUED: #174's entire product is the sentence "the functions
# directory is watched", and that sentence is FALSE while a one-byte change reads GREEN.
# CC STOPPED AND REFUSED TO COMMIT. Correct — shipping it as it stood would have made #174
# ITSELF the false artefact the condition exists to prevent.
#
# WORDING SPLIT APPROVED, and the second string is better than the finding that prompted it:
#   "absent from disk"
#   "on disk but untracked and gitignored — will not deploy"
# In #176's shape the file IS sitting in the directory, so "MISSING" alone would read as a
# broken gate. The second string names the CONSEQUENCE, which is what a reader needs at 7am.
#
# #176'S SHAPE, REPRODUCED AND MEASURED (git rm --cached, then restored):
#   on disk: yes | tracked: 0 | ignored: yes
#   PASS      | netlify/functions/get-dpp.js  3c050904…  matches named baseline
#   FAIL      | contracted file MISSING from netlify/functions/: …/get-dpp.js
# THE DIGEST GATE SAYS PASS. The bytes on disk are exactly the named ones and it cannot know git
# will no longer ship them. ONLY THE DIRECTORY GATE CATCHES IT. Without Part B this production
# endpoint would vanish on the next deploy UNDER A GREEN VERDICT.
#
# APPLIED AND RE-DEMONSTRATED IN THIS SAME SHIPMENT. #174 IS THEREFORE THE POINT AT WHICH THE
# DIGEST GATE BEGAN TO REFUSE — before this entry it could not, on any surface. Seen refusing in
# a pristine copy, both directions, each reverted to PASS:
#   A1  anthropic-proxy-background.js, byte 134 'N'→'n' (length unchanged)
#       FAIL … DIFFERS … — AWAITING NAME · exit 1 · OVERALL: NOT GREEN — AWAITING NAME
#   A2  index.html, byte 5910 'S'→'s' — CHOSEN INSIDE A CSS COMMENT, OUTSIDE EVERY <script>, so
#       the AST battery stayed at 57 passed / 0 failed AND THE REFUSAL PROVABLY CAME FROM THE
#       DIGEST GATE ALONE. A demonstration that controls for its own confound.
#   A3  a changed function AND a scratch file together → the GENERIC verdict, with awaiting-name
#       demoted to a parenthetical. THE MILD WORDING CANNOT COVER A REAL FAULT: it is gated on
#       RC -eq 0, so it appears only when an unnamed digest is the SOLE cause.
#   M1  mv a function out      → "contracted file absent from disk"
#   M2  git rm --cached (#176) → "on disk but untracked and gitignored — will not deploy"
#
# THE VERDICT IS NOW THREE-WAY: GREEN · AWAITING NAME ONLY · generic NOT GREEN. Exit is 1 for
# either kind of failure. DIFF_RC is tracked apart from RC, which is what lets the verdict say
# whether an unnamed baseline is the ONLY thing between this tree and GREEN.
#
# CONSEQUENCE FOR EVERY FUTURE SPEC, AND IT MUST NOT BE READ AS A REGRESSION: the VERIFY line
# this lane has written in every batch — "GREEN expected; index.html DIFFERS; 1 of N modified" —
# DESCRIBES A STATE THAT CAN NO LONGER OCCUR. Mid-batch is now:
#   NOT GREEN — AWAITING NAME for: index.html · exit 1 · every other gate passed
# The spec template is corrected as of #167. A future batch reading "GREEN expected" against
# this harness would see a refusal and could try to "fix" the gate — which is the failure this
# shipment exists to prevent, arriving through the documentation instead of the code.
#
# SEEN FAILING, FOUR CASES, each added, run, removed, re-run:
#   A  zz-scratch-174.js                 FAIL, file named    → PASS after removal
#   B  index.html (the 10 Jun shape)     FAIL, file named    → PASS
#   C  scratch174/fn.js (subdirectory)   FAIL, file named    → PASS
#   D  .DS_Store                         PASS — must not fail, and does not
#
# THE ONE-EDIT LABEL IS DERIVED, CONFIRMED AT 11: NFILES=$(echo $FILES | wc -w). No live string
# in verify.sh or verify.js says 6; it survives only in comments.
#
# #176 RAISED BY CC, AND IT IS WORSE THAN THE SHIPMENT THAT FOUND IT.
# .gitignore lines 33–37 list anthropic-proxy.js, get-brand-pack.js, get-dpp.js,
# share-brand-pack.js and share-dpp.js WITH NO LEADING SLASH, so they match AT ANY DEPTH —
# INCLUDING netlify/functions/. FIVE OF SEVEN PRODUCTION ENDPOINTS ARE GITIGNORED, and
# share-dpp.js is one of the six ORIGINALLY TRACKED SURFACES. They survive only because git
# ignores ignore-rules for already-tracked files. DELETE ANY ONE AND `git add .` SILENTLY SKIPS
# IT; THE NEXT DEPLOY HAS NO SUCH ENDPOINT AND NOTHING REPORTS AN ERROR.
# The file states the principle itself at line 20 — "No leading slash on these patterns, so they
# match at any depth" — knowingly, for the daily log. #111 reused the shape for ROOT-LEVEL
# working files and its comment says "this changes nothing about what deploys": TRUE OF THE ROOT
# COPIES, FALSE OF THESE. Fix is a leading slash per pattern, which is what #111 meant.
# EXPOSURE TODAY IS ZERO — nothing has deleted them. THE FAILURE MODE IS SILENT AND TOTAL, and
# the recovery path is the one that fails.
#
# THE GOVERNANCE POINT CC RAISED AGAINST ITS OWN WORK, FOR STRATEGY: verify.sh lines 12–14 say
# "CC does not write, edit, or run the harness that verifies CC." THIS SHIPMENT HAD CC WRITE THE
# GATE THAT NOW GATES CC. The lane's position: the operative control is AUTHOR ≠ VERIFIER, which
# held — the lane specced it, read the diff at the bytes, Charlotte ran it, and the seen-failing
# demonstration is what converts "CC wrote it" into "it provably refuses". BUT THE COMMENT IS NOW
# FALSE, AND A FALSE COMMENT IN THE HARNESS IS PRECISELY THE FOURTH-ARTEFACT RISK ABOVE. The
# practice is defensible; the sentence is not. Strategy's to settle, not the lane's.
#
# OPEN, AND BOTH DECIDE WHAT THIS GATE PROTECTS: `main` has NO netlify/functions/ AT ALL, and
# the repo does not record which branch Netlify deploys. origin/legacy-web-uploads still carries
# netlify/functions/index.html. The gate also protects GIT-DRIVEN DEPLOYS ONLY — a
# `netlify deploy` from the working tree would ship gitignored files; no .netlify/ link exists
# today, so it is not happening, but it is the gate's edge.
#
# THE MISSING BRANCH IS UNTESTED AND IT IS THE ONE #176 WOULD FIRE — a contracted file absent
# from disk. CC declined to delete a tracked function, correctly. DEMONSTRATED BEFORE COMMIT BY
# `mv` OUT AND BACK: reversible, no deletion, and it exercises the exact path a silent
# .gitignore skip would take.
#
# verify.sh EXITS 1 EVEN ON PASSING RUNS while any baseline is unnamed, so the exit code was not
# a usable signal during this shipment; the gate's own line was. Resolves once these five are
# named.

# index.html moved efeab029 → 4b268e17 on 11 Sep 2026 — #175, #160'S OWN REGRESSION ON THE
# SURFACE #160 EXISTED TO REPAIR. TWO LINES. THE LANE'S ERROR, NOT CC'S.
#   after   4b268e1774a4a5f90f218bb2df28b71cf6c96159ff7c487d804849345787d3b4
#           41,780 lines · 57 gates · 1 of 6 modified
# NAMED BY THE LANE after reading 33713 and 33723–33730 at the bytes.
#
# FOUND IN SMOKE, BY CHARLOTTE, MINUTES AFTER #160 WENT LIVE. The calendar's day cell read
# `—` for all 8 overdue rows — and THE EM DASH IS THIS FILE'S MARKER FOR "NO VALUE RECORDED"
# (#118, written into fmtNum at 4872). So an overdue deadline rendered as though NO DATE
# EXISTED, inches from the date it has and the red Overdue pill contradicting it.
#
# 33713 read `Number.isFinite(r.days) && r.days >= 0 ? r.days+'d' : '—'`. That test was written
# when daysUntil COULD NOT RETURN A NEGATIVE. #160 made it able to. Before: days=0 → `0d`.
# After: days=-345 → fails `>=0` → `—`. WE INVERTED OUR OWN RULE: absence must not render as a
# measurement, AND A MEASUREMENT MUST NOT RENDER AS ABSENCE.
#
# THE CAUSE IS THE SPEC, AND IT IS THE LANE'S RECURRING ERROR — SEVENTH INSTANCE, AND THE FIRST
# TO PRODUCE A LIVE REGRESSION RATHER THAN A WRONG CLAIM IN A REPORT. Part B named 4909 as the
# only copy site; for the calendar the spec said "33651 → urg() — no edit; report the label
# flip". CC did exactly that, correctly: it measured daysUntil's OUTPUT and urg()'s LABEL, and
# both were right. THE RENDER SITE SITS BETWEEN THEM AND HAS ITS OWN `>= 0` TEST THAT NEITHER
# CHECK TOUCHED. VERIFY THE PARTS, ASSERT THE JOIN.
# EXTENSION ADOPTED: when a function's RANGE widens, every consumer's GUARD is in scope — not
# only its callers and its labels. A guard written against the old range is a silent consumer.
#
# THE FIX, 33713: `Number.isFinite(r.days) ? Math.abs(r.days)+'d' : '—'`. null still renders the
# em dash; magnitude shows in both directions; DIRECTION IS CARRIED BY THE PILL AND u.k's
# COLOUR. Measured on all 14 rows: 8 overdue moved `—` → their true magnitude, 6 futures
# BYTE-IDENTICAL, and all four constructed undated forms still render `—`.
#
# CC VERIFIED THE FIX'S PREMISE INSTEAD OF ACCEPTING IT, AND THAT IS THE REPORT'S BEST MOMENT.
# The fix rests on "the pill carries the word". CC confirmed 33717 renders u.label AS VISIBLE
# TEXT (not colour alone), and that no CSS hides .cc .pill at any width — the narrow-screen rule
# at 418 only re-grids .cc .right. HAD THE PILL BEEN COLOUR-ONLY OR HIDDEN ON MOBILE, `345d`
# WITH NO WORD WOULD HAVE BEEN #160'S DEFECT A THIRD TIME. The word-not-colour criterion was
# checked at the surface it was being relied on, not assumed from the spec.
#
# 33724 — THE BADGE SAID "8 UPCOMING" OVER 5 OVERDUE ROWS. #160's strip-heading defect, on the
# count. RULED (b): ONE NUMBER WHOSE NOUN SWITCHES WITH STATE — `N overdue` whenever any row has
# passed, otherwise `N upcoming`. Same reasoning as the heading: the label must match what it
# counts, and a late reader must meet the breach first. Option (a) `N deadlines` never lies but
# does what `-41d` did — survives a glance without conveying that anything is wrong.
#   var overdueCount = rows.filter(r => Number.isFinite(r.days) && r.days < 0).length;
# THE isFinite GUARD IS LOAD-BEARING: an undated row is never counted as overdue — #120's rule
# surviving into the badge, which is where it would have been easiest to drop.
#
# CC CORRECTED THE LANE'S COUNT WITHOUT ASSUMING THE LANE WAS WRONG. The lane read 8/5 off
# Charlotte's screen; CC ran the seed NORDLYS through the real regDeadlinesForBrand and got
# 10/7/3, then said the difference is presumably Cloud & Glow's own categories and markets,
# WHICH IT CANNOT SEE. Both are right for different brands — Cloud & Glow is Sweden-only and
# misses the two UK rows. "NOT FOUND, WITH THESE SEARCHES" APPLIED TO A DISAGREEMENT.
#
# THE EDIT IS PROVABLY LOCALISED: 41,774 → 41,780 lines, and EVERY anchor below 33723 moved by
# EXACTLY 6 (certificationRecords call site 33738→33744, _appendStamp 39636→39642,
# _dppIsPublished 35807→35813, checkState 40407→40413) while NOTHING above it moved.
#
# LEFT FOR DESIGN, DELIBERATELY, AND THE LINE IS DRAWN AT TRUTH: 33725's "soonest first" is TRUE
# (the sort is ascending by date) and 33704's "No upcoming deadlines" is TRUE WHERE IT APPEARS
# (reached only at zero rows, where there are no upcoming AND no overdue). ONLY THE BADGE
# ASSERTED SOMETHING FALSE. Drawing the line there kept #175 a hotfix instead of a copy pass.
# Also DESIGN's: #deadline-bar is var(--amber-bg) for EVERY row, so a breach renders inside a
# container styled as a warning.
#
# PROCESS NOTE, RECORDED RATHER THAN TIDIED: #160's narrative block below was written WHILE
# Charlotte's `git add` was running, so f98d0c1 carried the DIGEST but not the REASONING. The
# load-bearing half landed — verify.sh had the right baseline and nobody worked from a stale
# one — but the record trailed by one commit and arrives here. THE RULE IS SHARPENED: the record
# must be COMPLETE before `git add`, not merely started. Same family as 9a88b4f, caught in
# minutes instead of days because the digest was named first.

# index.html moved da3aefd5 → efeab029 on 11 Sep 2026 — #160, THE PLATFORM COULD NOT SAY A
# DEADLINE HAD PASSED. ONE LINE OF CODE, ONE WORD OF COPY, SHIPPED ALONE.
#   after   efeab029972740566c29238d9307da018879d2918ff6d6c7346df6457275dc73
#           41,774 lines · 57 gates · 1 of 6 modified
# NAMED BY THE LANE after reading 4890–4898 and 4918 at the bytes. TWO INDEPENDENT
# COMPUTATIONS AGREED (verify.sh's own shasum vs CC's reported value) — the rule that a sha CC
# reported itself is not sufficient, satisfied rather than waived.
#
# STRATEGY PLACED IT ABOVE #134 AND #150, ON EXPOSURE. Both of those had exposure of zero: no
# cosmetic product on the market, no imported catalogue. THIS ONE CHARLOTTE WAS LOOKING AT, ON
# HER OWN PRODUCTS, WHEN IT WAS FOUND. And it does not merely misstate a status — IT INVERTS AN
# OBLIGATION. "Approaching" means you have time; "Overdue" means you are already in breach.
# Telling someone they have time when they are six weeks late is the most harmful thing a
# compliance product can do.
#
# THE DEFECT: daysUntil ended `Math.max(0,n)`. Measured against REG_DEADLINES on 10 Sep,
# 8 OF 14 ROWS WERE IN THE PAST — from 27 days (uk_cmr_si_2026_23_cmrban) to 345 (epr_dk) —
# and every one reported 0d.
#   BEFORE  all 8 → `0d`, all 8 → urg() "Approaching", all 8 sorted equal at 0
#   AFTER   all 8 → their true negative, ALL 8 FLIP TO "Overdue", epr_dk(-345) sorts first
# THE LABEL FLIP IS THE EVIDENCE AND urg() WAS NOT TOUCHED — the dead branch went live purely
# as a consequence of the clamp coming off, which is what the spec demanded instead of a
# hand-wired label. 0 OF 6 FUTURE ROWS MOVED; all four undated forms still return null and
# still sort last (#118 and #120 both survive, and matter MORE now: a clamp that turned absence
# into 0 would today turn it into a LATENESS CLAIM).
#
# TWO ARTEFACTS VOUCHED FOR THE MISSING CHECK, AND THAT IS THE TRANSFERABLE PART.
#   (1) urg() at 33662 opens `if(days < 0) return 'Overdue'` — UNREACHABLE, while the legend at
#       33674 ADVERTISES "Overdue / blocked". A user who looks for it, fails to find it, and
#       concludes nothing is late.
#   (2) #118'S OWN COMMENT sat four lines above the surviving clamp calling `Math.max(0, …)`
#       "a floor-at-zero guard [that] guards nothing". #118 removed the line's NaN role and left
#       its clamping role. THE COMMENT'S ACCURACY IS WHAT MADE THE LINE LOOK AUDITED.
# > AN ARTEFACT THAT CERTIFIES A CHECK WHICH NEVER HAPPENED IS WORSE THAN NO ARTEFACT AT ALL.
# Absence prompts a check; a false assurance forecloses one. Two instances in one finding, one
# screen apart — and #167 made it three the same day.
#
# STANDING RULE, STRATEGY: fixing the defect a line is NAMED for is not the same as auditing
# what else that line does. WHEN A LINE CHANGES — COUNT ITS BEHAVIOURS, NOT ITS BUGS.
#
# CC CORRECTED THE LANE TWICE AND BOTH STAND.
#   THE CALL-SITE COUNT: the spec said three, the AST says FOUR — 4 expressions, 3 locations,
#   because the strip's comparator calls daysUntil TWICE ON ONE LINE (once per operand).
#   Reported as a discrepancy rather than reconciled to the lane's number. No unaccounted
#   consumer exists; the calendar's sort reads the precomputed a.days and never calls it.
#   THE CENSUS GENERALISES BETTER THAN THE SPEC ASKED: 6 `Math.max(0, …)` sites, 2 unsafe.
#   THE FOUR SAFE ONES FLOOR A MAGNITUDE; THE TWO UNSAFE ONES FLOOR A DIRECTION IN TIME.
#   That makes the next instance recognisable without re-deriving it. ADOPTED.
#
# THE COPY CRITERION WAS INVERSION, NOT LEGIBILITY. `0d` is the defect and `-41d` is not a fix:
# a minus sign in a mono font reads as "about zero" at a glance. A PASSED DEADLINE MUST DIFFER
# FROM A FUTURE ONE IN WORD. Shipped as `Overdue · 345d` — word first, magnitude second, using
# urg()'s own label (33662) and the legend's (33674), so no new register was invented.
# fmtNum's contract is UNCHANGED: it renders the future case and the em dash and learns nothing
# about sign. The distinction lives at one call site.
# AND CC ESTABLISHED SOMETHING THE SPEC ASSUMED WRONGLY: #deadline-bar is amber FOR EVERY ROW,
# so colour was never going to carry this. Word-not-colour was right for a better reason than
# the one given.
#
# THE HEADING WAS A SEPARATE RULING AND CC WAS RIGHT TO STOP FOR IT. `⚠ Upcoming deadlines`
# sits on the same line as an emoji belonging to #141's sweep. RULED: the word and the glyph are
# separable edits that share a line — change the word, leave the glyph byte-for-byte. Shipped as
# `⚠ Regulatory deadlines`. CC'S CONTROL IS THE RIGHT ONE: 67 `⚠` before AND after, proving the
# character was neither moved nor duplicated, which a one-line diff alone cannot establish.
# HALF-REPAIRED WAS THE ONE STATE THIS SHIPMENT COULD NOT SHIP IN: an item reading
# `Overdue · 345d` beneath a heading calling it upcoming is this finding's own defect,
# reintroduced by its own fix.
#
# #161 CONFIRMED AND THIS FIX CREATES IT. regDeadlinesForBrand (1960) filters by category and
# market ONLY; REG_DEADLINES carries no completion, dismissal or acknowledgement state and
# nothing writes one. So the app now says "345 days overdue" PERMANENTLY, with no way to record
# having done it. CC's proposed shape is right and better than the lane's instinct: a
# BRAND-CONFIRMED ACKNOWLEDGEMENT (verifiedBy:'brand', dated) — the #135 record doing the job it
# was built for — never a "done" the platform vouches for. Strategy's to rule.
#
# #173 RAISED, AND IT IS COMMERCIAL RATHER THAN COMPLIANCE. renderTrialExpiredScreen (4777) has
# 1 DEFINITION AND 0 CALL SITES across both channels, and `expired:false` is written once at
# 4641 and never set true. THE TRIAL NEVER ENDS. So `⏰ Free trial: 0 days remaining` is not a
# stopgap before an expiry screen — IT IS THE ONLY THING AN EXPIRED TRIAL EVER SHOWS. That
# reframes the sibling clamp at 4650 from "is a floor right here" to "is a floor right when it
# is the sole surface", and the answer is no: the floor is what keeps the dead path invisible.
# Second smartEmpty (#133). A SaaS with no way to end a trial is a revenue question.
#
# LEFT DELIBERATELY: the calendar's empty state at 33685 still says "No upcoming deadlines" —
# only reachable when there are zero rows, so not false today; it moves with DESIGN's pass on
# the amber container. The `⚠` at 4918 belongs to #141.
#
# #174 RAISED, HARNESS: FIVE OF SEVEN NETLIFY FUNCTIONS ARE OUTSIDE THE BATTERY —
# anthropic-proxy.js, anthropic-proxy-background.js, get-brand-pack.js, get-dpp.js,
# share-brand-pack.js. Every one is a public HTTP endpoint with no named baseline and no place
# in the one-edit count; a change to any passes GREEN at "0 of 6 modified".
# anthropic-proxy-background.js is the sharpest: a BACKGROUND function (202 immediately, up to
# 15 minutes) holding ANTHROPIC_API_KEY and SUPABASE_SERVICE_KEY, which bypasses RLS. NO
# HARDCODED SECRETS ANYWHERE IN THE DIRECTORY — every reference is process.env. The battery grew
# 4→5 (#126) and 5→6 (#130) for exactly this reason; this is 6→11, and it ships as a
# HARNESS-ONLY shipment (the #131 precedent: no tracked digest moves).

# index.html moved 93139858 → da3aefd5 on 10 Sep 2026 — #135 + documentRef: THE DOSSIER
# WRITE PATHS. ONE RECORD, NOT TWO SHIPMENTS. verify.js also moved (FIXED_CALLSITES 3→4).
#   after   da3aefd5c7c2144c7c53a7746950a036503a891194fe836ea2bafbefff28c8ac
#           41,744 lines · 57 gates · 1 of 6 modified
# NAMED BY THE LANE after reading 18262–18319 at the bytes, INDEPENDENT shasum. ONE DIGEST
# FOR THE WHOLE SHIPMENT — the naming was deliberately HELD while the stamp and the sweep
# were outstanding, because a baseline that moves twice for one shipment is the confusion
# the naming rule exists to prevent.
#
# THE CEILING ROSE 50% → 86% BY MAKING FIELDS FILLABLE, NOT BY RELAXING THE COUNT. The easy
# way to fix a 50% ceiling is to delete required fields; that road was not taken. The two
# remaining are #134's pair — nanomaterials and CMR — still deliberately unwritable.
# CAVEAT ON THE NUMBER: the category select is scoped by brand.category through
# EU_COSMETIC_CATEGORIES, so 86% ASSUMES A SKINCARE & BEAUTY BRAND. Verified for Cloud &
# Glow before the run rather than during it. #139's territory: a BRAND-LEVEL ATTRIBUTE
# CONSTRAINING A PER-SKU REGULATORY FACT — the scalar the substance discriminator already
# ruled against, surviving in a new place.
#
# STEP 0 — SCOUT FOR PRECEDENT BEFORE CONSUMERS. Strategy's new standing rule after the
# seventh instance, and it earned itself immediately: sku.safetyRef IS ALREADY the Safety
# Assessment reference — FRAMEWORK_VOCAB.cosmetic.safetyDoc names it, SINGLE_DEFINITION
# gates it, it is seeded, exported, in RETAILER_SAFE_FIELDS and in the retailer templates.
# So "Safety Assessment ref" was A SECOND NAME MISMATCH, NOT A NEW RECORD. Restructuring it
# into {value,…} would have been a second scheme for one value across a dozen consumers.
# THE PRECEDENT WON, as the rule requires.
#   Precedent 1 — the {value, verifiedBy, verifiedDate} triple exists THREE times
#   (TRUST_TIER_META, CONFIDENCE_LEVELS, VERIFICATION_TIERS) and transfers as VOCABULARY,
#   not structure: all three keep "who established this" separate from the value, and none
#   lets a lower tier borrow the higher tier's word. None is user-editable.
#   Precedent 2 — THE TREE HAS NO PRECEDENT FOR A REFERENCE TO A DOCUMENT VEYAFLOW DOES NOT
#   OWN. dppId, templateVersionUsed/outputFileHash and bil_extractions all point at the
#   platform's OWN artefacts. documentRef is genuinely new — which is exactly the temptation
#   Strategy named: holding the file tempts the product to assert what it read.
#   Precedent 3 — saveManualRp's parallel-map pattern, followed literally, and it is what
#   kept Part B from becoming a schema migration.
#
# PART A — THE COUNT IS ZERO. countryOfManufacture, mfrCountry, safetyAssessment and
# euCosmeticCategory are written NOWHERE: a rename, not a merge. AND THE CPNP HELPER WAS THE
# ONLY SURFACE READING THE WRONG NAMES — every other consumer already read countryOfMfr
# (13696, 19929, 22202, 22214, 30156, 34859, 36036) and safetyRef (6804, 17255, 17515,
# 22198, 22219). The dead aliases are GONE, not joined by a third.
#
# PART B — value in its own canonical field, metadata in sku.dossier[key] as
# { verifiedBy, verifiedDate, documentRef }. documentRef HOLDS A REFERENCE, NOT A FILE.
# verifiedBy:'brand', never 'verified', never buyer-facing as though the platform confirmed
# it — the #116 distinction caught before shipping rather than after.
#
# THE PREFILLING RULE IS LOAD-BEARING NOW, NOT LATER. Strategy: prefilling MOVES AUTHORITY —
# an empty field asks the user to know something, a prefilled field asks the user NOT TO
# OBJECT, and the second is a much weaker act. So: CONFIRMATION MUST BE AN ACT, NOT AN
# ABSENCE OF OBJECTION. dossierConfirmed requires value AND verifiedBy==='brand' AND
# verifiedDate; an unconfirmed value RENDERS, SAYS "· not confirmed", AND DOES NOT MOVE THE
# METER (measured: 10/14 = 71% with values entered but unconfirmed). Written in before
# anything extracts, so B inherits it instead of needing it retrofitted — otherwise the
# meter measures extraction instead of truth, which is #134's filled:true one level up.
# _dossierInvalidate: EDITING A VALUE CLEARS ITS CONFIRMATION. Not in the spec; it follows
# from "verifiedDate is written when the human confirms, not when the field is edited".
#
# THE FOURTH _appendStamp CALL SITE — RULED IN AS AN INSTANCE, NOT AN EXTENSION. Strategy
# ruled stamps go on a DISCRETE YES and excluded bulk writes; confirmDossier is not a
# borderline case, it is THE CONFIRMATION ACT ITSELF. FIXED_CALLSITES 3→4 with the reason
# recorded beside the number. THE GATE WORKED: it failed the battery until the decision was
# said out loud, which is a gate functioning rather than a gate in the way.
# ONE TIMESTAMP FOR BOTH WRITES — two new Date() calls can straddle a millisecond, and a
# record disagreeing with its own stamp is a worse artefact than either alone. Grouped as
# 'sku:'+id, mirroring the checklist call's stateKey: the stamp must say WHICH product.
#
# ── THE PROXY SWEEP. Strategy's new standing rule, adopted RETROACTIVELY to the shipment in
# flight: WHEN A FIELD BECOMES REAL, ITS PROXIES BECOME DEFECTS. The proxy is the inference
# that stood in for the missing field; adding the field makes the inference obsolete but
# does not remove it. Scoped BY FIELD NAME, NOT BY SCREEN — #157's lesson, that
# read-never-written does not respect function boundaries.
#
# #156 IS NOW 8 SITES ACROSS 7 LOCATIONS, for 4 facts. Three things beyond the count:
#   1. They are ~4 DECISIONS, TWO DUPLICATED — PIF_REQUIREMENTS (2), COMPLIANCE_RADAR_ITEMS
#      (1, serving two facts), and the acknowledged copy-pair at 6527/20420.
#   2. FIVE OF EIGHT INFER FROM A FIELD WITH NO WRITER, so they return THE SAME ANSWER FOR
#      EVERY PRODUCT — not a proxy for missing data, a proxy for a field that can never be
#      filled.
#   3. #135 MADE ONE CONTRADICTORY RATHER THAN MERELY WRONG. 19576 sits THREE LINES from a
#      row that now requires human confirmation and reports the same fact satisfied without
#      one — two verdicts about one fact, on one screen, disagreeing.
#   AND THE ONE FOR STRATEGY: 22595, PIF_REQUIREMENTS.prod_desc, severity BLOCKER, whose
#   description explicitly names country of manufacture — checked by !!(b?.name). A
#   REGULATORY SECTION MARKED SATISFIED BY THE BRAND HAVING A NAME.
#
# #158 RAISED — estimateCarbon RESOLVES A COUNTRY BY TRUNCATING ITS NAME.
# (sku.countryOfMfr||'').toUpperCase().slice(0,2) used as an ISO code. Measured against the
# app's own 32-country list: 2 RESOLVE TO THE WRONG COUNTRY (Denmark→DE = Germany's 0.88,
# Indonesia→IN = India's 1.30), 27 silently fall back to 1.0 (Sweden, China, Germany, Spain,
# Poland, Turkey, Vietnam), 3 correct BY COINCIDENCE. THE EDITOR'S OWN PLACEHOLDER IS
# "e.g. Sweden" — the app asks for the form that fails. confidence:'LOW' softens an
# estimate; A DANISH PRODUCT PRICED AT GERMANY'S FACTOR IS A WRONG NUMBER, NOT AN IMPRECISE
# ONE. wMult's substring test for 'sw' (matching Swedish, Switzerland and Southwest alike)
# is the same defect in the same function and folds in.
#
# #157 CONFIRMED AND LOAD-BEARING: cpnpNumber is read at 6521, 6527, 6529 and 20420 and has
# no writer — the editor writes cpnp. It is half of BOTH PIF gates.
#
# THE FIX'S SHAPE, FROM CC: once the proxies are corrected all four displaced names reach
# ZERO readers — which makes MUST_NOT_EXIST entries the way to keep them there, the same
# idiom that holds normalizeBrandRP absent.
#
# NOTICED, TRIVIAL, NOT FIXED: skuForm.id || 'unsaved' at 18317 is a guard that cannot fire,
# since newSkuForm always sets an id. The shape declined five times this week.

# index.html moved 80555dcc → 93139858 on 9 Sep 2026 — #148 PART 1, SKU ID COLLISION.
# ONE LINE OF CODE, seven of comment.
#   after   93139858c820002eedc5fa1ec95359d21632237a38c1d13710817bb835e1c81a
# NAMED BY THE LANE after reading 18234–18243 at the bytes, INDEPENDENT shasum.
#
# THE DEFECT, LIVE TODAY ON THE APP'S OWN RECOMMENDED IMPORT PATH: newSkuForm() set
# id:Date.now().toString() — MILLISECOND resolution — and the importer calls it inside a
# SYNCHRONOUS .map() over every row. Measured on both sets of bytes:
#   BEFORE  20 rows → 3 distinct ids · 100 rows → 2 · 500 rows → 3
#   AFTER   20 → 20 · 100 → 100 · 500 → 500
# id is not in SKU_FIELDS so nothing overwrote it downstream, and there are 24
# .find()-by-id resolution points, every one returning the FIRST match.
#
# FIXED WITH retailUuid() (37882), THE TREE'S OWN GENERATOR, already used for retail rows.
# Block ordering confirmed before editing: both are top-level FunctionDeclarations in BLOCK
# 1, neither nested nor variable-assigned, so hoisting covers the backward reference.
# HONEST CHARACTERISATION, VOLUNTEERED: retailUuid is NOT a UUID — within one millisecond
# uniqueness rests on 6 base-36 chars (2.18e9). Birthday probability at the app's own stated
# ceiling of ~500 SKUs per batch is 5.7e-5. Adequate here; inventing a generator would have
# been worse than reusing the tree's.
#
# NOTHING EXISTING MOVES, STRUCTURALLY: 27 assignments to a .id property across both blocks,
# EVERY ONE a DOM element id. Nothing in the tree reassigns sku.id. Seed ids are hardcoded
# literals. AND THERE IS NO DELETE PATH FOR SKUs ANYWHERE — only add and edit.
#
# #150 RAISED, AND IT IS THE SHARPEST LIVE CONSEQUENCE — DESTRUCTION, NOT MIS-RESOLUTION.
# saveSkuEdit does skus.findIndex(...) then skus[idx]={...skuForm}. SO EDITING MEMBER 57 OF A
# COLLIDED IMPORT OVERWRITES MEMBER 1. Silent data loss, on existing catalogues, TODAY, with
# no backup and no delete path to undo it. The lane had been treating the back-fill as
# cleanup for confusing data; it is stopping an ongoing loss. THAT MOVES THE BACK-FILL UP.
#
# AND THE QR RISK IS NARROWER THAN THE SPEC FEARED — the lane pointed the danger at the wrong
# asset. Because ONLY MEMBER #1 IS EVER ADDRESSABLE, a second publish cannot mint a competing
# passport: it re-publishes #1's data to #1's dppId. THE PASSPORT IS SAFE; THE CATALOGUE IS
# NOT.
#
# #151 RAISED — addToSourcingCRM (36631) reached from a .map() at 28444: same generator, same
# missing random component, and id:Date.now() there is a NUMBER, not even type-compatible
# with SKU ids. Weaker because each call follows an awaited round-trip.
#
# THE LANE'S legacyId INSTINCT WAS HALF WRONG AND CC REFUTED THE LOAD-BEARING HALF.
# PRESERVE: yes — it deletes nothing, makes the migration re-runnable, and is a BETTER guard
# than _schemaVersion because it is per-record AND records what happened.
# RESOLVE THROUGH EITHER: NO. `s.id===X || s.legacyId===X` through find-first does not
# disambiguate a collided group — it returns an arbitrary member SILENTLY. THE DEFECT
# RE-CREATED INSIDE ITS OWN FIX, and a direct violation of "the system does not choose".
# Correct shape: resolve through legacyId ONLY when exactly one SKU carries it; more than one
# is the `attention` case, surfaced and unresolved.
#
# MIGRATION PRECEDENT CORRECTED: it is migrateSkuSchemaV2–V8 (nine migrations exist), not the
# brand one. AND PER-RECORD _schemaVersion IDEMPOTENCY IS ACTIVELY DANGEROUS HERE: an
# interrupted run that reassigns ids before rewriting dppData keys leaves DANGLING REFERENCES
# PERMANENTLY, with no way to detect it. No prior migration crossed a reference boundary;
# this one crosses seven local stores and two server columns.
#
# THE SCOUT MISSED loop_events.sku_id (supabase-proxy.js:514) — DURABLE AND READ BACK via
# loop.list. It is the ONLY sku-id reference that both survives a browser wipe and returns
# into the app, so it is the one place an orphan is guaranteed to surface rather than stay
# latent. dpp_records.sku_id is durable but write-only from the client's perspective.
#
# TWO CACHE FAMILIES EMBED THE ID IN THE localStorage KEY ITSELF — ns_inci_<skuId>_<market>
# and ns_pkg_<skuId>_<market>. A back-fill must RENAME KEYS, not rewrite fields — and
# _evictCaches() may already have deleted some, so absence is normal and not evidence of a
# missed record.
#
# STRATEGY'S "SYSTEM DOES NOT CHOOSE" RULING STANDS WITH ITS COST REVISED DOWN: genuine
# ambiguity needs two content-identical SKUs, since only member #1 is addressable and
# dpp_records stores name and ean. "A passport attached to nothing" is a RARE TERMINAL CASE,
# not the expected one.

# index.html moved 914f549c → 80555dcc on 9 Sep 2026 — #141 PART B, one line, plus the
# three owed reports. NAMED BEFORE THE COMMIT, which is the process fix for what went wrong
# on 9a88b4f.
#   after   80555dccd90119286eb5d48679e0abef203c75e20bb5d67ce201b411646a488b
#           41,611 lines · 57 gates · 1 of 6 modified
#
# THE LINE: "Upload an existing Apotek Hjärtat or Matas Excel file — columns recognised
# automatically" → "Apotek Hjärtat and Matas column names are recognised automatically."
# The imperative goes, the capability stays. It had also become a more prominent duplicate
# of the secondary line now inside the upload card. DESIGN may still overrule the wording.
#
# ── REPORT 1 — THE EMOJI SWEEP. 101 occurrences, 99 remain, ALL IN index.html.
# dpp/index.html, portal.html and brand/index.html: 0 — reported as NOT FOUND, WITH THESE
# SEARCHES, per the standing rule adopted the same evening. Two channels: every character
# tested against \p{Emoji} then split on \p{Emoji_Presentation} with a U+FE0F lookahead, and
# every \uXXXX decoded with surrogate pairs recombined — the second is the channel that hides
# from a glyph search, and where BOTH of #141's lived. TWO MORE ARE STILL ESCAPED: ⚡
# (27358) and 💡 (27365). Heaviest: 🔴×8 ⏰×6 🔒×6 ⛔×6 🟢×5 ⭐×4, plus a block of 15
# at 5139–5255. The criterion is Emoji_Presentation rather than CC's own judgement line, SO
# THE 99 IS CHECKABLE; ⬛ (35553) and ⚪ (8579) sit ON the boundary as status dots beside
# 🔴🟢🟡 and are flagged for a joint ruling rather than decided unilaterally.
#
# ── REPORT 2 — DURABILITY. MORE SURVIVES THAN LOOP EVENTS, AND THE GAP IS NOT WHERE THE
# LANE LOOKED. persistCritical verifies the write landed IN THIS BROWSER — read-back, length
# compare, evict-and-retry, and it TELLS THE USER on failure. That is a real guarantee about
# quota and corruption and NOT a guarantee about durability. A SECOND, WEAKER TIER exists:
# savePageState is a bare setItem in a try{}catch(e){} with an EMPTY CATCH — 9 form keys,
# silent on quota failure, the opposite contract on the same screens (#146).
#   SURVIVES A BROWSER WIPE: brand (brand.save on all 18 saveBrandState calls; brand.load on
#   boot) · loop events (server-authoritative) · BIL rows · portal submissions PARTLY — the
#   row lives server-side, the local record does not come back.
#   DOES NOT SURVIVE: sourcingCRM · mfr field overrides · listing submissions · SKUS ·
#   everything else.
#
# #143 — THE CATALOGUE DOES NOT SURVIVE. saveSkus is never mirrored and NO SKU ACTION EXISTS
# IN THE PROXY AT ALL. SKU data reaches a server only as a side effect of publishing a DPP or
# creating a portal submission — partial projections of an explicit user action. The
# product's most-used data lives in one browser.
#
# #144 — FOUR PROXY ACTIONS ARE IMPLEMENTED AND CALLED BY NOTHING: crm.load, field.load,
# listing.list, crm.delete — 0 occurrences on any of the four surfaces. THREE OF THEM ARE
# EXACTLY THE READ-BACKS that would make sourcingCRM, field overrides and listing submissions
# durable. THE DATA IS ALREADY ON THE SERVER; THE APP NEVER ASKS FOR IT. #133 INVERTED —
# written-never-read, across a network boundary. Closing it is WIRING, NOT ARCHITECTURE.
#
# THE SENTENCE THAT REFRAMES STRATEGY'S DOCUMENT RULING: A DOCUMENT UPLOADED TODAY WOULD BE
# MORE DURABLE THAN THE SKU IT DESCRIBES. The Supabase Storage path ALREADY EXISTS AND IS IN
# PRODUCTION — bil.uploadUrl returns a signed URL and the browser PUTs straight to Storage
# (2934–2950), written that way because ~569KB of base64 cannot pass through a Netlify
# function, with a real failure path that marks the row `failed` rather than leaving it on
# `processing`. SO DOCUMENT UPLOAD DOES NOT SIT BEHIND BUILDING PLUMBING. It sits behind
# RETENTION AND CUSTODY — a policy gate, which is precisely Strategy's stated concern — and
# behind the fact that NOTHING BUT brand HAS A RESTORE PATH.
#
# CC CORRECTED ITSELF TWICE BEFORE REPORTING: it first read bil.uploadUrl as uncalled (it is
# called by a RAW fetch, not a wrapper) and the portal.* actions as unimplemented (a
# three-segment name its pattern missed; index.html does not call them, portal.html does).
#
# ── REPORT 3 — APOTEK HJÄRTAT AND MATAS: 172 MENTIONS, split structurally (a string in a
# top-level const initialiser is DATA; in a function body it is COPY). 134 in reference data
# across 29 structures — out of scope by the boundary rule. 38 IN RENDERED COPY across 20
# functions: showSkuExport 9, scoreReadiness 3 (the hardcoded ediMatas blocker for the "Matas
# Taastrup hub"), exportForRetailer 2, renderRetailChecklist 2, printRetailChecklist 2,
# showCSRDListingPrompt 2, renderBrandPack 2, generateBizCase 2, eleven singletons.
#   EARNED: the export copy (RETAILER_TEMPLATES really holds both), downloadSkuTemplate's
#   column hint, showCSRDListingPrompt (both ARE CSRD-obligated).
#   THIN: the retail-checklist copy rests on LISTING_REQUIREMENTS, which has 2 entries. One
#   mention sits inside smartEmpty — copy in UNREACHABLE code.
#   AND A CORRECTION TO THE LANE'S SPEC: RETAILER_REGISTRY holds 214 entries, not 158. So it
#   is 2 OF 214 — 0.9% — carrying the product's entire "we know retail" surface across 38
#   mentions in rendered copy. The finding SHARPENS rather than softens.
#
# #145 — RETAILER_REGISTRY HAS 8 DUPLICATE IDS. 214 entries, 206 distinct: kicks_dk,
# normal_dk, magasin_dk, illum_dk, ici_paris_xl_be, di_be, kruidvat_be, helsam_dk — each at
# ~9533–10701 AND again at ~12070–12284. Two differ in NAME as well (Di (Parfuma) vs Di
# (Drogisterij Di); ICI Paris XL vs ICI PARIS XL), so this reads as a MERGED BLOCK, not a
# paste slip. .find silently takes the first; .filter returns two — the behaviour depends on
# which the caller used.
#
# #146 — savePageState's EMPTY CATCH, above.
#
# RAISED AS A QUESTION FOR STRATEGY, NOT NUMBERED AS A DEFECT: brand.save mirrors the WHOLE
# brand object — including the EU RP's name, email and address — ON EVERY AUTOSAVE. Whether
# business contact data belongs on an automatic path rather than an explicit one is a policy
# call, not a code fault.
#
# STILL OWED: #141 smoke step 3 — a real CSV and a real Apotek Hjärtat file through a browser.
# The structural evidence (import machinery byte-identical, no import identifier in either
# diff) is necessary and not sufficient.

# index.html moved 5461416e → 914f549c on 9 Sep 2026 — #141, THE ADD PRODUCTS SCREEN.
# Both emojis removed; the VeyaFlow template promoted to RECOMMENDED; the retailer-template
# line demoted to a secondary sentence.
#   after   914f549c76ed881db466f8d6e80cc75d4f7210054e72ba800f8d435dfca0b7d3
#
# NAMED AFTER THE FACT, AND THE PROCESS FAILED — RECORDED HERE RATHER THAN TIDIED AWAY.
# Commit 9a88b4f carries this work under the message "#134: nanomaterials and CMR no longer
# declared None unasked … name 5461416e" — the PREVIOUS shipment's message, and a digest the
# file does not have. Three rules were skipped: the lane never named the value before the
# commit, verify.expected.txt recorded 5461416e while the tree held 914f549c (so verify.sh
# would FAIL its own digest check), and CC's three reports were never delivered.
# Same class as the mislabelled commit 39b7725 on 3 Sep. CORRECTED FORWARD, no rewriting —
# 9a88b4f's message stays wrong in the history and this entry is what makes it findable.
#
# THE WORK ITSELF IS SOUND AND WAS BYTE-VERIFIED AFTERWARDS, by reading the whole diff:
#   📂 (20005) and ✏️ (20011) removed — canon already forbids emoji in
#   UI, so this was ENFORCEMENT, not a new decision. Both were written as UNICODE ESCAPES,
#   so a search for emoji glyphs would not have found them.
#   A new top card — "Start from the VeyaFlow template", RECOMMENDED, wired to
#   downloadSkuTemplate() — replaces the old bottom-of-screen "Need a template?" footer.
#   "Upload a file" loses its RECOMMENDED badge and its Apotek/Matas subtitle, gaining
#   "Already filled a retailer's template? Upload it."
#
# PART B IS HALF-DONE. Line 20023 STILL reads "Retailer templates: Upload an existing Apotek
# Hjärtat or Matas Excel file — columns recognised automatically" in the green
# Accepted-formats box. The headline moved; the box below it still leads with the retailer
# claim. Owed.
#
# THREE REPORTS OWED AND ONE OF THEM GATES A WHOLE BUILD:
#   (1) the emoji sweep count across all four HTML surfaces, WITH THE SEARCH TERMS STATED;
#   (2) WHETHER SKUs AND BRAND DATA MIRROR TO THE SERVER AT ALL — persistCritical is
#       localStorage with a read-back; loop events reach Supabase via the proxy; nothing
#       else has been checked. This decides how far behind the gate DOCUMENT UPLOAD sits,
#       and Strategy has ruled that the document half must not precede durability: a lost
#       text field can be rewritten, a lost CPSR may be the customer's only copy.
#   (3) every other place naming Apotek Hjärtat or Matas in user-facing copy — those two
#       appear wherever the product needs to look capable (LISTING_REQUIREMENTS 2 of 158,
#       printable checklists, the importer). The count is the finding.
#
# THE CLAIM ITSELF IS TRUE AND #142 WAS WITHDRAWN. RETAILER_TEMPLATES (19835) really does map
# Apotek Hjärtat's Swedish column names. THE LANE ASSERTED ITS ABSENCE FROM AN INCOMPLETE
# GREP — having reported L34732 correctly two days earlier as "an absence, not a clearance".
# STANDING RULE ADOPTED: a negative result is reported as "NOT FOUND, WITH THESE SEARCHES",
# never as "does not exist". Asserting absence is the hardest claim there is.
#
# THE STRUCTURAL FINDING UNDERNEATH THE SCREEN, and it is the week's largest: THE IMPORTER
# CARRIES THE COMMERCIAL RECORD AND STOPS EXACTLY WHERE THE DOSSIER BEGINS. SKU_FIELDS
# (19811) is 21 columns — name, EAN, article, net content, country, shelf life, INCI, CPNP
# number, EU RP, six RSP currencies, net price, MOQ, lead time, two descriptions. NONE of
# #135's five blockers is importable: no nanomaterials, no CMR, no safety assessor, no PIF
# reference, no safety assessment ref. The CPNP helper marks 14 required; the importer can
# carry 4. THAT GAP IS #135, and the importer and the write path are ONE piece of work.
# Strategy: the codebase is display-first and the importer is spreadsheet-first — one
# observation. The product built everything fillable from a sheet and nothing requiring a
# document or a judgement. It explains the 64% wall, the seven blockers, the five unfillable
# fields and #109 at once — and it means THE FRONT DOOR IS THE LEAST-BUILT PART, contrary to
# what the surface suggests.

# index.html moved c8a754f2 → 5461416e on 9 Sep 2026 — #134, "None" DECLARED FOR
# NANOMATERIALS AND CMR SUBSTANCES, UNASKED. TWO LINES, SHIPPED ALONE.
#   after   5461416e60ab5c0e9b50a6ada0692d490baed339d3672f32d4cd711facb4babc
# NAMED BY THE LANE after reading 19423–19424 and 19301 at the bytes, INDEPENDENT shasum.
# Five other surfaces UNCHANGED; 1 of 6 modified; 57 gates.
#
# STRATEGY: ITS OWN SEVERITY CLASS, NOT MERELY FIRST IN THE QUEUE. Nanomaterials and CMR
# are the two categories under EC 1223/2009 carrying the strictest notification
# requirements. A false "None" on a surface that feeds a CPNP notification is not a product
# defect — IT IS AN INCORRECT STATEMENT ON ITS WAY INTO A REGULATORY FILING. Everything
# else on the board damages trust; this could lead a customer to file an untrue declaration.
#
# THE FIX WAS TO MAKE TWO LINES CONSISTENT WITH SEVENTEEN SIBLINGS. Nineteen object literals
# in the file carry `filled:`; ALL NINETEEN are in this one array; seventeen derive it from
# their value and these two set it unconditionally. After this shipment the count of
# non-derived `filled:` is ZERO. A PAIR, NOT A PATTERN — the `done:` shape was checked too
# (40 objects, 12 done:false asserting nothing, 3 done:true all async-loading flags).
#
# THE NUMBER GETS WORSE ON PURPOSE AND WAS MEASURED FROM BOTH SETS OF BYTES, NOT COMPUTED:
# the CPNP readiness ceiling falls 64% → 50% (9/14 → 7/14), and the seed cosmetic SKU 57% →
# 43%. THE BANNER ALSO CROSSES A THRESHOLD — readyPct>=60 selects amber — so the panel goes
# amber → red. Not compensated for. Strategy's gate point 7: a pilot that hits a wall at 64%
# has a worse experience than one meeting an honestly empty product. #135 inherits a
# MEASURED ceiling of 50%, not the 64% it was scoped against.
#
# THE CONTROL IN SMOKE STEP 3 IS THE STRONGEST EVIDENCE IN THE REPORT: a SKU that DOES carry
# nano and CMR values scores 64% before AND 64% after — identical. That proves the drop comes
# only from the unasked case, which no amount of reasoning about the diff could establish.
# 17 of 17 other rows byte-identical on {value, filled, required, hint, cell}.
#
# REPORT 2 DECIDED THE FIX'S SHAPE AND WAS DONE BEFORE EDITING. The renderer keys off
# f.value, NOT f.filled: an unfilled field renders "⚠ Missing — add in My Products" in red
# italic plus a red ✗ row. NOT a blank, and it says THE OPPOSITE of "None" — so the sibling
# treatment was sufficient and no not_recorded string was invented.
#
# THE RESIDUAL, NAMED RATHER THAN LEFT: that remediation copy is ITSELF UNTRUE. There is no
# nanomaterials or CMR input in My Products (#133: no writer, any channel). This shipment
# converts A FALSE REGULATORY DECLARATION into A FALSE NAVIGATION HINT — a large net
# improvement and the right trade — but by the standing rule that a false claim can be
# phrased as an instruction, the copy is now what is left. It belongs to the write path.
#
# #139 RAISED, AND IT IS BIGGER THAN #134. THE CPNP HELPER IS GATED ON BRAND CATEGORY, NOT
# PRODUCT FRAMEWORK. Line 19301, inside the per-SKU loop:
#   getBrandCategories().some(c=>c.includes('Skincare')||c.includes('Beauty'))
# openCPNPModal and renderCPNPModal contain ZERO references to resolveProductFramework and
# ZERO to .productType — counted on the AST. So every SKU of a beauty brand gets an
# EU-cosmetics CPNP notification helper, DEVICE AND ACCESSORY INCLUDED, complete with an INCI
# row. THE SHIPPED SEED DEMONSTRATES IT: brand category 'Skincare & Beauty', two SKUs, one
# cosmetic and one device — the device gets the helper.
# THIS IS #108'S SHAPE ON A REGULATORY SURFACE: a brand-level scalar standing in for a
# product-level regime. resolveProductFramework exists so that `unknown` is first-class and
# cosmetic is never a default, and this surface never calls it.
#
# EXPOSURE, SETTLED BY VERIFICATION RATHER THAN BY PREMISE. Strategy assessed it as zero
# because the catalogue is device and accessory. THE HYPOTHESIS DOES NOT SURVIVE on two
# independent grounds: the catalogue contains a cosmetic SKU (the portal shows "Cloud & Glow
# Face Serum · Cosmetic · EU 1223/2009" with CPNP on file), AND a device-only catalogue would
# not have avoided the surface anyway, because the framework resolver is never consulted.
# Strategy accepted the correction and downgraded on the DERIVATION while the CONCLUSION
# held — no cosmetic product is on the market today, so no filing is pending. SECOND
# SELF-CORRECTION IN TWO HOURS under the rule they adopted the same evening: Strategy states
# claims about runtime behaviour as HYPOTHESES until the lane verifies them.
#
# ALSO NOTICED: brand.euResponsible.phone is a FOURTH dead read on this surface (RP phone) —
# manualRpForm writes name, email, address, renewalDate and no phone. It sat outside #133's
# scope because it hangs off a NESTED object, which is a limit of that sweep worth knowing.
# #135 should count it.
#
# THE TIMING IS WORTH RECORDING. #134 was found by a sweep aimed at something else and fixed
# THE DAY BEFORE the product it would have harmed became the priority. The face tape is a
# real, blocked cosmetic product, and #135's five unfillable required fields — nanomaterials,
# CMR, safety assessor, PIF reference, country of manufacture — ARE ITS BLOCKERS. Had it
# moved first, the surface offering to prepare its CPNP notification would have declared
# "Nanomaterials present: None" about a product whose dossier is the actual problem.

# index.html moved 6d4d09b7 → c8a754f2 on 9 Sep 2026 — TIME AXIS STEP 8, MIGRATE 1: the
# record accessor. MIGRATE IS FOUR SHIPMENTS, NOT ONE — M1 the accessor and ruling 5's gate
# (behaviour-neutral), M2 the writer + its three editor includes tests + the description
# prompt as ONE UNIT, M3 legacy conversion and the five publish/persist sites, M4 Strategy's
# rulings 1–3. M1 FIRST because the authorised path must exist before anything produces data
# that needs it — expand-before-write, one level up.
#   after   c8a754f2695427c8dc249d53caa3c079e9d176618fdf1c1cd272c8d0adcd0aea
# NAMED BY THE LANE, INDEPENDENT shasum. Five other surfaces UNCHANGED; 1 of 6 modified.
#
# certificationRecords() returns [{name, expiryDate?, verifiedBy?}] — ONE COPY, index.html
# ONLY. brand/index.html and dpp/index.html do not read expiryDate and MUST NOT GAIN IT, so
# this is deliberately not a copy-gate concern. A string entry becomes {name} with NO
# expiryDate and NO verifiedBy: it does not invent verifiedBy:'not_recorded', because HOW AN
# ABSENCE RENDERS IS M4'S RULING and this returns what exists.
#
# THE LANE'S GATE WAS UNSATISFIABLE AND CC RESHAPED IT CORRECTLY. The spec demanded "zero
# occurrences of .expiryDate outside the accessor" — which would forbid the CONSUMERS from
# reading it off the records the accessor RETURNS, i.e. forbid the accessor's entire purpose.
# Ruling 5 forbids reading it off THE RAW FIELD. The gate now asserts: every .expiryDate
# outside the accessor's body sits inside a function that CALLS certificationRecords().
# Located by banner and closing brace, never by line number.
# SEEN FAILING on an injected plausible consumer — a new certExpirySummary() reading
# (brand?.certifications||[]).some(c=>c.expiryDate) → FAIL naming the line and the reason;
# reverted; PASS. certificationRecords added to SINGLE_DEFINITION and to FIXED_CALLSITES at 2,
# so a third consumer is a deliberate decision rather than drift.
#
# THE EVIDENCE: 12 cases three-way. NINE IDENTICAL — including `label instead of name`, since
# the accessor resolves label exactly as the old chain did. THREE DIFFERENCES, all reported:
# a NAMELESS entry with an expiryDate previously raised a calendar event titled "Certification
# expires" with sourceId null and flipped hasCertData true (hiding the prompt); a nameless
# entry among named ones produced a second unnamed event; and a BARE STRING THREW
# (`forEach is not a function`). All three now return empty or are dropped.
#
# #132 RAISED, AND IT IS LARGER THAN THIS SHIPMENT. brand.certifications HAS SEVEN CONSUMERS
# AND NO WRITER. Read at 7 sites, written at 0 — verified independently: no assignment exists
# anywhere; 1305 and 18253 are the SKU template, 21326 is skuForm, the rest are third-party
# registries and requestListedForm. The seed brand has no certifications key; onboardForm has
# none; `expiryDate:` APPEARS IN ZERO OBJECT LITERALS ACROSS THE TREE.
#   So the Compliance Calendar's certificate-expiry feature has never had data, hasCertData is
#   therefore false, and the empty state ALWAYS shows "Add your certification dates in Brand
#   Profile" — POINTING THE BRAND AT A PLACE WHERE NO SUCH FIELD EXISTS. That is #122's class
#   on a brand-facing surface: the app instructing a user to do something it does not support.
#   It is also GAP 3 INVERTED — Strategy called hasCertData "gap 3 in miniature, already
#   shipping" because it says what remains with a button to the fix. It says what remains and
#   the button goes nowhere.
#   CAVEAT KEPT: ns_brand is persisted, so a workspace carrying data from an older build could
#   hold entries this code cannot produce. Reported as a difference, not folded into the pass.
#
# M4 IS DEFINING A SCHEMA, NOT INHERITING ONE. Three fields exist in the tree — name (18
# uses), label (5), expiryDate (3). No issuer, no verifiedBy, no documentRef anywhere.
# Strategy's ruling 1 ("the migration is what makes the rendering legitimate") presumed a
# rendering with data behind it. IT MUST SEE #132 BEFORE M4 IS SPECCED.
#
# REPORT 1 — normalizeCertifications is NOT re-expressed as accessor(raw).map(r=>r.name), and
# CC's reason beats the lane's instinct: the name reader is THREE byte-identical copies under
# the copy gate; the record accessor is ONE, deliberately. Coupling them forces either copying
# the record accessor to surfaces the spec forbids from having a validity concept, or letting
# the copies diverge and defeating the gate. Duplicated drop-logic in adjacent identical text
# is the lesser cost; divergence would be a #131 count contract, not a refactor.
#
# NOTICED: the gate's function-scope walk uses ^\} to find a function's end — correct for this
# file's top-level style, mis-scoping a nested declaration, and CONSERVATIVE IN THE SAFE
# DIRECTION (a too-large block makes the gate more permissive, never falsely failing).

# NO DIGEST MOVED — #131, 9 Sep 2026, HARNESS ONLY. All six tracked surfaces unchanged;
# 0 of 6 modified; verify.js the only modified file. Recorded here because the reasoning
# belongs with the shipments even when no baseline moves.
#
# THE FINDING, TWICE IN TWO SHIPMENTS: six export locale rows (3b) and the ESG merged-cert
# spread (3c) were IDENTICAL BY LUCK. In both cases one sed with an exact count replaced
# them all — possible only because nobody had hand-edited one. That is not a property of
# the code; it is a fact about what has not happened yet. #128 is what it looks like
# afterwards: six root copies, five untracked and identical, one tracked and rotted.
#
# NEAR_COPY_GROUPS: named groups, each an expression anchor plus an EXACT expected count.
# A count that differs either way FAILS until someone updates the contract deliberately —
# same discipline as FIXED_CALLSITES. A floating count is not a contract.
#   export locale certification rows        6
#   ESG merged sku+brand certification spread  3
#   csrdAutoFill allCerts spread members     3
#
# THE LANE'S COUNT WAS WRONG ON GROUP 2 AND THE REASON IS INSTRUCTIVE. The spec said 2,
# anchored on the BYTE-IDENTICAL PAIR (calcBrandESGScore, checkMandatoryESG). renderESGModal
# carries the SAME MERGED SPREAD without the trailing .toLowerCase() map — not byte-identical,
# equally dangerous to leave behind. CC anchored on THE SPREAD, giving 3, because the spread
# is what must stay in agreement: edit one merge and the three functions compute DIFFERENT
# CERTIFICATE SETS. The lane defined the group by a SURFACE PROPERTY (byte-identity of two
# lines) instead of by THE INVARIANT. Scope-by-shape, one level in. The measurement won, as
# the spec instructed, and no code was adjusted to match the spec.
#
# GROUP 3 NEEDED CARE: a naive `...` count returns 4 — `[...new Set([` contributes one. The
# contract counts only lines whose TRIMMED TEXT STARTS with `...`, giving the three real
# members. It also reports NOT FOUND rather than a count if its anchor disappears, so a
# rewritten allCerts cannot pass silently.
#
# ALL THREE SEEN FAILING, then reverted from a pristine copy: an export row reverted to the
# pre-3b form → 5 of 6; an ESG spread narrowed to sku only → 2 of 3; an allCerts member
# deleted → 2 of 3. The failure message NAMES THE REMEDY (update NEAR_COPY_GROUPS
# deliberately if intended), so the gate asks for a decision rather than blocking.
#
# GROUP 3 IS WRITTEN FOR A SHIPMENT THAT HAS NOT HAPPENED. Its anchor is the CONTAINING
# LITERAL and it counts MEMBERS, not shapes — so migrate can rewrite all three bodies and the
# gate keeps passing, because the property protected is "THREE SOURCES FEED ONE PERSISTED
# STRING", not "these three expressions look like this". It fails exactly when it should: if
# migrate converts two and leaves one. Groups 1 and 2 pin an expression shape, so migrate
# changing that shape means updating the pattern deliberately. That is intended.
#
# NO OTHER GROUP DESERVES A CONTRACT, AND THE COUNT IS THE EVIDENCE: 162 line-groups repeated
# 3+ times and 352 repeated logic lines, examined. The 162 are REGISTRY DATA
# (verifiedDate/verifiedBy ×220, brandModel ×201); the 352 are IDIOMS (renderFind(...) ×22).
# Neither is the hazard. THE HAZARD IS A COMPUTATION DUPLICATED ACROSS CALL SITES THAT MUST
# STAY IN AGREEMENT, and in this file that is exactly these three. Not structural — three
# hardcoded numbers is the right answer. CC: "Had the number been twenty, I would be
# reporting the opposite."
#
# IT LIVES IN verify.js, and the AST-vs-text objection does not hold: verify.js ALREADY does
# raw textual counts (PERSIST_KEYS counts quoted occurrences; the copy gate compares extracted
# source text). It already reads the file once and owns line()/section(). The distinction that
# matters is not AST-vs-text but "does this belong to the file-contract battery" — it does.
#
# HONEST LIMITS, BOTH WRITTEN DOWN RATHER THAN ONE GATE CLAIMING BOTH: it reads index.html
# only, so a group spanning surfaces would be invisible (the reader is guarded separately);
# and IT PINS CARDINALITY, NOT CONTENT — rename a label in one export locale and the count
# stays 6. The copy gate is the shape that pins content where content matters.
#
# THE EPITAPH ON 33362/33514 WAS RETIRED BEFORE IT WAS WRITTEN. Strategy's ruling 5: after
# migrate the second accessor is the ONLY path to expiryDate, so a direct read becomes
# IMPOSSIBLE rather than forbidden and the comment is never needed. Two sites that would have
# required a note explaining why they are special simply stop being special. Fourth time this
# week the answer was to move a condition from policy into structure — and the first time it
# retired a fix already planned.

# index.html moved eb829955 → 6d4d09b7 on 9 Sep 2026 — TIME AXIS STEP 7, EXPAND PHASE 3c.
# THE EXPAND PHASE IS COMPLETE. Five surfaces tolerate all three certification shapes;
# portal.html never needed it.
#   after   6d4d09b707228e89f99b68ae2b7b2ed3fe7374ebe98283e329ff4295afa245f7
# NAMED BY THE LANE after reading 32434–32438 at the bytes, INDEPENDENT shasum. Five other
# surfaces UNCHANGED; 1 of 6 modified; copy gate still three surfaces, 2269 bytes.
#
# THE GAP 3b LEFT WAS THE LANE'S SCOPE DEFINITION, NOT CC'S ENUMERATION. 32436/32475/32528
# spread sku?.certifications RAW into a merged array, and an object entry is TRUTHY, so
# `(c||'')` passed it through and `.toLowerCase()` THREW. They fell outside 3b's scope
# because the lane wrote it as "reads that produce a DISPLAYED OR PROMPT STRING" — these
# produce a MATCHING ARRAY. STANDING CORRECTION: THE SHAPE MUST BE THE OPERATION PERFORMED
# ON THE FIELD, NOT THE DESTINATION OF THE RESULT. It is also what makes the publish/persist
# sites fall out cleanly — excluded by destination ON PURPOSE, not by oversight.
#
# THE EVIDENCE: 160 comparisons — 10 sku shapes × 8 brand shapes × 2 expression forms,
# three-way against a pre-edit capture. 60 identical, 100 differ, and REACHABLE DIFFERENCES:
# ZERO. Reachability stated per side rather than assumed — sku is written only by
# toggleSkuCert from a hardcoded list; brand HAS NO WRITER IN THE FILE AT ALL (it arrives
# from the seed at 1980 and onboarding at 5338) and is already the object form.
# TODAY'S CODE THROWS IN 38 OF 80 COMBINATIONS at the two .toLowerCase() sites. After the
# change, none throws. Expand closes that ahead of the writer, which is the phase's purpose.
#
# THE `||''` GUARD DROPPED: the reader's contract is that every element is a non-empty
# string, so it cannot fire. .toLowerCase() stays and does real work — the comparison
# downstream is case-insensitive.
#
# REPORT 1 ANSWERED BEFORE ANY EDIT, AS INSTRUCTED. 26228 PUBLISHES — shareBrandPack's
# brandPackData goes to fetch('/.netlify/functions/share-brand-pack') at 26245–26251. 34761
# PERSISTS — csrdAutoFill folds it into answers.certifications and
# persistCritical('ns_csrd_answers', …) at 34802. BOTH TO MIGRATE, NEITHER TOUCHED.
#
# AND 34761 HAS TWO UNNAMED SIBLINGS THE SPEC MISSED: 34759 (sku0?.certifications) and
# 34760 (skus.flatMap(s=>s.certifications||[])). All three feed ONE persisted string, so
# they MUST MOVE AS A UNIT — normalising the brand half of a value whose sku half stays raw
# is the exact defect this migration exists to close.
#
# THE ACCURATE COMPLETION STATEMENT IS CC'S, NOT THE LANE'S. The spec's phrasing — "none
# except the expiryDate consumers and the writer" — OMITTED THE FIVE PUBLISH/PERSIST SITES.
# They are still RAW READS. Correct: "none except the expiryDate consumers, the writer and
# its editor, and the five sites that publish or persist."
#
# THE BOUNDARY IS NOW VISIBLE IN THE CODE AND UNMARKED — A TRAP. 33362 (buildComplianceEvents)
# and 33514 (.some(c=>c.expiryDate)) MUST stay raw: they read a field the reader deliberately
# DISCARDS, and brand.certifications is already the object form so they already work. They sit
# THREE LINES from sites that just moved, with nothing in the file saying why. A future sweep
# would "finish the job" and break the Compliance Calendar. THE EPITAPH GOES IN WITH MIGRATE,
# where those sites either get the comment or get the second accessor.
#
# THE READER ANSWERS "WHAT ARE THEY CALLED", NEVER "WHAT DO WE KNOW ABOUT THEM." A second
# accessor returning the OBJECT form is a migrate-phase design question with two existing
# consumers already waiting for it — and it is where Strategy's validity-rendering ruling
# will land. The question is no longer abstract; it is a named function that does not exist.
#
# #131 APPEARED TWICE IN TWO SHIPMENTS. Six export locales (3b), then 32436/32475 —
# calcBrandESGScore and checkMandatoryESG, BYTE-IDENTICAL, confirmed by string comparison
# rather than by eye. In both cases ONE sed WITH AN EXACT COUNT was what proved it, and that
# is the gate shape: ASSERT THE EXPECTED COUNT OF AN EXPRESSION rather than trusting
# near-copies to stay in step. It can fail, so it earns its place. Ships with #131.
#
# THE MIGRATE SET, PROPERLY SIZED: five publish/persist sites (34592, 35734, 26233, 26228,
# and the csrdAutoFill trio 34759/34760/34761 as one unit) plus the writer and its four
# skuForm readers (20356, 20449/20450, 20662/20663, 20704/20705, 21283–21286). UNLIKE
# EVERYTHING SINCE THE SCOUT, THAT SHIPMENT IS NOT BEHAVIOUR-NEUTRAL BY CONSTRUCTION — its
# evidence must be a judgement about what SHOULD change, not a proof that nothing did.

# index.html moved 8fd343de → eb829955 on 9 Sep 2026 — TIME AXIS STEP 6, EXPAND PHASE 3b:
# the read-to-display sites. TWENTY-TWO SITES, NOT THE EIGHTEEN THE LANE LISTED.
#   after   eb8299557005ae16cfb2f3d44ac7dd390133c5a5f031fd02915bf9f047a1041f
# NAMED BY THE LANE after reading 7159/7164 at the bytes, INDEPENDENT shasum. Five other
# surfaces UNCHANGED; 1 of 6 modified. Copy gate still three surfaces, 2269 bytes — this
# shipment added no copy. normalizeCertifications now appears 24 times in the file.
#
# THE LANE'S ENUMERATION MISSED FOUR OF TWENTY-TWO — EIGHTEEN PERCENT — AND THE SPEC IS
# WHAT CAUGHT IT. Scope was defined BY SHAPE with an instruction to enumerate
# independently and report what the lane missed, rather than as a line list to work
# through. Missed: 7159 and 7164 (renderExpansionSequencer), 16898 (renderBrandPack),
# 36618 (buildBriefContext).
#   7159/7164 ARE THE HIGHEST-RISK OF THE FOUR: `.some(c => c.toLowerCase()…)` THROWS on
#   an object entry rather than rendering wrong, and they gate a CONTRADICTORY PAIR of
#   panels, so a throw takes out the whole market row. Had this been a line list, they
#   would have shipped in migrate against a writer that produces objects.
# STANDING: SCOPE BY SHAPE, NEVER BY LINE LIST.
#
# THE EVIDENCE: 13 distinct expression shapes × 11 inputs = 143 comparisons, three-way
# (old vs new vs a PRE-EDIT capture). 60 differences, EVERY ONE confined to an input that
# cannot occur today. On every REACHABLE input the output is byte-identical.
#
# THE ONE APPARENT REACHABLE DIFFERENCE, TRACED TO GROUND RATHER THAN EXPLAINED AWAY:
# site F returned undefined/null from the old ternary and returns '' now — but row() passes
# through bdVal, which maps undefined, null AND '' to the identical '— not provided'. A
# difference in the EXPRESSION that is not a difference in the OUTPUT, proven at the
# consumer. Its sibling at 30002 skips the row either way, same outcome by a different route.
#
# WHAT THE UNREACHABLE COLUMNS SHOW ABOUT TODAY'S CODE: the bare-string form THROWS at 11
# of 13 shapes — an exception, not a wrong value — and the object form renders
# [object Object] at 12. Both were live hazards the moment a second writer appeared.
# Expand closes them AHEAD of the writer, which is the phase's entire purpose.
#
# 34674 CHANGES BEHAVIOUR AND IS UNREACHABLE: an array of only unusable entries was `true`
# (length>0) and is now `false`. Reported as a difference, not a pass. When it becomes
# reachable it is a FIX — a readiness row reading "done" because the array had length while
# every entry was unnamed is #127's class, a boolean not backed by what it claims.
#
# 40129 — THE RISK RAN THE OPPOSITE WAY FROM THE LANE'S CONCERN. The spec asked for proof
# that normalisation does not REMOVE a buyer-facing badge (pack.claims is what the portal
# renders; #113 was a claims problem). Byte-identical on all reachable inputs — and on
# unreachable ones it REMOVES keys like "[object Object]" and "42" that would have rendered
# as literal badge text to a retailer. Normalisation PROTECTS that surface.
#
# NOTHING IN SCOPE REACHES STORAGE, PUBLICATION OR A HASH — each destination checked
# individually. The sites that DO publish or persist are excluded and named: 34592, 35734
# (DPP payloads), 26233 (shareBrandPack magic-link payload), 34756/34757 (csrdAutoFill,
# persisted to ns_csrd_answers and mixing brand?.certifications — 3c AND a persist site).
#
# 20356 CLASSIFIED → MIGRATE. It reads skuForm.certifications, not sku, inside
# generateSkuDesc's prompt. Same reasoning as the editor's includes tests: skuForm is
# written by toggleSkuCert, so it moves WITH the writer. THE MIGRATE SPEC NOW HAS FIVE
# SITES TO MOVE TOGETHER, not the writer alone — toggleSkuCert, the three editor includes
# tests (20449/20662/20704), and the description prompt.
#
# USEFUL ORDERING FACT FOR MIGRATE: 26233's payload feeds brand/index.html, which phase 1
# already made tolerant — so publishing the object form is safe on that surface BEFORE
# migrate touches it.
#
# #131 RAISED — THE SIX EXPORT LOCALES ARE IDENTICAL BY LUCK, NOT BY GATE. All six are
# ['<label>', normalizeCertifications(...).join('; ')] differing only in the label. ONE sed
# replaced all six with an exact-count assertion of 6, WHICH IS ONLY POSSIBLE BECAUSE NOBODY
# HAS HAND-EDITED ONE. Six near-copies with no check is the #128 shape, in the moment before
# it stops being true. Not fixed here.
#
# NOTICED: renderExpansionSequencer computes the same .some(…'cosmos') twice, once positive
# and once negated, so the reader is called four times per market row. Pure and cheap; a
# double negation that would read better as one local. 3c-adjacent tidy, not this shipment's.

# index.html moved 9eb3da69 → 8fd343de on 9 Sep 2026 — TIME AXIS STEP 5, EXPAND PHASE 3a:
# _ssCanon and the reader's third copy. ONE SITE, SHIPPED ALONE ON PURPOSE — 44 other
# certification sites in this file could have ridden along safely, and _ssCanon would then
# have been verified in a diff where nobody was looking only at it.
#   after   8fd343de9d80bf17dfb89639e00d9be85e6f6e87c82f443fcf55b6f3505fd34c
# NAMED BY THE LANE after reading 30013–30037 at the bytes, INDEPENDENT shasum.
# Five other surfaces UNCHANGED; 1 of 6 modified.
#
# WHY THIS LINE IS THE HIGHEST-CONSEQUENCE ONE IN THE MIGRATION: everywhere else a mistake
# shows as a visibly wrong string. Here it shows as EVERY PREVIOUSLY-ISSUED SPEC SHEET
# BECOMING SILENTLY UNREPRODUCIBLE — #93's one-time cost re-paid, with no symptom.
#   certs:(sku.certifications||[]).slice().sort()  →  certs: normalizeCertifications(...).sort()
#
# THE EVIDENCE: eight inputs, three-way compared (old vs new vs a PRE-EDIT capture), with
# the SHA-256 Document ID computed for each. SEVEN identical, ONE MOVED — a certification
# name carrying LEADING OR TRAILING WHITESPACE. The reader trims; .slice().sort() did not,
# and a padded entry sorted first because space precedes letters.
#
# REPORTED AS A DIFFERENCE, NOT FOLDED INTO THE PASS — and traced before concluding.
# sku.certifications has EXACTLY ONE WRITER: toggleSkuCert (21283), pushing from a
# hardcoded literal list with no padded element. No free-text input, no CSV import, no
# autofill, no parse path. The parsed./nlParsed. sites are manufacturer SEARCH FILTERS —
# third-party population, never assigned to a SKU. The moved case is UNREACHABLE, so every
# reachable Document ID is unchanged and the shipment stands.
#
# .slice() DROPPED, and the reasoning is the lane's own standard: the reader builds a fresh
# array per call and returns it directly, so .sort() cannot mutate caller state. Keeping the
# copy would assert a defence against an aliasing hazard the contract already forecloses —
# the "check that cannot fail" shape declined five times this week. The guarantee is WRITTEN
# in the reader's banner rather than implied by a redundant call.
#
# THE LANE'S SPEC WAS WRONG ABOUT THE FAILURE MODE. It said the latent bare-string crash sat
# OUTSIDE the try. It sits outside the INNER try around _atmHashContent but INSIDE the outer
# one at 29920, whose catch alerts "Could not generate spec sheet". So the failure was a
# VISIBLE REFUSAL, not a silent crash — and refusal-versus-silence is the distinction this
# lane keeps insisting on. Not reachable either way: the single writer forecloses it. The
# Array.isArray fallbacks at 29997/30002 are DEFENSIVE, NOT EVIDENTIAL — someone anticipated
# the form rather than observed it.
#
# THE COPY GATE SAID THREE, UNADJUSTED. Written to scan every surface carrying the banner
# rather than a hardcoded pair, so the third copy was picked up automatically. Extracted
# programmatically again — banner → opening → closing brace → scratch file → insert — so no
# transcription step existed in which an error could occur. 2269 bytes on all three.
# Placed at 4952 beside normalizeNumericProof, block one, ahead of the earliest read (7119).
#
# HASH COVERAGE ESTABLISHED, NOT ASSUMED: `certs` is the ONLY hashed field touching
# certifications (whole _ssCanon literal scanned for /cert/i — one hit). Five callers of
# _atmHashContent: two hash raw file bytes, and _lsCanon (29942, the LINE SHEET Document ID)
# hashes {n,d,rsp,ean,moq,lead} and does NOT touch certifications. No other document type
# carries this requirement; none is unscheduled.
#
# THE READER NORMALISES CONTENT, NOT ONLY SHAPE — and this belongs in the MIGRATE spec as a
# stated property rather than a rediscovery. Trimming is correct: a padded name and its
# trimmed form denote the same certification. But IF THE MIGRATE PHASE ADDS A FREE-TEXT
# CERTIFICATION WRITER, PADDED INPUT WILL HASH DIFFERENTLY THAN THE SAME NAME WOULD HAVE
# BEFORE TODAY. The banner's "normalises a shape, does not interpret one" is now slightly
# narrower than the behaviour; a clarifying line is folded in whenever the reader is next
# opened for another reason, not paid for with a three-copy shipment of its own.
#
# THE CHECK NOBODY COULD RUN FROM THE FILE: the live Cloud & Glow catalogue is in
# localStorage. CC's evidence covers the seed SKUs (neither declares certifications) plus
# eight synthetic shapes. SMOKE STEP 1 — an old spec sheet's footer Document ID against a
# freshly generated one for the same SKU — is the only test that closes this, and it is
# Charlotte's.

# dpp/index.html moved 6e946a27 → e4809761 on 9 Sep 2026 — TIME AXIS STEP 4, EXPAND
# PHASE 2, plus the COPY GATE. FIRST MOVE ON dpp/index.html SINCE BATCH #7. All four
# other surfaces UNCHANGED; 1 of 5 modified. 51 gates (was 50).
#   after   e480976126cb980ab8d248d651c9f60e3be27a33274d59494d8778e602a29714
# NAMED BY THE LANE after reading 172–177 and 275–279 at the bytes, INDEPENDENT shasum.
#
# THE COPY WAS NOT RETYPED. CC extracted the reader from brand/index.html
# PROGRAMMATICALLY — banner text → function opening → next closing brace — wrote it to a
# scratch file and inserted that file's contents. No transcription step existed, so no
# transcription error was possible. 2269 bytes both sides, identical digest.
#
# THE GATE WAS SEEN TO FAIL. It scans every HTML surface, keeps those carrying the banner,
# and compares extracted text — located by banner and closing brace, NEVER by line number
# (witness lines pinned to fixed numbers degrade after every edit, 3 Sep). A third copy in
# index.html at phase 3 is picked up automatically; no hardcoded pair. A surface carrying
# the banner without a complete function is its own FAIL, so a half-paste cannot pass.
# PROOF OF FAILURE: one space added inside the reader → FAIL, 2269 vs 2270 bytes; reverted;
# back to PASS. A gate never seen to fail is a gate nobody knows works — and this lane has
# now written two that could not fail.
#
# #129 CLOSED, AND IT IS A DIFFERENCE, NOT A PASS. The old hand-rolled reader at 279 —
# `esc(typeof c === 'string' ? c : (c.name || ''))` — rendered a NAMELESS entry as an EMPTY
# <span class="vf-cert"></span> while certs.length counted it, so the heading could say
# "Certifications (3)" above two visible badges. The count and the badges now read ONE
# normalised array and cannot disagree by construction. Byte-identical on 5 of 6 probes;
# the sixth DIFFERS deliberately and that difference is the fix.
#
# THE LANE'S PATTERN IS THE LANE'S, NOT ONE PERSON'S. CC reported the Certifications
# section as DEAD — that payload.certifications.certifications reads a nesting level the
# writer never creates — having read publishDPP (the client) and get-dpp.js (the read path)
# but NOT share-dpp.js, THE LAYER THAT BUILDS IT. share-dpp.js:27–35 reshapes the flat
# payload into sections via DPP_PUBLIC_FIELDS, where the section `certifications` contains
# a field `certifications`. THE NESTING IS DELIBERATE AND LINE 177 IS CORRECT. Same error
# the lane made five times and on #114a: verify the parts, assert the join — recorded here
# on the same day the log named it as the lane's own. #130 does not become that finding.
#
# AND THE CHECK SURFACED A LIVE MIGRATION CONSTRAINT: share-dpp.js:41 gates the section on
# `Array.isArray(p.certifications) && length > 0`. A LEGACY BARE COMMA-SEPARATED STRING
# FAILS Array.isArray AND THE ENTIRE SECTION IS SILENTLY DROPPED AT PUBLISH — no error, no
# partial render, an absent section. Whatever converts legacy rows must run BEFORE publish.
#
# #130 RAISED — share-dpp.js decides what a published passport CONTAINS (the section shape
# and the gating) and the harness has never compared it. Identical finding to #126 on a
# different file, and it stayed invisible to both lanes until it settled a false report.

# brand/index.html moved 1e99d32c → fc6c0586 on 9 Sep 2026 — TIME AXIS STEP 3, EXPAND
# PHASE 1. FIRST MOVE ON brand/index.html SINCE BATCH #5. index.html, dpp/index.html,
# portal.html and the proxy all UNCHANGED; 1 of 5 modified.
#   after   fc6c0586e209f6c67e48d8b770da3e9a2aafa1006647db13beab136d3937da73
# NAMED BY THE LANE after reading 223 and 272–286 at the bytes, INDEPENDENT shasum.
#
# EXPAND → MIGRATE → CONTRACT, ONE SURFACE PER SHIPMENT. Strategy REFUSED an exception
# to §2's one-edit rule for the certification migration, on the meta-rule written the
# same morning applied to Strategy's own side: an exception for "a coordinated migration"
# turns a structural guard into a procedural one — the label becomes available to anyone
# who believes their change is coordinated, and the guard survives only while someone
# remembers not to abuse it. AND THE SEQUENCING MAKES VERIFICATION STRONGER: the expand
# phase is behaviour-neutral, so every step is a DIFF TEST rather than a judgement. The
# sequencing is not a tax the migration pays; it is what makes it verifiable at all.
#
# THE BOUNDARY, A RULE AND NOT A SCOPE NOTE: a brand's own certifications and our
# reference data about third parties MUST NOT share a schema, now or ever. A brand's
# certification on a buyer-facing document is THE BRAND'S LEGAL EXPOSURE; our registry
# entry about a manufacturer is OUR EDITORIAL CLAIM carrying a confidence level. Share a
# schema and something eventually renders a research_verified third-party fact with
# brand-confirmed authority — invisible, because both would be well-formed.
#
# SEQUENCE CORRECTED BEFORE ANY EDIT: the scout proposed the proxy as expand step 1. The
# proxy is NOT IN THE MIGRATION — its single certifications (247) is inside a manufacturer
# LISTING APPLICATION, third-party data, which the scout's own population table classified
# correctly while its proposed sequence did not. Found by reading the line. Revised order:
# brand/index.html (1 site) → dpp/index.html (3) → index.html (45, split). portal.html has
# ZERO occurrences — it receives pack.claims, built FROM certifications, which is why #113
# was a claims problem and not a certifications one.
#
# THE MIGRATION IS FINISHING A HALF-DONE ONE, NOT STARTING ONE. brand.certifications is
# ALREADY the object form and already carries expiryDate; six readers already hand-roll
# `c.name || c`; buildComplianceEvents already raises calendar events from cert.expiryDate
# and renderComplianceCalendar gates a whole panel on `.some(c => c.expiryDate)`. None of
# it was ruled on or recorded. That is how a codebase acquires two schemas without anyone
# deciding to have two.
#
# THREE SHAPES, NOT TWO — the lane's spec was wrong. index.html:29957/29962 test
# Array.isArray and fall back to a bare comma-separated string, so that form exists in the
# wild. normalizeCertifications handles string[], string, and the object form.
#
# BEHAVIOUR-NEUTRALITY MEASURED, NOT ASSERTED: the old expression's output was captured to
# disk BEFORE the edit, then old and new were evaluated against six inputs and compared
# three ways — old vs new, and both vs the pre-edit capture. 6/6 byte-identical. The
# three-way form matters: a two-way comparison can be satisfied by an error common to both
# expressions. Escaping and the empty branch are both covered.
#
# esc() CANNOT RECEIVE A NON-STRING, proven by 11 probes including null, 42, {}, nested
# arrays and whitespace-only entries — all dropped rather than stringified. So
# [object Object] on a buyer-facing page is UNREACHABLE, and so is an empty
# <span class="vf-cert"></span>. Order preserved: [{name:"Z"},"A",{name:"M"}] → Z,A,M.
#
# ORDER PRESERVATION IS A CONTRACT SET HERE FOR A SURFACE THREE STEPS AWAY: _ssCanon
# (30028) does `certs:(sku.certifications||[]).slice().sort()` and hashes the result into
# the DOCUMENT ID. If normalisation reordered, every ID in the app would move — #93's
# one-time cost re-paid, silently. _ssCanon must go through the reader, and it ships with
# its own before/after ID evidence.
#
# RULED — THE TARGET FORM USES expiryDate, NOT validUntil. The lane's spec named
# validUntil; the field that EXISTS is expiryDate, already written and already consumed by
# two functions. Introducing a synonym would create two fields meaning one thing, inside
# the migration that exists to end exactly that. The name is uglier than the lane would
# choose; that is not a reason. A rename is a separate decision with its own migration.
#
# THE READER LIVES IN THREE COPIES and must stay BYTE-IDENTICAL — separate HTML files, no
# module system. #128 was decided the same afternoon on this hazard. The gate cannot exist
# with one copy; it lands with dpp/index.html in phase 2, comparing the reader's source
# text extracted BY COMMENT BANNER AND CLOSING BRACE, never by line number — witness lines
# pinned to fixed numbers degrade after every edit (3 Sep). That gate CAN fail, which is
# why it is worth building and #128's must-not-exist gate was correctly declined.
#
# PLACEMENT FOR PHASE 3, CC's reasoning better than the lane's: beside
# normalizeNumericProof at 4928 in BLOCK ONE — before the EARLIEST read (7119,
# renderExpansionSequencer), not near the riskiest site (_ssCanon at 30028, 22,000 lines
# later). The lane would have anchored on risk; the constraint is reach.
#
# INHERITED, NOT INTRODUCED: the reader's .split(',') cannot represent a legacy cert whose
# name contains a comma. That form was already lossy. It becomes a decision in the MIGRATE
# phase — whether such a name is recoverable or must be flagged for human review.

# index.html moved 0190c726 → 9eb3da69 on 7 Sep 2026 — TIME AXIS STEP 1, the
# confirmation stamp. +79 lines. CAPTURE ONLY: renders nothing, reasons about nothing.
#   after   9eb3da6917ba6ec516a846006f27d5579418edb730636a2d1fc5b9589c9c71e7
#           41,507 lines · 50 gates (was 40)
# NAMED BY THE LANE after reading 16393, 16484, 39359–39390 at the bytes, INDEPENDENT
# shasum. portal.html and supabase-proxy.js UNCHANGED.
#
# WHY THIS OUTRANKED EVERYTHING ELSE IN THE QUEUE (Strategy, 7 Sep):
#   "Nothing new is built before evidence" stands. But CAPTURE and RENDERING are not the
#   same thing, and the difference is ASYMMETRIC: deferring a feature costs waiting time,
#   deferring collection destroys the option permanently. Collection that cannot be
#   backfilled may precede evidence. Anything that renders or reasons about it may not.
#   The stamp passes. A "last confirmed" chip does not. A staleness flag definitely does
#   not.
# Every day without the stamp was a day of history that can never be reconstructed. The
# thesis is that the history accrues in real time and cannot be migrated in — nobody can
# build our customers' past, and until today we were not building it either.
#
# THREE WRITE SITES, ALL HUMAN CONFIRMATIONS: toggleRetailChecklistItem (39388),
# saveManualRp (16484), saveManualOperator (16393). saveSkus and saveBrandState stay OUT
# — bulk writes, not a discrete yes; stamping them would record "the brand pressed save"
# as though it meant "the brand confirmed this fact".
# CC's argument carried the RP pair into scope and it is the sharpest in the report: the
# app already knows when the RP agreement EXPIRES and has never known when the brand TOLD
# US IT EXISTED. The expiry is re-enterable from the document; the confirmation date is
# reconstructable from nothing.
#
# THE TRAP, AVOIDED BY CONSTRUCTION: retailChecklistState[key][reqId] is a bare bool read
# as truthy (`if(autoDone || state[r.id])`). Turning it into {done,at} would make EVERY
# object truthy — a brand's entire compliance checklist silently complete. The stamp
# therefore lives in a PARALLEL map the existing read path cannot see. autoDone is never
# stamped: it is derived, and stamping it would record that a person confirmed something
# nobody confirmed.
#
# THE LANE'S SPEC WAS SELF-CONTRADICTORY AND CC CAUGHT IT. The sketch said
# {reqId:"<ISO>"} while implement 3 demanded the untick be recorded — those cannot both
# hold, because a bare string cannot say which direction a toggle went and overwriting
# produces exactly the "survives only while ticked" record the spec rejected. The value
# is now an APPEND-ONLY array of {at, done}. Nothing is removed or overwritten.
#
# THE GATE — because "write-only" must be a CANNOT, not a MUST-NOT. Strategy's decision
# filter, applied to the spec itself: when a rule is phrased as "must not", ask whether it
# can be phrased as "cannot". Six assertions: 1 quoted occurrence per stamp key (its const
# only), 0 getItem-by-literal each, no stamp identifier in any innerHTML/html+= line, and
# _EVICTABLE_CACHE_PREFIXES does not match either key. Plus _appendStamp:3 in
# FIXED_CALLSITES — A FOURTH CONFIRMATION SURFACE FAILS THE BATTERY UNTIL STRATEGY RULES
# ON IT. This is the first shipment whose gate constrains FUTURE shipments rather than
# fixing a defect in this one.
#
# EVICTION, ANSWERED BEFORE EDITING: _evictCaches is ALLOWLIST-ONLY —
# _EVICTABLE_CACHE_PREFIXES = ['ns_comp_','ns_inci_','ns_pkg_','ns_srisk_'] and anything
# not matching is `continue`d. Neither stamp key is ever a candidate, and any new key is
# non-evictable BY DEFAULT. No exemption needed; the design already inverts the risk. CC
# gated the allowlist anyway, beyond spec, because it is one edit from being wrong.
#
# DRIFT AUTHORISED AFTER READING THE SITE: persistCritical 53 → 54, the new call inside
# _appendStamp (39375), required by implement 1. verify.baseline.json edited BY HAND for
# that one value rather than recaptured — a recapture would silently absorb any OTHER
# drift alongside the one verified, which is the VACUOUS-baseline problem already queued.
# Same discipline as resolveProductFramework 19→20 in #93.
#
# LIMITS STATED, NOT SMOOTHED OVER: the gate counts literals, so a computed key
# ('ns_retail'+'_checklist_stamps') defeats it. It stops the HONEST ACCIDENT — the person
# who wants a chip and adds one in good faith — which is the failure mode the rule was
# written for. And _appendStamp has NO CAP: a cap discards unbackfillable history, which
# is the one thing this shipment exists to prevent. Both are open decisions, not
# oversights, and both belong to Strategy alongside retention — triggered by #63, not by
# anything today.

# index.html moved 48fe2758 → 0190c726 on 7 Sep 2026 — #122 and #127, NUMBERS THAT
# ASSERT WHAT WAS NEVER MEASURED. Three items, one class; two found by using the app,
# one introduced by this lane in #118.
#   after   0190c7263883240c2cd23bd1c8ec05084070e2a09f35c0e66315df25f950e640
# NAMED BY THE LANE after reading 39265–39279 and 4432–4461 at the bytes, from an
# INDEPENDENT shasum. portal.html and supabase-proxy.js UNCHANGED.
#
# #127 — the Supplier Audit nav badge was a LITERAL `2`. It said 2 forever, whatever the
# data did, while the badge above it (savedReports.length) and the two below it (stale
# submissions, unseen portal updates) are all computed. A user reading that nav had no
# way to tell which numbers meant anything, which devalued the three that did. Deleted,
# not recomputed — inventing a computation to justify an existing badge is reasoning
# backwards from the decoration to the data.
#
# #122 — the Listing Checklist rendered NaN% and a RED bar for 156 OF 158 RETAILERS.
# LISTING_REQUIREMENTS holds two keys, so reqs is [] for everyone else and 0/0 = NaN
# landed in three places at once: visible "NaN%", red (NaN loses both comparisons), and
# width:NaN% as invalid CSS. THE FIX IS THE EMPTY STATE, NOT A GUARD — guarding would
# render "0%" and a red bar, a tidier version of the same lie, because 0% asserts nothing
# complete out of a real total and there is no total. This is #63 surfacing as a colour.
#
# AND IT PROVES #118 DID NOT TERMINATE. That shipment enumerated 16 percentage renders,
# declared the class removed, and this lane wrote that into this file and committed it.
# 39258 is a percentage render and was missed. The record was wrong; CC's report on WHY
# the scan missed it matters more than the fix, because whatever the reason is, other
# sites may have been missed the same way.
#
# TWO GUARDS OF OUR OWN REMOVED: 33499 and 34154, both added in #118, neither able to
# fire — ev.days comes from rows past addEvent's isNaN early return, and openDate is
# built with ||1 fallbacks. A guard that cannot fire asserts a check nobody performs.
# Fifth naming of that defect this week, twice now in the lane's own work.
#
# BLOCKED THE NEXT SHIPMENT, CORRECTLY: CC refused to start the time-axis stamp against
# an unverified working tree. Procedurally right (0190c726 was a sha CC reported itself)
# and mechanically right — #122's empty state sits immediately beside
# toggleRetailChecklistItem, so one diff would have made neither separately verifiable.

# index.html moved a1756c3b → 48fe2758 on 7 Sep 2026 — #114a, the portal poll sending
# a blank rejection reason. +18/−1. FOUND BY USING THE APP: the loop had recorded FOUR
# rejections across eleven days and ZERO reasons, telling the brand "No specific reason
# was given" every time — including the time she gave one.
#   after   48fe2758068e96918b9cb1becb98016aae2b990a1a6758cdca44b02ced7a2d76
#           41,428 lines
# NAMED BY THE LANE after reading 41375–41394 at the bytes, INDEPENDENT shasum.
#
# THE FIX: the poll's `reason: ''` was accompanied by a comment claiming the portal's
# details were "not pulled in this version". THE COMMENT WAS FALSE. The sync twenty
# lines earlier (40209–40213) already reads rejectionReason.category/.detail off the
# payload onto the local submission as rejectionCategory/rejectionNote — which is the
# very storage `post` is re-read from. The reason was in hand and discarded one
# function later. It is now resolved through OUTCOME_REASON_TAXONOMY, THE SAME TABLE
# THE CAPTURE FLOW USES, so a portal reason and a brand-entered reason read identically
# downstream instead of one being a code and the other a sentence.
#
# THE LANE'S SPEC HAD THE MECHANISM WRONG, AND THE WAY IT WAS WRONG IS THE RECORD:
# it reported "the portal poll ERASES the reason". THERE IS NO ERASURE. loop.upsert in
# supabase-proxy.js is INSERT-IF-ABSENT — it returns isNew:false and never writes — so
# it is FIRST-WRITER-WINS. The poll simply gets there first (every 60s, and on
# page-show) and the capture flow's later write is silently discarded. `context` is not
# even in loop.update's whitelist, so NO client path can write a reason into an existing
# row. The updated_at timestamps the lane read as "the poll rewriting rows" are moved by
# the loop_events_touch_updated_at DB TRIGGER, which fires only on real UPDATEs —
# drafted_content/accepted/dismissed/resolved.
#
# THE LANE ASSERTED A MECHANISM FROM FOUR TIMESTAMPS AND TWO CALL SITES WITHOUT READING
# THE LAYER THAT DECIDES WHAT A WRITE DOES. Fifth instance this week of the same move —
# verify the parts, assert the join (DPP filter · claimLights · #121's shared-array
# premise · #123's chain description · this). AND IT HAD TEETH: the spec would have
# shipped a merge guard in upsertLoopEvent protecting against an overwrite THAT CANNOT
# HAPPEN — a check that cannot fail, authored by the lane, in the fourth shipment where
# it named that defect in someone else's work.
#
# IMPLEMENTS 2 AND 3 WERE NOT WRITTEN, CORRECTLY: re-logging is already a no-op at the
# proxy and `source` cannot be relabelled, so a client-side dedupe or source guard would
# each have been another check that cannot fail.
#
# #126 RAISED — THE HARNESS CANNOT SEE THE FILE THAT DECIDES WHETHER A WRITE LANDS.
# supabase-proxy.js is not one of the four tracked surfaces. It holds loop.upsert's
# insert-if-absent semantics and loop.update's whitelist; it settled this finding, and
# verify.sh has never compared it. Baseline added below.
#
# #114c — THE REMAINING HALF, needs the proxy: a portal rejection with NO structured
# reason still creates a blank row, and a brand who later types the real reason loses
# it, because context is not updatable. Fix is loop.upsert merging non-empty incoming
# context fields over empty stored ones, or loop.update whitelisting context.
#
# OPEN, NOT ASSUMED BENIGN: row 4 (27 Aug rejection) has updated_at of 7 Sep 12:02:45.
# The lane's #110 item 4 UPDATE was 4 Sep, and ensureEventDraft early-returns because
# drafted_content is 'not_recorded' (truthy). Something wrote that row today and we do
# not know what.

# index.html moved e1f68ca1 → a1756c3b on 7 Sep 2026 — #120, ABSENCE RENDERED AS A
# VERDICT. +46 lines. Raised by CC under #118's "noticed, not fixed", and it outranks
# #118: NaN is VISIBLY broken so a user distrusts it; LOW, red and "· today" are
# PLAUSIBLE so a user believes them. Same manufactured finding as the Lyko rejection
# inventing causes into an empty reason field — except arithmetic did it rather than a
# model, so no prompt sweep would ever have caught it.
#   after   a1756c3bc195fc7d7cde7488948e57e4ff1e8fa19dc20bf78c6de1026c2c5d70
#           41,411 lines
# NAMED BY THE LANE after reading 35856–35885 at the bytes, INDEPENDENT shasum.
# portal.html UNCHANGED at beaa72b8.
#
# THE RULE, recorded above fieldAge where a reader arrives at the class:
#   AN ABSENCE MAY NEVER BE RENDERED AS A JUDGMENT — not as a colour, not as a
#   confidence level, not as a relative time, not as a position in a ranked list.
#
# THE DEEPEST DEFECT WAS NOT A NaN BUG. fieldConfidence had `if(days===null) return
# 'LOW'` — deliberate, on its own line. "We have no date" and "this data is over 180
# days old" produced the SAME RED VERDICT. Three states where there must be four; the
# missing one is not_recorded, which this app already knows how to express (checkState,
# the portal compliance legend, and #110 item 4 writing the literal string into the DB
# rather than blanking a field). NOT_RECORDED now returns var(--muted) — the file's
# established neutral, 752 uses. NO fourth colour invented.
#
# ALSO: getTemplateStaleness's unreadable date fell through to layer1='red'; it now
# leaves layer1 at its own existing 'unverified'. The magic-link surface printed
# "· today" for a date it could not read — a plain false statement — and now uses
# 'Generation date not recorded', wording its own sibling branch already had. In all
# three cases the vocabulary for absence WAS PRESENT and simply was not reached.
#
# SORT RULING APPLIED: daysUntil returning null made undated rows sort FIRST in a
# soonest-first list — urgency asserted from an absence, this finding's own shape
# introduced by its predecessor's fix. Undated now sorts LAST, both comparators.
#
# THE SIBLING COUNT: 22 candidates, 2 false positives, 3 fixed here, ~11 provably
# finite, 6 LIVE. THE PATTERN IS THE FINDING, more than the six sites are:
#   THE FINAL `else` OF A COMPARISON CHAIN IS WHERE ABSENCE LANDS, and whichever
#   verdict sits there gets asserted — sometimes the worst, sometimes the best, never
#   "unknown".
#
# 33293 SHIPS NEXT, and it is HALF-FIXED BY THIS SHIPMENT — which is worse than
# untouched. Its `days` field was made null-safe for SORTING here, so the row now
# sorts correctly and still colours itself GREEN from an absence. Green is the INVERSE
# hazard and the worse one: red reads as a problem to check, green reads as checked
# and fine. The sort now implies the colour was considered.
#
# OBSERVABILITY, STATED HONESTLY RATHER THAN MARKED PASSED: fieldConfidence and
# CONFIDENCE_COLOR have ZERO callers — 1 occurrence each, their own definitions. Smoke
# steps 1 and 2 are unrunnable today. KEPT ANYWAY, and that is the ruling: whoever
# wires them later inherits four states instead of writing three. Dead code encoding a
# truth rule is a liability only if it is wrong.
#
# THE ONE OBSERVABLE CHANGE: fieldAge's single live caller is manufacturer scoring,
# where an UNREADABLE date awarded score+=3 of 40 freshness points while an ABSENT date
# scored 0. Garbage scored higher than nothing. Both are 0 now.
#
# FOR DESIGN: CONFIDENCE_COLOR now mixes raw hex with one var() value. Resolving that
# means hex-ing the neutral or tokenising the other three — a design call.
#
# #119 STILL OPEN — percentages on user-supplied money (3961, 6758, 7061, 15212,
# 15266, 15860 at e1f68ca1). The em dash may be WRONG there: 0% is a real answer where
# a missing RSP is not.

# index.html moved e2b148d4 → e1f68ca1 on 7 Sep 2026 — #118, NaN reaching the DOM.
# +34 lines. A CLASS SWEEP, not an instance fix: the reported NaN was never
# reproduced (no console errors; a DOM query for leaf nodes containing NaN returned
# nothing minutes later). The class was removed instead, so the defect is gone
# whether or not the site that produced it is ever identified. Same method that
# terminated the regulatory-vocabulary whack-a-mole on 4 Sep.
#   after   e1f68ca17b898bac9bc90554b70b4b0435d507b71d955cd8a6b0d2250e47bb52
#           41,365 lines
# NAMED BY THE LANE after reading 4859–4879 at the bytes, from an INDEPENDENT shasum.
# portal.html UNCHANGED at beaa72b8.
#
# THE COUNT, WHICH IS THE FINDING: 19 date-arithmetic sites enumerated from source.
#   11  accidentally safe — NaN loses every comparison and is falsy, so `<`/`>`/`||`
#       and truthiness excluded it. NOT ONE because anyone checked.
#    1  already correct (isNaN guard)
#    1  partially guarded (absent date caught, unparseable not)
#    6  LIVE DEFECTS — NaN reached a rendered string:
#       daysUntil 4853→4868 `NaNd` · getTrialDaysLeft 4639→4685 `NaN days remaining`
#       renderBrandHomeAlert 31346→31357 · calendar strip 33373 · event label 33432
#       · retailer window 34081
# 11 of 18 behaving by accident IS the condition under which the other 6 survive for
# months. That ratio is why the class had to be swept rather than the instance chased.
#
# THE CATCH THE SPEC'S OWN RULE WOULD HAVE MISSED, and only running it found:
# new Date(null) is a VALID date — the epoch. So null passed Number.isFinite, Math.max
# floored the huge negative, and an ABSENT deadline rendered "0d": a stated zero days
# for a deadline nobody recorded. The spec said "never render 0, zero is a measurement"
# and the guard the spec specified would have rendered exactly that. daysUntil now
# tests null/undefined/'' explicitly BEFORE parsing.
#
# FIXED AT THE FUNCTION, not at call sites — one definition serving many callers is
# where a guard belongs; patching callers is the narrow-anchor error. Returns null
# rather than NaN so a caller cannot silently arithmetic on it. fmtNum(n,suffix) added
# for the six render decisions: FORMATTING ONLY, so it cannot become a second date
# implementation beside daysUntil and daysSinceIso.
#
# THE REFERENCE IMPLEMENTATION WAS ALREADY IN THE FILE: daysSinceIso (37724) does
# `if(isNaN(t)) return null`. Six functions away from the ones that lacked it.
# portal.html:587 does it correctly too. dpp/index.html and brand/index.html do not
# contain the class at all — no date arithmetic, nothing to guard. No edit warranted
# on any of the three; none touched.
#
# GUARDS DELIBERATELY NOT ADDED, because a check that cannot fail is the defect this
# lane exists to prevent: Math.round(r.marginMin*100) at 12784/12815/36459 reads
# RETAILER_REGISTRY — 158 entries, 0 missing marginMin, 0 missing marginMax, parsed
# to establish it rather than assumed. 14891 divides by modules.length inside a loop
# over modules. 23889 is literal prose. Five sites already guarded by ||/?/typeof —
# adding Number.isFinite beside them would be a second scheme for one decision.
#
# ── CORRECTION, APPENDED 7 Sep 2026 (#122). THIS ENTRY OVERSTATED ITS COVERAGE. ──
# The claim above that the NaN-to-DOM class "was removed" is WRONG. It was not.
# renderRetailChecklist rendered `NaN%`, a RED bar and `width:NaN%` for 156 of 158
# retailers — LISTING_REQUIREMENTS holds two keys, so reqs is [] and 0/0 is NaN — and
# #118's sweep walked straight past it.
# WHY THE SWEEP MISSED IT, which is the useful part: the scan was LINE-SCOPED. It
# required Math.round(...), a division, and a '%' ON THE SAME LINE. At 39285 the
# computation is `var pct = Math.round((doneCount/reqs.length)*100);` and the '%' render
# is four lines later. Every site that computes a percentage into a variable and renders
# it elsewhere was invisible to it.
# RE-RUN CROSS-LINE, the corrected scan finds 12 such sites. ELEVEN ARE SAFE and were
# checked rather than assumed: NORWAY_CUSTOMS_CHECKLIST.length and CSRD_QUESTIONS.length
# are constant arrays; 31948 guards with `total ?`; 14654 with `rsp>0&&netPrice>0`;
# 21245 returns early on `if(!rsp||!net)`; 39535 hard-blocks on `delivered <= 0`;
# 40385 needs two months to run. ONE was the defect, and it is fixed in this shipment.
# THE LESSON IS ABOUT SWEEPS, NOT ABOUT NaN: a line-scoped regex cannot see a class
# whose two halves sit on different lines, and reporting such a sweep as terminating the
# class is the same defect as a badge that overstates its own measurement.
#
# #119 RAISED — computed percentages on USER-SUPPLIED MONEY: 3961, 6758, 7061, 15212,
# 15266, 15860. Different input class from unreadable dates, and the em dash may be
# WRONG for some: a margin of 0% is a real answer where a missing RSP is not. Per-site
# judgement, so a shipment rather than a sweep. Correctly held back.
#
# #120 RAISED, AND IT MATTERS MORE THAN THE NaN DID — ABSENCE RENDERED AS A VERDICT:
# 13403 renders an unreadable date as RED staleness; fieldConfidence renders it as
# LOW; 26057 renders it as "· today". Same root, worse output. NaN is VISIBLY broken
# so a user distrusts it; "red", "LOW" and "today" are PLAUSIBLE so a user believes
# them. The dangerous output is not the one that looks wrong. Both fieldAge and
# fieldConfidence have an intended null contract that NaN slips past.
#
# RULED WITH #120: daysUntil's sort comparators (4862, 33337) now subtract nulls,
# which coerce to 0, putting UNDATED rows FIRST in a soonest-first list — urgency
# asserted from an absence. Undated must sort LAST.

# portal.html moved b96423ac → beaa72b8 on 7 Sep 2026 — #113, brand claims rendered
# to the buyer in the success palette. FIRST MOVE ON portal.html SINCE BATCH #5.
#   after   beaa72b85e853dab6cae4d29413fc15e131999e3ee8a2b71bd20e0beb13cd237
# NAMED BY THE LANE after reading 1044–1071 at the bytes, from an INDEPENDENT
# shasum. index.html UNCHANGED at e2b148d4 — this shipment touches one surface.
#
# THE DEFECT: (s.claims||[]) rendered as .detail-badge-green — #ECFDF5/#065F46, the
# same success palette as the ✓ in the compliance table twenty lines below. Six
# brand-DECLARED items read to a retail buyer as findings. The page then
# contradicted itself: six green badges above, CLAIMS "–" (not recorded) below,
# same brand, same submission, same screen. Found by USING the portal, not reading it.
#
# THE FIX IS A SUBTRACTION: base .detail-badge only, coloured from existing tokens
# (--cream/--navy/--border), plus a legend matching the compliance table's own
# discipline at 1209 — "Declared by the brand · not verified by VeyaFlow. Per-product
# status is in the compliance table below." NO state treatment invented: green/amber/
# red badges are DESIGN's and would need the payload fixed first. .detail-badge-green
# now has 0 live uses; the CSS rule at 257 was LEFT IN PLACE — removing a token is a
# design decision, and it may be wanted once state actually reaches this surface.
#
# THE LANE'S SPEC PRESCRIBED THE WRONG REMEDY, AND CC'S REPORT IS WHAT CAUGHT IT.
# The spec asserted "the state exists and the surface throws it away", on the strength
# of seeing claimLights computed in buildSubmitSnapshot. The connection was never
# checked. pack.claims is built from sk.certifications + sk.claimsApproved;
# claimLights is computed over sku0.claims — DIFFERENT ARRAYS. Plumbing the payload
# would have attached lights to items not on the badge row, and string-matching them
# would be a guess. Same error as the retracted ESPR_TIMELINE/DPP-filter claim:
# asserting a CONNECTION between two things each separately observed. Caught before
# shipping only because the author reported against the spec instead of executing it.
# claimLights occurrences in portal.html: 0. In supabase-proxy.js: 0.
#
# #116 — ★ VeyaFlow Verified IS DEAD, AND THE DEAD STATE IS WHAT PROTECTS US.
# Line 1048 reads s.verified/s.verifiedTier, sourced from brand.verifiedTier
# (index.html 39888–39889, two reads, never assigned, never migrated). The real
# machinery is calcVerificationTier() at 7220, reading brand?.verificationTier — a
# DIFFERENT PROPERTY NAME. So the badge never renders. REPAIRING THAT NAME WOULD SHIP
# "★ VeyaFlow Verified · Tier 2" IN GOLD TO A BUYER, on a value the brand typed into
# an admin prompt() at 7529 — the function's own comment says "Tier 2+ is manually
# granted". Tier 1 is computed and rigorous; tiers 2 and 3 are not computed at all.
# NOT TOUCHED. This is a landmine for exactly the kind of tidy-up commit that looks
# like housekeeping. What our own verification mark means is STRATEGY'S CALL.
#
# #115 — getClaimLight resolves the framework from GLOBAL EDITING STATE:
# resolveProductFramework(skuForm) while the claim comes from sku0. At submit time it
# can resolve against a different product than the claim belongs to. claimLights is
# therefore unreliable EVEN ONCE PLUMBED, and must be fixed before anything depends
# on it.
#
# #117 — pack.claims is .slice(0,8). A brand with nine certifications silently loses
# the rest, with no indication to the brand or the buyer.
#
# FAIL-CLOSED AND CORRECT, recorded as the counter-example: getClaimLight returns
# {light:'amber', note:'Unknown claim — verify per target market before use'} for an
# unrecognised claim, and amber for EVERY claim when the framework is 'unknown'
# rather than falling back to cosmetics rules. Both right. Both discarded before
# reaching the buyer.
#
# SMOKE STEP 3 IN THE SPEC WAS WRONG and CC said so: it expected "a submission whose
# claims are all genuinely satisfied renders exactly as before". No claim on this
# surface is known-satisfied — the portal has no lights — so every badge changes.
# Replacement invariant: a submission with ZERO claims renders no badge row and no
# legend.
#
# UNREVIEWED BY DESIGN, flagged not hidden: the legend line uses a -.65rem top margin
# to sit under .detail-badges' own 1rem bottom margin.

# index.html moved 4a3a2e98 → e2b148d4 on 7 Sep 2026 — #112, the add-submission
# button that opens a form below the fold. +24 lines. FIRST FINDING FOUND BY USING
# THE APP rather than by reading it.
#   after   e2b148d4463922c0bcd669d8af04e198bd8a89ab3886ba8d84734ac49c88f09a
#           41,331 lines · 2,724,663 bytes · 2 blocks
# NAMED BY THE LANE after reading both call sites at the bytes, from an INDEPENDENT
# shasum on Charlotte's machine.
#
# THE DEFECT: openAddRetailSubmission set a flag and called renderPage. The form's
# markup is emitted after the pipeline summary, the aggregated rejection view,
# Pattern Intelligence and the Brand Outcome Dashboard, so it opened several
# hundred pixels below the fold and the viewport never moved. The button worked and
# looked broken — the user's next action is to click it again.
#
# NOT A TRUTH DEFECT. Recorded here under the SIBLING rule: the pattern occurs
# twice (openAddRetailSubmission 38279, openAddRetailComms 39703 at the old
# baseline) and fixing only the reported instance is the narrow-anchor error that
# has bitten this lane nine times. The comms form is LATENT rather than visibly
# broken — it sits high on its page today — and was fixed on identical terms
# anyway, using the same idiom so the two cannot drift apart.
#
# MECHANISM MATCHED, NOT INVENTED: id + scrollIntoView deferred 80ms, which is
# already the file's idiom at 7109 (readiness panel) and 31200 (retailer picker).
# block:'start' not 'center' — a form is filled top-down. Scroll-on-open is
# STRUCTURAL: the call lives in the open functions, not the renderers, so ordinary
# navigation cannot trigger it.
#
# A FALSE CLAIM IN AN OTHERWISE EXEMPLARY REPORT, recorded because of its shape.
# CC wrote that the spec guessed the comms heading as `Log communication` and that
# the tree says `Log new entry` — "a small drift between spec and bytes, no
# consequence". The spec contains no heading guess: zero occurrences, grepped. CC
# described a discrepancy between a document and the code by inventing the
# document's half. Consequence nil; the shape is a confident checkable claim,
# wrong, phrased in the reassuring direction. CC's reports are the channel this
# lane verifies through, so a false statement about a spec is the same class as a
# false digest.
#
# OPEN, RULED INTO A SECOND SHIPMENT (both need ids that do not exist yet):
#   FOCUS. .focus() scrolls as a side effect using instant positioning, which cuts
#     across an in-flight smooth scroll — a race, not a design. The file already
#     resolves this at retailScrollToManualField (37704): scroll, then focus at
#     400ms against a ~300ms smooth scroll. focus({preventScroll:true}) is better
#     if it holds against target browsers — a check, not a guess. Needs first-field
#     ids on both forms.
#   CLOSE — THE INVERSE DEFECT. Browsers preserve scroll OFFSET, not scroll ANCHOR.
#     Closing a several-hundred-pixel form shifts everything below it up into those
#     pixels, so the user is left looking at Pattern Intelligence having closed a
#     form. Symmetric fix: scroll back to the button that opened it. Needs an id on
#     the + NEW SUBMISSION button.
#
# ESCALATED TO DESIGN, AND THE FIX IS SUPERSEDABLE: scrolling compensates at
# runtime for a layout decision, on two pages, in both directions. Rendering the
# form adjacent to its button, or as an overlay, removes both compensations at
# once. Shipped anyway because the defect is live and users hit it now. If DESIGN
# rules for adjacency or an overlay, this is SUPERSEDED rather than extended — the
# ids stay useful, the scroll calls come out.
#
# NOTICED: the two form containers were BYTE-IDENTICAL before this change — same
# opening <div style="…border:1px solid var(--gold)…">, distinguishable only by a
# heading three lines later. That is how a future edit lands on the wrong form.
# The ids incidentally fix it.

# index.html moved 38dd86a5 → 4a3a2e98 on 4 Sep 2026 — #110 item 5 first half,
# ITEM A: the reorder builder's invented input. +17 lines.
#   after   4a3a2e982ebc75ba3104de8aac697e6a217be5b61d96b2adb1beee21801c0a0b
#           41,307 lines · 2,722,881 bytes · 2 blocks
# NAMED BY THE LANE after reading 40289–40328 at the bytes and confirming the
# `||'a similar market'` fallback survives only as comment text, from an
# INDEPENDENT shasum on Charlotte's machine.
#
# THE DEFECT: when ctx.nextMarket was empty, the prompt printed `Next market to
# consider: (not set)` in the data block while instructing the model to suggest
# pitching "a similar market" — one it had to choose from nothing. The rejection
# builder's defect relocated. A fallback that invents its own input is a second
# scheme, the same shape as ||'Denmark' (3 Sep) and the #93 typeof guard.
#
# THE FIX IS STRUCTURAL, NOT INSTRUCTED: `_nextMarket` (40305) trims before
# testing, and a ternary at 40308 selects one of two whole prompts. Paragraph 2,
# its fallback, the format line, the sentence budget AND the data line all move
# together. The empty branch cannot emit a market because no instruction and no
# field mentions one — not because it is told not to.
#
# TWO THINGS CC DID BEYOND THE RULING, BOTH RIGHT, BOTH RULED IN AFTER THE FACT:
# 1. Removed the data line. The lane's spec named the paragraph and the fallback
#    and left `(not set)` visible. A field printed with no instruction referring
#    to it is a dangling invitation, and dangling context is what got filled last
#    time. The lane under-specified; CC caught it.
# 2. Added a prohibition to the empty branch's format line rather than deleting
#    only. Mirrors item 1, where the replacement beat the bare deletion. It is
#    grounded, not asserted: "none is on file" is true by construction in the
#    branch it appears in.
#
# TRIM WAS NOT IN THE RULING EITHER: '   ' now counts as unset. The lane's ruling
# would have left whitespace passing the guard.
#
# DELIBERATELY KEPT: 'while this proof is fresh' (40311). The reorder has just
# happened, so the proof is fresh — the clause is grounded, not a decay claim.
#
# OPEN, FROM CC'S ITEM B REPORT, RULED INTO THE NEXT SHIPMENT:
#   B1 `Output 1-2 sentences (max 35 words)` (40336) spans both branches of the
#      rejection builder. A 1–2 sentence budget invites padding "no reason was
#      given" into something longer, and padding a null result is where invention
#      re-enters. Moves with the branch, as item A's budget did.
#   B2 `or similar retailers` (40336) generalises a stated reason from one
#      retailer to a class — an inference the record does not contain. THIS IS
#      #110's SHAPE ON THE POPULATED PATH, which is new: every prior instance was
#      generation into an EMPTY field. The class is wider than we had it.
#
# LATENT, RECORDED, NOT SHIPPED: loopEventAccept (40811) would show "Could not
# draft content — try again" for a sell_through_low event — a transient-failure
# message for a trigger that will never produce a draft. Unreachable today: that
# card renders only loopEventResolve, verified at the bytes. #89/Circular-GPS
# precedent — any future Accept button on this card is gated on fixing it first.
#
# STILL UNSCANNED BY BOTH LANES: the render half, 40700–41040.

# index.html moved f66c76fd → 38dd86a5 on 4 Sep 2026 — #110 ITEM 3, the
# sell_through_low rebuild, AND THE TRUTH DEFECT ITEM 3 CREATED IN ITS OWN SHIPMENT.
#   after   38dd86a54c0bb01cae6b5fd31caef13c20730e2a9def6debac4b18e4de9bf56e
#           41,290 lines · 2,721,537 bytes · 2 blocks
# NAMED BY THE LANE after reading the edits at the bytes, and from an INDEPENDENT
# `shasum -a 256` on Charlotte's machine — not from verify.sh's own digest line,
# which is the same channel as the GREEN it is being used to confirm.
#
# 8ffeecbb IS NOT A BASELINE AND MUST NEVER BE TREATED AS ONE. It is in the reflog.
# It is the tree where FEEDBACK_LOOP_PROMPTS.sell_through_low was gone and
# loopEventResolve still asked the brand to confirm "you've reviewed the diagnosis
# and either acted on the recommendations" — a window.confirm, so the app asked them
# to ASSERT it. The gap between f66c76fd and 38dd86a5 is deliberate, not a skipped
# entry. Two edits, one shipment, one named value.
#
# ITEM 3 (40272, 40744, 41017): the builder is GONE, NOT TRIMMED — nothing survives
# the deletion, because five candidate causes are indistinguishable from a single
# percentage. The five buckets return as STATIC content carrying their own
# disclaimer: "They are the same five for every product — the figures above cannot
# tell you which one applies here, so this is a checklist, not a finding." That
# sentence states the ABSENCE OF THE CONNECTION, which is where Strategy located the
# lie: the same five sentences are honest as a help panel and dishonest as a
# generated diagnosis.
#
# THE CONFIRM DIALOG (40853–40860): the middle sentence is DELETED, not reworded,
# and a comment records why the gap is deliberate so a later reader does not fill
# it. A rewrite would have invented a new description of what resolving means —
# the same error one level up from the one this batch removed.
#
# HOW THE DEFECT WAS CAUGHT, AND WHY NO GATE COULD HAVE CAUGHT IT: verify.js has no
# way to know that a confirm dialog's copy depends on a prompt builder 500 lines away
# in a different function. It was caught because CC reported its own collateral
# instead of reporting the item complete. That is author-≠-verifier working in the
# direction it normally fails. Set against #95 — a visibly broken PDF that passed
# every gate we had — the two data points agree: the byte battery catches drift and
# regression, and has never once caught a truth defect. It is not what protects us
# here. Do not let a GREEN be read as one.

# index.html moved d186db18 → f66c76fd on 4 Sep 2026 — #110 items 1 and 2, the
# events-surface fabrication instructions. +6 lines, ALL IN BLOCK 2 (1,515 → 1,521
# content lines), which is where the events surface lives; block 1 untouched.
#   after   f66c76fd2f4c70094a3096c4e60e8865214ec7b29bbb7e4c62960edab8ef03fb
#           41,293 lines · 2,721,114 bytes · 2 blocks (1176, 39769)
#
# ITEM 1, rejection builder (40334): the clause "suggest the most likely
# category-level cause" is replaced — not merely deleted — with an explicit
# prohibition: "say so plainly and stop. Do NOT suggest, infer or speculate about a
# cause the retailer did not state." The empty field now reads "(not provided by
# retailer)" in the prompt rather than blank. Better than the spec asked for.
# ITEM 2, reorder builder (40306): "Mention specifically why the timing matters"
# removed; the model holds no data about the next market.
#
# HOW THIS BASELINE WAS ESTABLISHED, because the process mattered more than the
# value: CC STOPPED rather than proceed on its own sha, correctly invoking the
# 31 Aug rule against itself — "including when I am confident, and especially
# then." Items 1 and 2 had been applied a turn earlier and I had never byte-verified
# them or named a digest. The lane verified both edits at the bytes, and the value
# above comes from an INDEPENDENT `shasum -a 256` on Charlotte's machine, not from
# CC's report and not from the battery's own line.

# index.html moved 7ebd1ac4 → d186db18 on 4 Sep 2026 — the completeness gate gets
# its own refusal panel instead of borrowing the API-failure channel. +56 lines.
#   after   d186db186fcc4d99538654c6969e4f3807d39bdaf68b8db982ce1281a66edd65
#           41,287 lines · 2,720,447 bytes · 2 blocks (1176, 39769)
#
# WHY: #105 fired on the FIRST live pack after shipment 4 — a 200 came back with
# 2.8 kB after 19.73s and the response was cut short. The gate correctly refused a
# severed document. But the message it had carefully written was passed to
# classifyApiError, which matched nothing and substituted "An unexpected error
# occurred… contact support." BOTH HALVES OF THAT WERE FALSE: it was entirely
# expected, and there is nothing to support. The gate now sets `INCOMPLETE:` and
# both surfaces render their own panel; the `ERROR:` path still routes real API
# failures through classifyApiError.
#
# AND A TRUTH DEFECT IN THE FIX'S OWN COPY, caught before it shipped: the new panel
# read "generating again USUALLY produces a complete pack" — an unmeasured frequency
# claim, on a panel that exists because the app stopped asserting what it cannot
# evidence, in a shipment whose own report says nothing counts how often the gate
# fires. Replaced on both surfaces with a statement of mechanism: "The text was cut
# off as it was written, not by anything in your data. Generating again produces a
# different draft."
#
# OWED, NOT DELIVERED: nothing measures how often the gate fires. A brand can loop
# on Generate again with no signal reaching us that the ceiling work is urgent.
# Belongs with the 4b ruling on ceiling-versus-section-budget.

# index.html moved ef786f2a → 7ebd1ac4 on 4 Sep 2026, batch #9 shipment 4 ITEM 6:
# SECTION 5 removed from the Brand Pack prompt, sections renumbered 1–6, the
# COMPLIANCE DATA label and ${complianceData} injection removed, and SECTION 2's
# two regulatory instruction lines plus the Certs: fragment deleted. −23 lines.
# NAMED BY THE LANE after reading every change at the bytes.
#   after   7ebd1ac4a79a1f013cef5369e174b8df2bf255f969146af7a900c430c09c3aea
#           41,231 lines · 2,716,813 bytes · 2 blocks (1176, 39713)
#
# HOW COMPLETION WAS ESTABLISHED, and this is the part worth copying:
# three successive passes each removed "the last site asking for regulatory
# content" and each was wrong. The fourth read _REGULATORY_STATUS_VOCAB OUT OF
# THE SOURCE and ran all 19 patterns across all 78 prompt lines. One hit — the
# COMPLIANCE HONESTY prohibition at 17122, which stays because the gate scans
# OUTPUT, never the prompt. Zero instruction lines. A terminating check, not
# another patch.
#
# CAVEAT KEPT ON THE RECORD: 17122 opens "state each compliance item's EXACT
# status only", which read alone is an instruction. It is inert because no
# compliance data is injected any more. If a pack is ever refused with no other
# candidate, that clause is where to look.
#
# #103 WENT 10 → 9, NOT CLOSED: DPP_CANONICAL_ORIGIN now has 9 RAW read sites,
# including the DPP PDF's own section('Digital Product Passport'). One occurrence
# of the class removed. The line got no new home while the domain is parked.
#
# §3 CONTRACT CHANGED: _dppIsPublished is 1 definition / 3 call sites, not 4.
# The fourth gated the removed passport line. verify.js FIXED_CALLSITES updated
# from 4 to 3 — a hard-coded contract, so no recapture would have cleared it.
# The handoff §3 table still says 4 and is stale.

# index.html moved be86c431 → ef786f2a on 4 Sep 2026, batch #9 shipment 4 —
# THE TRUTH GATES. Five items: the regulatory-prose gate (#106), #107's counter,
# #105's shared completeness check across all three multi-section generators,
# #93's _ssCanon field list, and #56 as a GATE rather than a warning.
# NAMED BY THE LANE after reading the load-bearing edits at the bytes and
# confirming the shift pattern: nothing above 14894 moved, then +12 by 16327,
# +149 by 17380, +154 by 35425 — monotonic, matching five insertion points.
#   after   ef786f2a2987ee22582b82f5a9c528b822cd7067fea18011fc1d0f64122c69c2
#           41,254 lines · 2,718,898 bytes · 2 blocks (1176, 39736)
#
# TWO THINGS THIS SHIPMENT ESTABLISHES, both permanent from here:
# 1. Document IDs now cover the regulatory section AND the resolved framework.
#    Every ID the app produces changed once, deliberately, while nothing had
#    been issued. The serum moved 5836261e14ab → 3af3865024a8. Removing the
#    typeof guard moved NOTHING (the true branch and the direct call hash
#    identically) — what it removed was a second scheme: on a legacy SKU the
#    branches diverge, garment→textile giving 7b4c57fa949f against the raw
#    fallback's ef47bf94286f. Two IDs for one product, decided by a hoisting
#    accident.
# 2. resolveProductFramework re-baselined 19 → 20 call sites. The new call is
#    the one _ssCanon needs and the only one; verified at 29934 before the
#    recapture was authorised.
#
# CAVEAT ON THAT GREEN, so it is not read as more than it is: verify.baseline.json
# was recaptured from this tree, so the BASELINE DRIFT section compared the tree
# against a snapshot of itself and could not fail. The digest, block-count, parse
# and cross-file checks are unaffected and carry the weight. Queued: verify.js
# should record the digest a baseline was captured from and mark that section
# VACUOUS when it matches.

# index.html moved 3ff93df4 → be86c431 on 3 Sep 2026, batch #9 third shipment
# (target market field: placeholder option, no retailers until a market is chosen,
# a market guard before the retailer guard, and the ||'Denmark' prompt fallback
# removed). NAMED BY THE LANE after reading all four sites at the bytes and
# confirming a uniform +17 shift below line 16674 with nothing above it moved.
#   after   be86c431c632557aeb50eaa546b75a1a637ab8fc6a79d5a8788b9c6fccd39c2b
#           41,100 lines · 2,708,821 bytes · 2 blocks (1176, 39582)
#
# STILL LIVE ELSEWHERE, unfixed: generateBizCasePDF carries the same defects —
# bizCaseState.market||'Denmark' (15814, the prompt) and ||'Market' (30531, the
# PDF), with no placeholder and no guard. L25938 holds a fourth copy of the
# split(/^## /m) preamble defect in the magic-link path. Three functions, one
# copied pattern; today fixed one.

# index.html moved 844d3887 → 3ff93df4 on 3 Sep 2026, batch #9 second shipment
# (#96A title stripping, #96B preamble drop, #97 placeholder omission). NAMED BY
# THE LANE after reading all three edits at the bytes and confirming a uniform
# +17 shift below line 30300 with nothing above it moved. #96C — extending
# stripMd for horizontal rules and pipe tables — was DELIBERATELY HELD: the
# catman prompt instructs the model to emit pipe tables, so the change would
# move Business Case output, and stripMd is shared.
#   after   3ff93df4ce60cf322a0e35d292a135533f56de41c868ad67cfc25cd0752a2428
#           41,083 lines · 2,707,508 bytes · 2 blocks (1176, 39565)

# index.html moved a7f44f07 → 844d3887 on 3 Sep 2026, batch #9 finding #95
# (computed value column in the Brand Pack compliance rows). NAMED BY THE LANE
# after reading the edit at the bytes and confirming the shift pattern: a
# uniform +12 on every line below the insertion point and nothing above it
# moved. CC reported the same value; that was not what made it proceedable.
#   before  a7f44f073b3a425669c2054aacc878e8dfb819a6d380b6d582d92d903184b5b5
#   after   844d3887ccc22c15679fad5c79d7e60e174a52fe3b9526d4aa6ffa2528f4558d
#           41,066 lines · 2,705,805 bytes · 2 blocks (1176, 39548)

# FULL digests, captured 2 Sep 2026 20:56 CEST at HEAD 649f0ef, on a run whose
# cross-channel witnesses (line count, block spans, five verbatim lines, three
# literal counts) were confirmed line-for-line against the coding lane's own
# Read of the same file. That is the moment to record these: the tree was clean,
# both channels were proven to be looking at one file, and every prefix above
# matched. Recording them later would mean capturing at a less-verified moment.
#
# Column 2 above stays 8 chars because the comparison in verify.sh is prefix-
# based. Upgrading that compare to the full 64 — 256 bits instead of 32 — is
# queued behind the TDZ port and stops 3 and 4. These values are here so the
# upgrade needs no new capture.
#
#   index.html        a7f44f073b3a425669c2054aacc878e8dfb819a6d380b6d582d92d903184b5b5
#   dpp/index.html    6e946a279f2d43827bfba12a3994ba0400cc4983b658a065dca4760bde21357d
#   portal.html       b96423ac3c931561ed29329d2864e884467dc8b6605b6c44f8e5fbbb24d2b408
#   brand/index.html  1e99d32c74b373a46bb02103ed9847ba81749d841c9dc942d012968dd487f775
#
# index.html at this digest: 41,054 lines · 2,704,870 bytes · 2 blocks

# Provenance notes carried from §3:
#   index.html        41,054 lines · 2 inline blocks at html-lines 1176, 39536
#   dpp/index.html    unchanged since batch #7
#   portal.html       unchanged since batch #5  [SUPERSEDED 7 Sep 2026 — #113]
#   brand/index.html  unchanged since batch #5
#
# The handoff quotes these as truncated prefixes (a7f44f07…), so this file can
# only assert 8 hex characters — 32 bits. That is ample against accidental
# drift and is NOT a tamper check. If a full digest is ever needed, capture it
# from a tree whose provenance the lane has established, not from a report.
